---
tags: [教程, 卷四, 测试, 工程]
day: 33
aliases: [ch54]
---

# 第 54 章 · 给数学库写测试

> [!abstract] 本章目标
> 用断言驱动的方式给 mathlib 写一套测试，
> 以后每次改数学代码都跑一遍，防止改坏。

> [!note] 承上
> 上一章写完了 `mathlib`。
> 数学 bug 的特点是"不崩只错"——本章用**断言驱动**给 mathlib 写一套测试，防止改坏。

## 先看为什么要写

数学代码的 bug 有个特点：**不会崩，只会错**。

```cpp
// 少了个负号
screen.Y = (1.0f + ndc_y) * py;    // 应该是 1.0f - ndc_y
```

程序照常跑，画面上下颠倒。你可能调半天偏移都找不出原因。

**测试能在改完的 1 秒内告诉你哪里错了。**

## 断言测试的基本套路

```cpp
#include <cassert>
#include <cmath>
#include <cstdio>

static bool Near(float a, float b, float eps = 1e-4f) {
    return std::fabs(a - b) < eps;
}

int main(void) {
    // 每个行为一条断言
    assert(Near(Vec3{3,4,0}.Length(), 5.0f));
    printf("通过：长度\n");
    return 0;
}
```

> [!note] 上面的 `Vec3` 来自第 53 章的 `mathlib.h`
> 这段是**测试套路的骨架演示**，单独复制会报 `Vec3 未定义`。
> 真正跑测试时，在文件顶部 `#include "mathlib.h"`（第 53 章保存的那个）即可。

`assert` 在 `NDEBUG` 下会失效（第 10 章），
所以测试程序**永远不要定义 NDEBUG**。

## 一个简单的测试框架

不引入 gtest，自己写一个够用的：

```cpp
// test_framework.h
#pragma once
#include <cstdio>
#include <cmath>
#include <string>

static int g_tests_run = 0;
static int g_tests_failed = 0;
static std::string g_current;

#define TEST(name) \
    g_current = name; g_tests_run++; \
    printf("[ RUN ] %s\n", name);

#define CHECK(cond) do { \
    if (!(cond)) { \
        g_tests_failed++; \
        printf("  [FAIL] %s:%d  %s\n", __FILE__, __LINE__, #cond); \
    } \
} while(0)

#define CHECK_NEAR(a, b) do { \
    float va = (a), vb = (b); \
    if (std::fabs(va - vb) > 1e-4f) { \
        g_tests_failed++; \
        printf("  [FAIL] %s:%d  %s = %f, 期望 %f\n", \
               __FILE__, __LINE__, #a, va, (float)(vb)); \
    } \
} while(0)

#define CHECK_NEAR_EPS(a, b, eps) do { \
    float va = (a), vb = (b); \
    if (std::fabs(va - vb) > (eps)) { \
        g_tests_failed++; \
        printf("  [FAIL] %s:%d  %s = %f, 期望 %f (eps=%f)\n", \
               __FILE__, __LINE__, #a, va, (float)(vb), (float)(eps)); \
    } \
} while(0)

static int ReportTests() {
    printf("\n共 %d 项，失败 %d 项\n", g_tests_run, g_tests_failed);
    return g_tests_failed == 0 ? 0 : 1;
}
```

`CHECK(cond)` 打印的是**表达式文本**（`#cond` 字符串化），
一眼看出哪条断言挂了。

## mathlib 的完整测试

```cpp
// mathlib_test.cpp
#include "mathlib.h"
#include "test_framework.h"
#include <cstdio>

using namespace ml;

void TestVec3() {
    TEST("Vec3 基础运算");
    // 注意：CHECK_NEAR 只能比较两个"数"，不能直接比较两个向量。
    // 别写成 CHECK_NEAR(Vec3{...} + Vec3{...}, Vec3{...}.Length())——那样编译不过。
    // 正确做法是逐分量检查：
    Vec3 sum = Vec3{1,2,3} + Vec3{10,20,30};
    CHECK_NEAR(sum.x, 11); CHECK_NEAR(sum.y, 22); CHECK_NEAR(sum.z, 33);

    Vec3 diff = Vec3{10,20,30} - Vec3{1,2,3};
    CHECK_NEAR(diff.x, 9); CHECK_NEAR(diff.y, 18); CHECK_NEAR(diff.z, 27);

    TEST("Vec3 长度与归一化");
    CHECK_NEAR(Vec3(3,4,0).Length(), 5.0f);
    CHECK_NEAR(Vec3(1,0,0).Length(), 1.0f);
    CHECK_NEAR(Vec3(3,4,0).Normalized().Length(), 1.0f);
    CHECK_NEAR(Vec3(0,0,0).Normalized().LengthSq(), 0.0f);   // 零向量不产生 NaN

    TEST("Vec3 点积与叉积");
    CHECK_NEAR(Vec3(1,0,0).Dot(Vec3(0,1,0)), 0.0f);          // 垂直
    CHECK_NEAR(Vec3(1,0,0).Dot(Vec3(1,0,0)), 1.0f);
    CHECK_NEAR(Vec3(2,0,0).Dot(Vec3(3,0,0)), 6.0f);

    Vec3 cr = Vec3{1,0,0}.Cross(Vec3{0,1,0});
    CHECK_NEAR(cr.x, 0); CHECK_NEAR(cr.y, 0); CHECK_NEAR(cr.z, 1);  // 叉积代数定义：x̂ × ŷ = ẑ
    CHECK_NEAR(cr.Dot(Vec3{1,0,0}), 0.0f);     // 与两边都垂直
    CHECK_NEAR(cr.Dot(Vec3{0,1,0}), 0.0f);

    TEST("Vec3 距离");
    CHECK_NEAR(Vec3::Distance({0,0,0}, {3,4,0}), 5.0f);
    CHECK_NEAR(Vec3::DistanceSq({0,0,0}, {3,4,0}), 25.0f);
}

void TestAngle() {
    TEST("角度归一化");
    CHECK_NEAR(NormalizeAngle(0), 0);
    CHECK_NEAR(NormalizeAngle(180), 180);
    CHECK_NEAR(NormalizeAngle(190), -170);
    CHECK_NEAR(NormalizeAngle(-190), 170);
    CHECK_NEAR(NormalizeAngle(720), 0);

    TEST("角度差走最短路");
    CHECK_NEAR(AngleDelta(350, 10), 20);      // 不是 -340
    CHECK_NEAR(AngleDelta(10, 350), -20);
    CHECK_NEAR(AngleDelta(0, 90), 90);
    CHECK_NEAR(AngleDelta(-170, 170), -20);   // 跨 ±180 边界
}

void TestRotator() {
    TEST("前方向");
    Vec3 f0 = RotatorToForward({0, 0, 0});
    CHECK_NEAR(f0.x, 1); CHECK_NEAR(f0.y, 0); CHECK_NEAR(f0.z, 0);

    Vec3 f90 = RotatorToForward({0, 90, 0});
    CHECK_NEAR(f90.x, 0); CHECK_NEAR(f90.y, 1); CHECK_NEAR(f90.z, 0);

    Vec3 fUp = RotatorToForward({90, 0, 0});
    CHECK_NEAR(fUp.x, 0); CHECK_NEAR(fUp.y, 0); CHECK_NEAR(fUp.z, 1);

    TEST("方向 ↔ 角度 往返");
    Vec3 dirs[] = {{1,0,0}, {0,1,0}, {1,1,0}, {3,4,5}, {-1,0,0}};
    for (const auto& d : dirs) {
        Rotator r = DirectionToRotator(d);
        Vec3 back = RotatorToForward(r);
        Vec3 nd = d.Normalized();
        // 方向应一致（点积接近 1）
        CHECK_NEAR_EPS(nd.Dot(back), 1.0f, 1e-4f);
    }

    TEST("俯仰角符号");
    Rotator up = DirectionToRotator({1, 0, 1});    // 前上方
    CHECK(up.Pitch > 0);
    Rotator down = DirectionToRotator({1, 0, -1}); // 前下方
    CHECK(down.Pitch < 0);
}

void TestQuat() {
    TEST("单位四元数");
    Quat q;   // 默认 (0,0,0,1)
    Mat4 m = QuatToMatrix(q, Vec3{}, Vec3{1,1,1});
    CHECK_NEAR(m.M[0][0], 1); CHECK_NEAR(m.M[1][1], 1); CHECK_NEAR(m.M[2][2], 1);

    TEST("绕 Z 转 90 度");
    Quat q90 = QuatFromAxisAngle({0,0,1}, 90);
    CHECK_NEAR(q90.z, 0.70710678f);
    CHECK_NEAR(q90.w, 0.70710678f);
    CHECK_NEAR(q90.x, 0); CHECK_NEAR(q90.y, 0);

    TEST("四元数矩阵是正交的（旋转矩阵三个行向量长度都为 1）");
    Quat q37 = QuatFromAxisAngle({1,2,3}, 37);
    Mat4 mm = QuatToMatrix(q37, Vec3{}, Vec3{1,1,1});
    for (int i = 0; i < 3; i++) {
        Vec3 row{mm.M[i][0], mm.M[i][1], mm.M[i][2]};
        CHECK_NEAR(row.Length(), 1.0f);
    }
}

void TestMatrix() {
    TEST("单位矩阵");
    Mat4 I = Mat4::Identity();
    CHECK_NEAR(I.M[0][0], 1); CHECK_NEAR(I.M[1][2], 0); CHECK_NEAR(I.M[3][3], 1);

    TEST("平移与取位置");
    Mat4 t = Mat4Translation({10, 20, 30});
    Vec3 p = Mat4GetPosition(t);
    CHECK_NEAR(p.x, 10); CHECK_NEAR(p.y, 20); CHECK_NEAR(p.z, 30);

    TEST("乘法不可交换");
    Mat4 A = Mat4Translation({10,0,0});
    Mat4 B = Mat4Scale({2,2,2});
    // 只看结果的平移分量（相当于点取原点）：
    //   Multiply(A,B) = A*B = T*S：A 的平移被 B 的缩放放大 → 10*2 = 20
    //   Multiply(B,A) = B*A = S*T：B 是纯缩放，平移分量不变 → 10
    // 两者不同，即证明不可交换。
    Vec3 p1 = Mat4GetPosition(Mat4Multiply(A, B));
    Vec3 p2 = Mat4GetPosition(Mat4Multiply(B, A));
    CHECK_NEAR(p1.x, 20);    // T*S：平移被缩放放大
    CHECK_NEAR(p2.x, 10);    // S*T：平移不变

    TEST("转置两次回到原矩阵");
    Mat4 src = Mat4Translation({1,2,3});
    Mat4 tt = Mat4Transpose(Mat4Transpose(src));
    CHECK_NEAR(tt.M[3][0], 1); CHECK_NEAR(tt.M[3][1], 2); CHECK_NEAR(tt.M[3][2], 3);
}

void TestCamera() {
    Camera cam;
    cam.Position   = {0, 0, 50};
    cam.Rotation   = {0, 0, 0};
    cam.FOV        = 90.0f;
    cam.HalfWidth  = 1080.0f;
    cam.HalfHeight = 1200.0f;
    CHECK(cam.BuildMatrix());

    Vec2 s;

    TEST("W2S 正前方在屏幕中心");
    CHECK(cam.WorldToScreen({100, 0, 50}, s));
    CHECK_NEAR(s.x, 1080); CHECK_NEAR(s.y, 1200);

    TEST("W2S 右方偏右、左方偏左（对称）");
    CHECK(cam.WorldToScreen({100, 50, 50}, s));
    CHECK_NEAR(s.x, 1620); CHECK_NEAR(s.y, 1200);
    CHECK(cam.WorldToScreen({100, -50, 50}, s));
    CHECK_NEAR(s.x, 540);  CHECK_NEAR(s.y, 1200);

    TEST("W2S 上方偏上、下方偏下（对称）");
    CHECK(cam.WorldToScreen({100, 0, 100}, s));
    CHECK_NEAR(s.x, 1080); CHECK_NEAR(s.y, 600);
    CHECK(cam.WorldToScreen({100, 0, 0}, s));
    CHECK_NEAR(s.x, 1080); CHECK_NEAR(s.y, 1800);

    TEST("W2S 透视：距离翻倍偏移减半");
    Vec2 nearP, farP;
    cam.WorldToScreen({100, 50, 50}, nearP);
    cam.WorldToScreen({200, 50, 50}, farP);
    CHECK_NEAR((nearP.x - 1080) / (farP.x - 1080), 2.0f);

    TEST("W2S 后方不可见");
    CHECK(!cam.WorldToScreen({-100, 0, 50}, s));

    TEST("W2S 焦距系数生效");
    cam.Kx = 2.0f;
    cam.WorldToScreen({100, 50, 50}, s);
    CHECK_NEAR(s.x, 1080 + 540*2);      // 偏移加倍
    cam.Kx = 1.0f;

    TEST("无效相机被拒绝");
    Camera bad;
    bad.Position = {0,0,0};             // 位置为 0
    CHECK(!bad.BuildMatrix());
    Camera bad2;
    bad2.Position = {1,1,1};
    bad2.FOV = 200;                     // FOV 超范围
    CHECK(!bad2.BuildMatrix());
}

int main(void) {
    TestVec3();
    TestAngle();
    TestRotator();
    TestQuat();
    TestMatrix();
    TestCamera();
    return ReportTests();
}
```

编译运行：

```bash
g++ -std=c++17 -Wall -Wextra mathlib_test.cpp -o mathlib_test -lm
./mathlib_test
```

## 每个测试背后的"为什么"

| 测试 | 防止什么错误 |
|---|---|
| 零向量归一化 | 除零产生 NaN，污染全链路 |
| 点积垂直为 0 | 基向量算错 |
| 叉积与两边垂直 | 叉积公式写错（最容易搞错符号） |
| 角度差走最短路 | 自瞄转到反向（绕一大圈） |
| 方向↔角度往返 | `atan2` 参数顺序错 |
| 四元数矩阵行向量长 1 | 四元数转矩阵公式错（旋转会带缩放） |
| 乘法不可交换 | 变换顺序写反 |
| W2S 对称性 | 某个轴符号错 |
| W2S 透视比例 | 忘除 w |
| 后方不可见 | 近平面判断缺失 |
| 无效相机被拒 | 防御检查被误删 |

> [!tip] 测试就是"可执行的文档"
> 半年后你忘了 `AngleDelta` 为什么要归一化，
> 看一眼测试用例 `AngleDelta(350, 10) == 20` 就明白了。

## 把测试接入构建

**方案 1：独立可执行文件（推荐）**

```makefile
include $(CLEAR_VARS)
LOCAL_MODULE := mathlib_test
LOCAL_CPPFLAGS := -std=c++17 -Wall
LOCAL_SRC_FILES := tests/mathlib_test.cpp
include $(BUILD_EXECUTABLE)
```

在电脑上编译运行（用本地 gcc 就行，不需要 NDK）：

```bash
g++ -std=c++17 tests/mathlib_test.cpp -o /tmp/t && /tmp/t
```

**方案 2：`#ifdef` 开关**

```cpp
#ifdef MATHLIB_SELF_TEST
int main(void) { /* 测试 */ }
#endif
```

主程序用 `atexit` 或命令行参数触发。

**方案 3：CI**（专业做法）

```yaml
# .github/workflows/test.yml
- run: g++ -std=c++17 tests/mathlib_test.cpp -o t && ./t
```

每次提交自动跑。

## 什么时候该加测试

| 情况 | 加测试 |
|---|---|
| 数学/算法函数 | **必加** |
| 有边界条件（除零、NaN、溢出） | **必加** |
| 修了一个 bug | **加一条回归测试** ★ |
| 简单的 getter/setter | 不用 |
| UI 绘制代码 | 难测，靠肉眼 |

**最重要的是第 3 条**：每修一个 bug，加一条测试防止它复发。

## 动手

1. 把这套测试跑起来，确认全绿
2. 故意在 mathlib.h 里改错一处（比如 `1.0f - cy` 改成 `1.0f + cy`），
   看测试能不能抓到
3. 再改一处（比如 `NormalizeAngle` 的 `<=` 改成 `<`），看能否抓到
4. 恢复，确认全绿

第 2、3 步特别重要——**它能验证你的测试真的有效**。
如果改错了测试还全绿，说明测试写漏了。

> [!note] 这个方法有名字：**变异测试**（Mutation Testing）
> 做法：**故意往代码里植入一个错误（"变异体"）**，然后跑测试。
> - 测试**变红**（抓到了）→ 说明这条测试有效，能防住这类错误；
> - 测试**还是全绿**（没抓到）→ 说明测试有漏，那段逻辑根本没人测。
>
> 它是衡量"测试质量"最直接的手段：**测试覆盖率高 ≠ 测试有效**，
> 能抓住人为植入的错误，才是真的有效。

## 常见坑排查表

| 症状 | 最可能的原因 | 解法 |
|---|---|---|
| 测试**永远通过**（改错也不报） | 定义了 `NDEBUG` | 测试程序**永不**定义 `NDEBUG` |
| `assert` 没触发 | 编译加了 `-DNDEBUG` | 去掉该宏 |
| 浮点断言**偶发失败** | 用了精确相等 | 用 `Near(a, b, eps)` 比差值 |
| 测试骨架**编译不过** | 缺 `Vec3` 定义 | 顶部 `#include "mathlib.h"` |

## 验收清单

- [ ] 写了 test_framework.h（TEST/CHECK/CHECK_NEAR）（三个宏可用）
- [ ] mathlib 的测试覆盖了向量/角度/旋转/四元数/矩阵/相机（每类至少一个测试）
- [ ] **所有测试通过** ★（运行测试，输出全绿）
- [ ] 故意改坏代码，确认测试能抓到（变异测试）（改错一个函数，测试变红）
- [ ] 知道"修 bug 要加回归测试"（说出为什么）
- [ ] 把测试接入了构建或本地脚本（`make test` 或 `./test.sh` 能跑）

→ 下一章：[[第55章-浮点误差与数值稳定]]　—— 知道误差从哪来、怎么累积、怎么防。
