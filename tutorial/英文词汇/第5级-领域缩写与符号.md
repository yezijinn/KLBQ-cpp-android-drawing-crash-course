---
tags: [教程, 词汇, 词频]
aliases: [W5, vocab L5]
---


# W5 · 第 5 级：领域缩写与符号（150 词）

> [!abstract] 本册在整套词汇表里的位置
> 代码里 90% 的短名都是缩写；这里给出缩写、全称、读法与含义
>
> **本册排序 = 项目源码里的真实出现次数**（从多到少）。
> 频次统计口径：`jni/` 下 97 个源文件、35 万行代码。
> 每个词条都附**这个项目里的真实例句**（含文件名与行号）。
> 音标采用**英式 IPA（DJ）**；与附录 K 的美式 IPA 差异见 [[词频词汇总览|W0]]。


## 本册词条

- [第 1 组（1–50）](#第-1-组)
- [第 2 组（51–100）](#第-2-组)
- [第 3 组（101–150）](#第-3-组)


---

## 第 1 组

- **001 `px`**　读作 ˈpɪkslz 或 piks　*缩写*　pixel(s) 像素　【2694 次 / 40 个文件】
    - **用法**：`px` 也常作坐标变量名，如 `x_px`。
    - **频次**：标识符 2648 次 · 注释 46 次
    - **例句**：`// screenX = px + row0(x) / camera * px (水平焦距 px/t)`
    - **中文翻译**：screenX = px + row0(x) / camera * px（水平焦距 px/t）。
    - **位置**：`jni/include/Android_draw/WorldToScreen.h:49`
- **002 `buf`**　读作 bʌf　*缩写*　buffer 缓冲区　【961 次 / 11 个文件】
    - **用法**：`buf` 和 `buffer` 混用，看变量大小写风格。
    - **频次**：标识符 927 次 · 注释 34 次
    - **例句**：`// - ImGuiInputTextFlags_CallbackResize: Callback on buffer capacity changes request (beyond 'buf_size' parameter value), allowing the string to grow.`
    - **中文翻译**：ImGuiInputTextFlags_CallbackResize：当缓冲区容量需要扩大（超出 'buf_size' 参数）时回调，使字符串可以增长。
    - **位置**：`jni/include/ImGui/imgui.h:2678`
- **003 `tex`**　读作 teks　*缩写*　texture 纹理　【955 次 / 12 个文件】
    - **用法**：`texCoord` = 纹理坐标（UV）。
    - **频次**：标识符 917 次 · 注释 38 次
    - **例句**：`// inline ImTextureRef ImTextureRefFromID(ImTextureID tex_id) { ImTextureRef tex_ref = { ._TexData = NULL, .TexID = tex_id }; return tex_ref; }`
    - **中文翻译**：内联函数：用给定的 ImTextureID 构造一个 ImTextureRef（纹理引用）。
    - **位置**：`jni/include/ImGui/imgui.h:363`
- **004 `ptr`**　读作 ˈpɔɪntə(r)　*缩写*　pointer 指针　【627 次 / 28 个文件】
    - **用法**：`ptr` 是变量名后缀或前缀的常见写法。
    - **频次**：标识符 615 次 · 注释 12 次
    - **例句**：`// if current ptr not contains or current ptr is height field, load it data!`
    - **中文翻译**：如果当前指针不含目标，或者当前指针是一个高度场，就加载它的数据！
    - **位置**：`jni/include/Embree/PhysX.h:1263`
- **005 `aa`**　读作 ˌeɪ-ˈeɪ　*缩写*　anti-aliasing 抗锯齿　【624 次 / 7 个文件】
    - **用法**：`ImGui::GetStyle().AntiAliasedLines`。**只出现 2 个字母，但含义重要**。
    - **频次**：标识符 599 次 · 注释 25 次
    - **例句**：`#define ICON_FA_ANCHOR_CIRCLE_CHECK "\xee\x92\xaa" // U+e4aa`
    - **中文翻译**：定义 FontAwesome 图标「带勾的锚形圆圈」的转义编码（Unicode 码位 U+e4aa）。
    - **位置**：`jni/include/ImGui/gui_icon.h:46`
- **006 `src`**　读作 sɔːs　*缩写*　source 源　【621 次 / 16 个文件】
    - **用法**：`src` 与 `dst` 成对出现于复制/转换代码。
    - **频次**：标识符 605 次 · 注释 16 次
    - **例句**：`//! Transform transform to parent (returns compound transform: first src, then *this)`
    - **中文翻译**：把变换转换到父级坐标系（返回复合变换：先应用 src，再应用 *this）。
    - **位置**：`jni/include/Embree/foundation/PxTransform.h:135`
- **007 `comp`**　读作 kəmˈpiːt / kəmˈpəʊnənt　*缩写*　compare 比较 / component 分量　【571 次 / 3 个文件】
    - **用法**：**两义要靠上下文**：`comp` 在压缩代码/纹理里是 compress，在几何里是 component，在排序里是 compare。
    - **频次**：标识符 559 次 · 注释 12 次
    - **例句**：`// - GIF always returns *comp=4`
    - **中文翻译**：解码 GIF 时总是返回 *comp=4（即 4 个通道/RGBA）。
    - **位置**：`jni/include/ImGui/stb_image.h:135`
- **008 `vulkan`**　/ˈvʌlkən/　*专名*　Vulkan（图形 API 名字）　【561 次 / 8 个文件】
    - **用法**：来自罗马火神 Vulcan。安卓上的现代图形接口（第 58 章）。
    - **频次**：标识符 431 次 · 注释 130 次
    - **例句**：`// dear imgui: Renderer Backend for Vulkan`
    - **中文翻译**：dear imgui：面向 Vulkan 的渲染后端。
    - **位置**：`jni/include/ImGui/imgui_impl_vulkan.h:1`
- **009 `bg`**　读作 ˌbiː-ˈdʒiː　*缩写*　background 背景　【522 次 / 6 个文件】
    - **用法**：`bg color` = 背景色。与 `fg`（foreground 前景）成对。
    - **频次**：标识符 468 次 · 注释 54 次
    - **例句**：`// - Use to split render into layers. By switching channels to can render out-of-order (e.g. submit FG primitives before BG primitives)`
    - **中文翻译**：用于把渲染拆分成多个图层。通过切换通道，可以打乱顺序渲染（例如先提交前景图元，再提交背景图元）。
    - **位置**：`jni/include/ImGui/imgui.h:3442`
- **010 `pfn`**　读作 ˌpiː-ef-ˈen　*缩写*　pointer to function 函数指针　【522 次 / 5 个文件】
    - **用法**：Vulkan 的加载器风格命名：`PFN_vkCreateInstance`。**看到 pfn 就知道是个函数指针类型**。
    - **频次**：标识符 522 次 · 注释 0 次
    - **例句**：`IMGUI_IMPL_API bool ImGui_ImplVulkan_LoadFunctions(uint32_t api_version, PFN_vkVoidFunction(*loader_func)(const char* function_name, void* user_data), void* user_data = nullptr);`
    - **中文翻译**：IMGUI_IMPL_API 函数：加载 Vulkan 函数集，传入 API 版本号、加载器回调 loader_func 及用户数据 user_data。
    - **位置**：`jni/include/ImGui/imgui_impl_vulkan.h:166`
- **011 `len`**　读作 leŋθ 或 len　*缩写*　length 长度　【508 次 / 17 个文件】
    - **用法**：**注意拼写是 length 不是 lenght**。
    - **频次**：标识符 492 次 · 注释 16 次
    - **例句**：`// - Note that all functions taking format strings in the API may be passed ("%s", text) or ("%.*s", text_len, text): which will automatically bypass the formatter.`
    - **中文翻译**：注意：所有接收格式字符串的 API 函数既可以传 ("%s", text)，也可以传 ("%.*s", text_len, text)，后者会自动绕过格式化器。
    - **位置**：`jni/include/ImGui/imgui.h:616`
- **012 `fd`**　读作 ˌef-ˈdiː　*缩写*　file descriptor 文件描述符　【481 次 / 11 个文件】
    - **用法**：一个非负小整数，代表打开的文件/套接字（第 22 章）。
    - **频次**：标识符 469 次 · 注释 12 次
    - **例句**：`#define ICON_FA_LAYER_GROUP "\xef\x97\xbd" // U+f5fd`
    - **中文翻译**：定义 FontAwesome 图标「图层组」的转义编码（U+f5fd）。
    - **位置**：`jni/include/ImGui/gui_icon.h:754`
- **013 `std`**　读作 ˌes-tiː-ˈdiː 或 stændəd　*缩写*　standard 标准库命名空间　【474 次 / 26 个文件】
    - **用法**：`std::vector` = 标准库的 vector。**不要写 `using namespace std;` 在头文件里**。
    - **频次**：标识符 453 次 · 注释 21 次
    - **例句**：`//std::mutex mutex_;`
    - **中文翻译**：std::mutex mutex_;（互斥量成员，用于线程同步）。
    - **位置**：`jni/include/Embree/PhysX.h:1682`
- **014 `img`**　读作 ˈɪmɪdʒ　*缩写*　image 图像　【470 次 / 1 个文件】
    - **用法**：`Img` 在 ImGui 里也指该库自身。
    - **频次**：标识符 459 次 · 注释 11 次
    - **例句**：`// where s->img_buffer isn't pointing to safe memory, e.g. 0-byte file`
    - **中文翻译**：例如当 s->img_buffer 没有指向安全内存时（比如 0 字节文件）。
    - **位置**：`jni/src/ImGui/stb_image.cpp:1059`
- **015 `ref`**　读作 ˈrefrəns　*缩写*　reference 引用　【432 次 / 10 个文件】
    - **用法**：C++ 的 `T&`（第 12 章）。`ref count` = 引用计数。
    - **频次**：标识符 414 次 · 注释 18 次
    - **例句**：`// inline ImTextureRef ImTextureRefFromID(ImTextureID tex_id) { ImTextureRef tex_ref = { ._TexData = NULL, .TexID = tex_id }; return tex_ref; }`
    - **中文翻译**：内联函数：用给定的 ImTextureID 构造一个 ImTextureRef（纹理引用）。
    - **位置**：`jni/include/ImGui/imgui.h:363`
- **016 `vtx`**　读作 veks 或 ˈvɜːteks　*缩写*　vertex 顶点　【405 次 / 7 个文件】
    - **用法**：`vtx` 与 `vertex` 混用，出现在顶点缓冲变量名里。
    - **频次**：标识符 398 次 · 注释 7 次
    - **例句**：`// FIXME: In theory we should be testing that vtx_count <64k here.`
    - **中文翻译**：待办：理论上我们应该在这里测试 vtx_count < 64k。
    - **位置**：`jni/src/ImGui/imgui_draw.cpp:739`
- **017 `cuda`**　/ˈkuːdə/　*专名*　NVIDIA 的 GPU 计算平台　【402 次 / 14 个文件】
    - **用法**：`Compute Unified Device Architecture`。Embree 用它做 GPU 加速。
    - **频次**：标识符 398 次 · 注释 4 次
    - **例句**：`//CUDA is currently supported only on windows`
    - **中文翻译**：CUDA 目前只在 Windows 上受支持。
    - **位置**：`jni/include/Embree/foundation/PxPreprocessor.h:530`
- **018 `num`**　读作 ˈnʌmbə(r)　*缩写*　number 数量/编号　【384 次 / 16 个文件】
    - **用法**：`numVertices` = 顶点数量。
    - **频次**：标识符 347 次 · 注释 37 次
    - **例句**：`// (Integer encoded as XYYZZ for use in #if preprocessor conditionals, e.g. '#if IMGUI_VERSION_NUM >= 12345')`
    - **中文翻译**：版本号用 XYYZZ 形式编码，便于在 #if 预处理条件中使用，例如 '#if IMGUI_VERSION_NUM >= 12345'。
    - **位置**：`jni/include/ImGui/imgui.h:31`
- **019 `args`**　读作 ˈɑːɡjumənts　*缩写*　arguments 参数（复数）　【356 次 / 11 个文件】
    - **用法**：`args` 是 `argv` 或变参宏的常见变量名。
    - **频次**：标识符 351 次 · 注释 5 次
    - **例句**：`// - Functions args 'int column_n' treat the default value of -1 as the same as passing the current column index.`
    - **中文翻译**：函数参数 'int column_n' 把默认值 -1 视为等同于传入当前列索引。
    - **位置**：`jni/include/ImGui/imgui.h:932`
- **020 `khr`**　读作 ˌkeɪ-eɪtʃ-ˈɑː(r)　*缩写*　Khronos 扩展标记　【354 次 / 5 个文件】
    - **用法**：`VK_KHR_swapchain` = Khronos 组织标准化后的扩展。**没有 KHR 的是厂商私有扩展**。
    - **频次**：标识符 331 次 · 注释 23 次
    - **例句**：`// Need to explicitly enable VK_KHR_dynamic_rendering extension to use this, even for Vulkan 1.3 + setup PipelineInfoMain.PipelineRenderingCreateInfo.`
    - **中文翻译**：即使 Vulkan 1.3 也要显式启用 VK_KHR_dynamic_rendering 扩展才能用这个，并设置 PipelineInfoMain.PipelineRenderingCreateInfo。
    - **位置**：`jni/include/ImGui/imgui_impl_vulkan.h:126`
- **021 `req`**　读作 rɪˈkwest　*缩写*　request 请求　【349 次 / 8 个文件】
    - **用法**：与 `resp`（response 响应）成对（第 79 章）。
    - **频次**：标识符 331 次 · 注释 18 次
    - **例句**：`// 此接口只负责启停监控，系统调用日志由驱动写入内核 printk 环形缓冲区，不通过 req 返回。`
    - **中文翻译**：此接口只负责启动/停止监控；系统调用日志由驱动写入内核的 printk 环形缓冲区，不通过 req 返回。
    - **位置**：`jni/src/Android_draw/driver.h:1255`
- **022 `prev`**　读作 ˈpriːviəs　*缩写*　previous 上一个　【342 次 / 9 个文件】
    - **用法**：`prev` / `next` 是链表指针的标准命名（题 2.4）。
    - **频次**：标识符 328 次 · 注释 14 次
    - **例句**：`// - Case 4: tab backward: store all results, on ref id pick prev, stop storing`
    - **中文翻译**：情形 4（向后切换标签页）：保存所有结果，遇到引用 ID 时取前一个，然后停止保存。
    - **位置**：`jni/src/ImGui/imgui.cpp:13679`
- **023 `err`**　读作 erə(r)　*缩写*　error 错误　【338 次 / 5 个文件】
    - **用法**：`errno` = 错误码全局变量（第 22 章）。
    - **频次**：标识符 332 次 · 注释 6 次
    - **例句**：`// stbi__err - error`
    - **中文翻译**：stbi__err —— 错误处理函数。
    - **位置**：`jni/src/ImGui/stb_image.cpp:542`
- **024 `cd`**　读作 ˌsiː-ˈdiː　*缩写*　change directory 切换目录　【328 次 / 5 个文件】
    - **用法**：shell 命令；也是 `current directory` 的偶见缩写。**附录 R 讲过它的用法**。
    - **频次**：标识符 316 次 · 注释 12 次
    - **例句**：`#define ICON_FA_WHEAT_AWN "\xee\x8b\x8d" // U+e2cd`
    - **中文翻译**：定义 FontAwesome 图标「麦穗」的转义编码（U+e2cd）。
    - **位置**：`jni/include/ImGui/gui_icon.h:247`
- **025 `fmt`**　读作 ˈfɔːmæt　*缩写*　format 格式　【319 次 / 8 个文件】
    - **用法**：`fmt` 是格式字符串变量名；`fmt` 也是 C++ 的格式化库名。
    - **频次**：标识符 306 次 · 注释 13 次
    - **例句**：`// codepoint-to-glyph conversion using table fmt=12`
    - **中文翻译**：使用 fmt=12 格式的表来做「码点→字形」转换。
    - **位置**：`jni/include/ImGui/imstb_truetype.h:5034`
- **026 `uv`**　读作 ˌjuː-ˈviː　*缩写*　纹理坐标（两个分量）　【277 次 / 6 个文件】
    - **用法**：u 是横向，v 是纵向，取值 0–1。
    - **频次**：标识符 215 次 · 注释 62 次
    - **例句**：`// - 'uv0' and 'uv1' are texture coordinates. Read about them from the same link above.`
    - **中文翻译**：'uv0' 和 'uv1' 是纹理坐标；可以从上面同一个链接了解它们。
    - **位置**：`jni/include/ImGui/imgui.h:651`
- **027 `dst`**　读作 ˈdestɪneɪʃn 或 dst　*缩写*　destination 目标　【272 次 / 10 个文件】
    - **用法**：也写作 `dest`。谁被写，谁是 dst。
    - **频次**：标识符 260 次 · 注释 12 次
    - **例句**：`//IM_ASSERT(dst_column->DisplayOrder == dst_order - reorder_dir);`
    - **中文翻译**：IM_ASSERT(dst_column->DisplayOrder == dst_order - reorder_dir);（断言目标列显示顺序正确）。
    - **位置**：`jni/src/ImGui/imgui_tables.cpp:743`
- **028 `cfg`**　读作 kənˈfɪɡ　*缩写*　config 配置　【270 次 / 7 个文件】
    - **用法**：也写作 `conf`。
    - **频次**：标识符 258 次 · 注释 12 次
    - **例句**：`ImFontConfig cfg1;`
    - **中文翻译**：ImFontConfig 类型的变量 cfg1（字体配置结构体实例）。
    - **位置**：`jni/src/ImGui/imgui.cpp:535`
- **029 `var`**　读作 vɑː(r)　*缩写*　variable 变量 / 可变数据目录　【209 次 / 6 个文件】
    - **用法**：`/var` 放日志；编程里 `var` 是变量。
    - **频次**：标识符 206 次 · 注释 3 次
    - **例句**：`// init result var`
    - **中文翻译**：初始化结果变量。
    - **位置**：`jni/include/Embree/PhysX.h:1639`
- **030 `ini`**　读作 ˌaɪ-en-ˈaɪ 或 ˈiːni　*缩写*　initialization file 初始化配置文件　【206 次 / 10 个文件】
    - **用法**：`key=value` 分节格式。
    - **频次**：标识符 134 次 · 注释 72 次
    - **例句**：`//---- Use legacy CRC32-adler tables (used before 1.91.6), in order to preserve old .ini data that you cannot afford to invalidate.`
    - **中文翻译**：使用旧的 CRC32-adler 表（1.91.6 之前使用），以保留你无法承受其失效的旧 .ini 数据。
    - **位置**：`jni/include/ImGui/imconfig.h:69`
- **031 `ms`**　读作 ˈmɪlisekənd　*缩写*　millisecond 毫秒　【189 次 / 7 个文件】
    - **用法**：1 ms = 1000 μs。`usleep(1000)` 就是睡 1 毫秒。
    - **频次**：标识符 161 次 · 注释 28 次
    - **例句**：`// Ballpark estimate of cost on my 2019 desktop PC: for a 100 KB text buffer: +~0.3 ms (Optimized) / +~1.0 ms (Debug build).`
    - **中文翻译**：在我 2019 年的台式机上大致估算成本：一个 100 KB 的文本缓冲区，优化版约 +0.3 毫秒，调试版约 +1.0 毫秒。
    - **位置**：`jni/include/ImGui/imgui.h:1311`
- **032 `int`**　读作 ɪnt　*缩写*　integer 整数　【160 次 / 17 个文件】
    - **用法**：`int` 通常 32 位。`integer` 才是全称。
    - **频次**：标识符 0 次 · 注释 160 次
    - **例句**：`// fix unsign int`
    - **中文翻译**：修正无符号整数问题。
    - **位置**：`jni/include/Embree/PhysX.h:948`
- **033 `bool`**　读作 buːl（布尔）　*缩写*　boolean 布尔值　【130 次 / 8 个文件】
    - **用法**：只有 true/false 两个值。名字来自数学家 Boole。
    - **频次**：标识符 0 次 · 注释 130 次
    - **例句**：`// Android 15+: setTrustedOverlay uses gui::TrustedOverlay enum instead of bool`
    - **中文翻译**：Android 15 及以上：setTrustedOverlay 改用 gui::TrustedOverlay 枚举，而不是 bool。
    - **位置**：`jni/include/ImGui/ANativeWindowCreator.h:408`
- **034 `char`**　读作 tʃɑː(r) 或 kær　*缩写*　character 字符　【127 次 / 13 个文件】
    - **用法**：`char` 是 C/C++ 里最小的整数类型，通常 1 字节。
    - **频次**：标识符 0 次 · 注释 127 次
    - **例句**：`//char mFlagPad[0x3]{};`
    - **中文翻译**：char mFlagPad[0x3]{};（3 字节的填充字段）。
    - **位置**：`jni/include/Embree/PhysX.h:498`
- **035 `pid`**　读作 pɪd　*缩写*　process ID 进程号　【112 次 / 10 个文件】
    - **用法**：`/proc/<pid>/` 里的 pid。**你的第一个偏移起点就是别人的 pid**。
    - **频次**：标识符 108 次 · 注释 4 次
    - **例句**：`// 当前绑定的目标进程 (本项目"二选一"驱动需要读回 PID)`
    - **中文翻译**：当前绑定的目标进程（本项目的「二选一」驱动需要读回 PID）。
    - **位置**：`jni/include/My_Utils/SysHal.h:98`
- **036 `unicode`**　读作 ˈjuːnɪkəʊd　*n.*　统一码（字符集）　【109 次 / 8 个文件】
    - **用法**：给全世界字符统一编号。UTF-8/16 是它的编码方式。
    - **频次**：标识符 73 次 · 注释 36 次
    - **例句**：`//---- Use 32-bit for ImWchar (default is 16-bit) to support Unicode planes 1-16. (e.g. point beyond 0xFFFF like emoticons, dingbats, symbols, shapes, ancient languages, etc...)`
    - **中文翻译**：用 32 位存储 ImWchar（默认 16 位）以支持 Unicode 第 1–16 平面（例如 0xFFFF 之外的表情符号、装饰符、符号、图形、古文字等）。
    - **位置**：`jni/include/ImGui/imconfig.h:72`
- **037 `tmp`**　读作 temp　*缩写*　temporary 临时的　【100 次 / 10 个文件】
    - **用法**：`/tmp` 目录、临时变量。**注意是 tmp 不是 tpm**。
    - **频次**：标识符 100 次 · 注释 0 次
    - **例句**：`const char* tmp = reinterpret_cast<const char*>(mPolygons);`
    - **中文翻译**：把 mPolygons 用 reinterpret_cast 转为 const char* 指针，存入 tmp。
    - **位置**：`jni/include/Embree/PhysX.h:428`
- **038 `arg`**　读作 ˈɑːɡjumənt　*缩写*　argument 实参　【97 次 / 8 个文件】
    - **用法**：`argc/argv` = 参数个数/参数数组（第 08 章）。
    - **频次**：标识符 82 次 · 注释 15 次
    - **例句**：`//inline bool BeginChild(const char* str_id, const ImVec2& size_arg, bool borders, ImGuiWindowFlags window_flags) { return BeginChild(str_id, size_arg, borders ? ImGuiChildFlags_Borders : ImGuiChildFlags_None, window_flags); } // Unnecessary as true == ImGuiChildFlags_Borders`
    - **中文翻译**：内联兼容函数 BeginChild（旧签名）；因为 true == ImGuiChildFlags_Borders，这个重载其实是多余的。
    - **位置**：`jni/include/ImGui/imgui.h:4156`
- **039 `mem`**　读作 mem　*缩写*　memory 内存　【80 次 / 11 个文件】
    - **用法**：`mem` 也常指 `ReadProcessMemory` 一类函数。
    - **频次**：标识符 74 次 · 注释 6 次
    - **例句**：`// to "guard" illegal mem access, for example.`
    - **中文翻译**：用来「防范」非法的内存访问，例如。
    - **位置**：`jni/include/Embree/foundation/PxAssert.h:68`
- **040 `param`**　读作 pəˈræmɪtə(r)　*缩写*　parameter 参数　【79 次 / 13 个文件】
    - **用法**：`param1` 是自动生成的参数名，说明作者没起好名。
    - **频次**：标识符 0 次 · 注释 79 次
    - **例句**：`\param size Number of bytes to allocate.`
    - **中文翻译**：（Doxygen 参数）size：要分配的字节数。
    - **位置**：`jni/include/Embree/foundation/PxAllocatorCallback.h:71`
- **041 `addr`**　读作 əˈdres　*缩写*　address 地址　【78 次 / 7 个文件】
    - **用法**：`addr` = 内存地址。本项目一切从地址开始。
    - **频次**：标识符 75 次 · 注释 3 次
    - **例句**：`// dr->Read<uint64_t>(addr);`
    - **中文翻译**：dr->Read<uint64_t>(addr);（通过驱动读取 64 位无符号整数，地址为 addr）。
    - **位置**：`jni/src/Android_draw/driver.h:60`
- **042 `us`**　读作 ˈmaɪkrəʊsekənd　*缩写*　microsecond 微秒　【75 次 / 12 个文件】
    - **用法**：**注意：变量名 `us` 也常表示 unsigned short**，看上下文。
    - **频次**：标识符 12 次 · 注释 63 次
    - **例句**：`// The keyboard key enum values are named after the keys on a standard US keyboard, and on other keyboard types the keys reported may not match the keycaps.`
    - **中文翻译**：键盘按键的枚举值是以标准美式键盘上的按键命名的；在其他键盘类型上，上报的按键可能与键帽标记不符。
    - **位置**：`jni/include/ImGui/imgui.h:1572`
- **043 `syscall`**　读作 ˈsɪskɔːl　*n.*　system call 系统调用　【63 次 / 5 个文件】
    - **用法**：程序请求操作系统干活的唯一入口（第 21 章）。
    - **频次**：标识符 61 次 · 注释 2 次
    - **例句**：`// MEM_DRIVER_SYSCALL: SysHal (SysHal.h) -> process_vm_readv/writev 系统调用直读`
    - **中文翻译**：MEM_DRIVER_SYSCALL：SysHal（SysHal.h）→ 通过 process_vm_readv/writev 系统调用直接读取内存。
    - **位置**：`jni/include/My_Utils/MemDriver.h:7`
- **044 `cpu`**　读作 ˌsiː-piː-ˈjuː　*缩写*　Central Processing Unit 中央处理器　【62 次 / 8 个文件】
    - **用法**：`cpu` 时间 = 真正干活的时间。
    - **频次**：标识符 36 次 · 注释 26 次
    - **例句**：`// The CPU cost is very roughly proportional to text length, so a 10 KB buffer should cost about ten times less.`
    - **中文翻译**：CPU 开销大致与文本长度成正比，所以一个 10 KB 的缓冲区开销大约少十倍。
    - **位置**：`jni/include/ImGui/imgui.h:1312`
- **045 `hsv`**　读作 ˌeɪtʃ-es-ˈviː　*缩写*　色相/饱和度/明度　【62 次 / 4 个文件】
    - **用法**：比 RGB 更符合人的直觉，取色器常用。
    - **频次**：标识符 43 次 · 注释 19 次
    - **例句**：`//ImGuiColorEditFlags_RGB = ImGuiColorEditFlags_DisplayRGB, ImGuiColorEditFlags_HSV = ImGuiColorEditFlags_DisplayHSV, ImGuiColorEditFlags_HEX = ImGuiColorEditFlags_DisplayHex // [renamed in 1.69]`
    - **中文翻译**：将颜色编辑标志重命名（RGB/HSV/HEX 显示标志，1.69 改名）。
    - **位置**：`jni/include/ImGui/imgui.h:1953`
- **046 `mac`**　读作 mæk　*缩写*　Media Access Control 网卡物理地址　【60 次 / 7 个文件】
    - **用法**：6 字节的硬件地址。**与苹果电脑无关**。
    - **频次**：标识符 49 次 · 注释 11 次
    - **例句**：`//#define IMGUI_DISABLE_DEFAULT_SHELL_FUNCTIONS // Don't implement default platform_io.Platform_OpenInShellFn() handler (Win32: ShellExecute(), require shell32.lib/.a, Mac/Linux: use system("")).`
    - **中文翻译**：宏：禁用默认 shell 函数（不实现 Platform_OpenInShellFn 处理器）（Win32 用 ShellExecute()，需要 shell32.lib/.a；Mac/Linux 用 system("")）。
    - **位置**：`jni/include/ImGui/imconfig.h:47`
- **047 `eol`**　读作 ˌiː-əʊ-ˈel　*缩写*　End Of Line 行尾 / End Of Life 停止支持　【48 次 / 3 个文件】
    - **用法**：也要注意 `CRLF`（Windows）与 `LF`（Unix）的换行符差异。
    - **频次**：标识符 47 次 · 注释 1 次
    - **例句**：`// +1 may not be a character start point in UTF-8 but it's ok because caller loops use (text >= word_wrap_eol).`
    - **中文翻译**：+1 在 UTF-8 里可能不是字符的起点，但没关系，因为调用方的循环使用 (text >= word_wrap_eol) 判断。
    - **位置**：`jni/src/ImGui/imgui_draw.cpp:5669`
- **048 `gpu`**　读作 ˌdʒiː-piː-ˈjuː　*缩写*　Graphics Processing Unit 图形处理器　【48 次 / 7 个文件】
    - **用法**：画三角形的地方（第 57 章）。
    - **频次**：标识符 17 次 · 注释 31 次
    - **例句**：`// ImTextureID = backend specific, low-level identifier for a texture uploaded in GPU/graphics system.`
    - **中文翻译**：ImTextureID = 后端相关的底层标识符，表示已经上传到 GPU/图形系统的纹理。
    - **位置**：`jni/include/ImGui/imgui.h:323`
- **049 `crc`**　读作 ˌsiː-ɑː-ˈsiː　*缩写*　Cyclic Redundancy Check 循环冗余校验　【46 次 / 4 个文件】
    - **用法**：判断数据有没有被人动过。
    - **频次**：标识符 30 次 · 注释 16 次
    - **例句**：`//---- Use legacy CRC32-adler tables (used before 1.91.6), in order to preserve old .ini data that you cannot afford to invalidate.`
    - **中文翻译**：使用旧的 CRC32-adler 表（1.91.6 之前使用），以保留你无法承受其失效的旧 .ini 数据。
    - **位置**：`jni/include/ImGui/imconfig.h:69`
- **050 `cnt`**　读作 kaʊnt　*缩写*　count 计数　【44 次 / 1 个文件】
    - **用法**：与 `num` 混用，含义相同。
    - **频次**：标识符 44 次 · 注释 0 次


---

## 第 2 组

- **051 `simd`**　读作 ˈsɪmdɪː　*缩写*　Single Instruction Multiple Data 单指令多数据　【42 次 / 6 个文件】
    - **用法**：一条指令算 4/8 个数据。矩阵运算的加速手段。
    - **频次**：标识符 25 次 · 注释 17 次
    - **例句**：`// Emscripten has partial SSE 4.2 support where _mm_crc32_u32 is not available. See https://emscripten.org/docs/porting/simd.html#id11 and #8213`
    - **中文翻译**：Emscripten 对 SSE 4.2 只有部分支持，其中 _mm_crc32_u32 不可用。参见……以及 #8213。
    - **位置**：`jni/include/ImGui/imgui_internal.h:71`
- **052 `lib`**　读作 lɪb　*缩写*　library 库　【38 次 / 13 个文件】
    - **用法**：`/lib` 目录；`.so` 就是 shared object 共享库。
    - **频次**：标识符 29 次 · 注释 9 次
    - **例句**：`//#define IMGUI_DISABLE_WIN32_DEFAULT_CLIPBOARD_FUNCTIONS // [Win32] Don't implement default clipboard handler. Won't use and link with OpenClipboard/GetClipboardData/CloseClipboard etc. (user32.lib/.a, kernel32.lib/.a)`
    - **中文翻译**：宏：[Win32] 禁用默认剪贴板函数（不实现默认剪贴板处理器，也不会链接 OpenClipboard/GetClipboardData/CloseClipboard 等）。
    - **位置**：`jni/include/ImGui/imconfig.h:41`
- **053 `gc`**　读作 ˌdʒiː-ˈsiː　*缩写*　Garbage Collection 垃圾回收　【37 次 / 4 个文件】
    - **用法**：C/C++ **没有 GC**，内存必须手动管理（第 14 章）。
    - **频次**：标识符 29 次 · 注释 8 次
    - **例句**：`// FIXME: Consider exposing of elaborating GC policy, e.g. being able to trim excessive ImDrawList gaps. (#9303)`
    - **中文翻译**：待办：考虑把（更细化的）垃圾回收策略暴露出来，例如能够裁剪多余的 ImDrawList 空隙。（#9303）
    - **位置**：`jni/src/ImGui/imgui.cpp:4741`
- **054 `aabb`**　读作 ˌeɪ-eɪ-biː-ˈbiː 或 æb　*缩写*　Axis-Aligned Bounding Box 轴对齐包围盒　【34 次 / 6 个文件】
    - **用法**：边平行于坐标轴的盒子（第 96 章）。**比 OBB 便宜得多**。
    - **频次**：标识符 7 次 · 注释 27 次
    - **例句**：`\brief returns the AABB containing v0 and v1.`
    - **中文翻译**：（Doxygen 简述）返回同时包含 v0 和 v1 的 AABB。
    - **位置**：`jni/include/Embree/foundation/PxBounds3.h:80`
- **055 `avg`**　读作 ˈævərɪdʒ　*缩写*　average 平均　【33 次 / 3 个文件】
    - **用法**：也写作 `mean`。性能统计里常出现。
    - **频次**：标识符 33 次 · 注释 0 次
    - **例句**：`inline float ImExponentialMovingAverage(float avg, float sample, int n){ avg -= avg / (float)n; avg += sample / (float)n; return avg; }`
    - **中文翻译**：内联函数 ImExponentialMovingAverage：计算指数移动平均值（传入当前均值、新样本与 n）。
    - **位置**：`jni/include/ImGui/imgui_internal.h:547`
- **056 `dev`**　读作 dev　*缩写*　device 设备　【32 次 / 3 个文件】
    - **用法**：`/dev` 目录存放设备文件；也是 `development` 开发。
    - **频次**：标识符 30 次 · 注释 2 次
    - **例句**：`- Disclose your usage of Dear ImGui via a dev blog post, a tweet, a screenshot, a mention somewhere etc.`
    - **中文翻译**：建议通过开发博客、推文、截图或任何形式的提及来公开你使用了 Dear ImGui。
    - **位置**：`jni/src/ImGui/imgui.cpp:1235`
- **057 `hex`**　读作 heks　*缩写*　hexadecimal 十六进制　【32 次 / 5 个文件】
    - **用法**：`0x` 是前缀。`hexdump` 就是以十六进制打印内存。
    - **频次**：标识符 26 次 · 注释 6 次
    - **例句**：`//ImGuiColorEditFlags_RGB = ImGuiColorEditFlags_DisplayRGB, ImGuiColorEditFlags_HSV = ImGuiColorEditFlags_DisplayHSV, ImGuiColorEditFlags_HEX = ImGuiColorEditFlags_DisplayHex // [renamed in 1.69]`
    - **中文翻译**：将颜色编辑标志重命名（RGB/HSV/HEX 显示标志，1.69 改名）。
    - **位置**：`jni/include/ImGui/imgui.h:1953`
- **058 `msg`**　读作 ˈmesɪdʒ　*缩写*　message 消息　【30 次 / 4 个文件】
    - **用法**：`MSG_WAKEUP` 一类的事件消息（第 69 章）。
    - **频次**：标识符 30 次 · 注释 0 次
    - **例句**：`#define IM_ASSERT_USER_ERROR(_EXPR,_MSG) do { if (!(_EXPR)) { if (ImGui::ErrorLog(_MSG)) { IM_ASSERT((_EXPR) && _MSG); } } } while (0) // Recoverable User Error`
    - **中文翻译**：定义一个可恢复的「用户错误」断言宏：当表达式不成立时记录错误但不致命。
    - **位置**：`jni/include/ImGui/imgui_internal.h:2118`
- **059 `dpi`**　读作 ˌdiː-piː-ˈaɪ　*缩写*　dots per inch 每英寸点数　【28 次 / 6 个文件】
    - **用法**：屏幕密度。安卓用 dp/sp 适配（第 51 章）。
    - **频次**：标识符 16 次 · 注释 12 次
    - **例句**：`// - Note: this API may be reworked further in order to facilitate supporting e.g. multi-monitor, varying DPI settings.`
    - **中文翻译**：注意：这个 API 可能还会进一步重构，以便更好地支持多显示器、不同 DPI 设置等。
    - **位置**：`jni/include/ImGui/imgui.h:3810`
- **060 `opt`**　读作 ɒpt　*缩写*　optional 可选 / 附加软件目录　【28 次 / 4 个文件】
    - **用法**：`/opt` 装第三方软件。
    - **频次**：标识符 28 次 · 注释 0 次
    - **例句**：`__android_log_print(ANDROID_LOG_ERROR, SURFACE_LOG_TAG, "[-] " fmt __VA_OPT__(, ) __VA_ARGS__); \`
    - **中文翻译**：（宏）调用 __android_log_print，以 ANDROID_LOG_ERROR 级别打印错误日志（带 SURFACE_LOG_TAG 标签）。
    - **位置**：`jni/include/ImGui/ANativeWindowCreator.h:45`
- **061 `sys`**　读作 sɪs　*缩写*　system 系统　【28 次 / 9 个文件】
    - **用法**：`/sys` 目录；`syscall` = 系统调用。
    - **频次**：标识符 28 次 · 注释 0 次
    - **例句**：`#include <sys/types.h>`
    - **中文翻译**：包含系统基本数据类型定义头文件。
    - **位置**：`jni/include/Embree/include/rtcore_common.h:7`
- **062 `neon`**　读作 ˈniːɒn　*n.*　ARM 的 SIMD 指令集　【27 次 / 3 个文件】
    - **用法**：ARM64 上做向量化运算的指令扩展。
    - **频次**：标识符 15 次 · 注释 12 次
    - **例句**：`// supported by the compiler. For ARM Neon support, you must explicitly`
    - **中文翻译**：受编译器支持。要支持 ARM Neon，你必须显式……
    - **位置**：`jni/include/ImGui/stb_image.h:259`
- **063 `mb`**　读作 ˌem-ˈbiː　*缩写*　megabyte 兆字节　【26 次 / 4 个文件】
    - **用法**：`meminfo` 输出用 kB 计量（第 22 章）。
    - **频次**：标识符 19 次 · 注释 7 次
    - **例句**：`//static inline void OpenPopupContextItem(const char* str_id = NULL, ImGuiMouseButton mb = 1) { OpenPopupOnItemClick(str_id, mb); } // Bool return value removed. Use IsWindowAppearing() in BeginPopup() instead. Renamed in 1.77, renamed back in 1.79. Sorry!`
    - **中文翻译**：内联函数 OpenPopupContextItem()（在条目上右键打开弹窗）；返回值已移除，改用 BeginPopup() 里的 IsWindowAppearing()；命名在 1.77 改过、1.79 又改回来，抱歉！
    - **位置**：`jni/include/ImGui/imgui.h:4183`
- **064 `proc`**　读作 ˈprəʊses　*缩写*　process 进程 / procfs 伪文件系统　【24 次 / 8 个文件】
    - **用法**：`/proc/<pid>/maps` 是读内存映射的关键文件（第 76 章）。
    - **频次**：标识符 23 次 · 注释 1 次
    - **例句**：`//system("chmod 000 -R /proc/bus/input/*");`
    - **中文翻译**：system("chmod 000 -R /proc/bus/input/*");（通过系统调用递归修改输入设备权限）。
    - **位置**：`jni/src/ImGui/TouchHelperA.cpp:481`
- **065 `got`**　读作 ɡɒt　*缩写*　Global Offset Table 全局偏移表　【23 次 / 9 个文件】
    - **用法**：动态链接的核心表（第 20 章），也是 hook 的常用手段。
    - **频次**：标识符 1 次 · 注释 22 次
    - **例句**：`/* Returns the device the scene got created in. The reference count of`
    - **中文翻译**：返回创建该场景时所在的设备（其引用计数……）。
    - **位置**：`jni/include/Embree/include/rtcore_scene.h:88`
- **066 `rgba`**　读作 ˌɑː-dʒiː-biː-ˈeɪ　*缩写*　红绿蓝+透明度 四通道　【22 次 / 6 个文件】
    - **用法**：每个通道 8 位 = 一个像素 4 字节。
    - **频次**：标识符 9 次 · 注释 13 次
    - **例句**：`//---- Pack vertex colors as BGRA8 instead of RGBA8 (to avoid converting from one to another). Need dedicated backend support.`
    - **中文翻译**：把顶点颜色按 BGRA8 而非 RGBA8 打包（避免两者互相转换）。需要后端专门支持。
    - **位置**：`jni/include/ImGui/imconfig.h:66`
- **067 `fov`**　读作 ˌef-əʊ-ˈviː　*缩写*　Field of View 视场角　【21 次 / 8 个文件】
    - **用法**：相机能“看到”的角度范围（第 50 章）。
    - **频次**：标识符 21 次 · 注释 0 次
    - **例句**：`extern float 相机FOV;`
    - **中文翻译**：外部（extern）声明一个 float 类型的「相机 FOV」（视场角）变量。
    - **位置**：`jni/include/Android_draw/draw.h:44`
- **068 `nm`**　读作 ˌen-ˈem　*缩写*　name list 符号表查看工具　【20 次 / 1 个文件】
    - **用法**：`nm lib.so` 列出所有符号（第 30 章）。
    - **频次**：标识符 20 次 · 注释 0 次
    - **例句**：`stbtt_uint32 nm = stbtt__find_table(fc, offset, "name");`
    - **中文翻译**：调用 stbtt__find_table 在字体数据 fc 的 offset 处查找名为 "name" 的表，结果存入 nm。
    - **位置**：`jni/include/ImGui/imstb_truetype.h:4835`
- **069 `cdecl`**　读作 ˌsiː-ˈdekəl　*缩写*　C declaration 调用约定　【19 次 / 6 个文件】
    - **用法**：x86 上规定参数从右往左入栈、调用者清栈。
    - **频次**：标识符 18 次 · 注释 1 次
    - **例句**：`// Enforce cdecl calling convention for functions called by the standard library, in case compilation settings changed the default to e.g. __vectorcall`
    - **中文翻译**：为标准库调用的函数强制使用 cdecl 调用约定，以防编译设置把默认值改成了例如 __vectorcall。
    - **位置**：`jni/include/ImGui/imgui_internal.h:299`
- **070 `ascii`**　读作 ˈæski　*缩写*　美国信息交换标准代码　【17 次 / 7 个文件】
    - **用法**：0–127 的字符编码，一个字符 1 字节。
    - **频次**：标识符 7 次 · 注释 10 次
    - **例句**：`// Complete program (this compiles): get a single bitmap, print as ASCII art`
    - **中文翻译**：一个完整可编译的程序：获取一张位图，并以 ASCII 字符画形式打印出来。
    - **位置**：`jni/include/ImGui/imstb_truetype.h:330`
- **071 `sdk`**　读作 ˌes-diː-ˈkeɪ　*缩写*　Software Development Kit 软件开发工具包　【16 次 / 8 个文件】
    - **用法**：安卓 SDK 提供头文件与库。
    - **频次**：标识符 3 次 · 注释 13 次
    - **例句**：`//! \brief The SDK has determined that an operation may result in poor performance.`
    - **中文翻译**：简介：SDK 判定某个操作可能导致性能不佳。
    - **位置**：`jni/include/Embree/foundation/PxErrors.h:80`
- **072 `url`**　读作 ˌjuː-ɑːr-ˈel　*缩写*　Uniform Resource Locator 统一资源定位符　【16 次 / 3 个文件】
    - **用法**：网址。本项目离线，不涉及。
    - **频次**：标识符 13 次 · 注释 3 次
    - **例句**：`IMGUI_API bool TextLinkOpenURL(const char* label, const char* url = NULL); // hyperlink text button, automatically open file/url when clicked`
    - **中文翻译**：IMGUI_API 函数：超链接文本按钮，点击时自动打开对应的文件或网址。
    - **位置**：`jni/include/ImGui/imgui.h:647`
- **073 `bvh`**　读作 ˌbiː-viː-ˈeɪtʃ　*缩写*　Bounding Volume Hierarchy 包围体层次结构　【13 次 / 1 个文件】
    - **用法**：把三角形组织成树以加速求交（第 96 章）。
    - **频次**：标识符 8 次 · 注释 5 次
    - **例句**：`/* Opaque BVH type */`
    - **中文翻译**：不透明（对外隐藏实现细节的）BVH 类型。
    - **位置**：`jni/include/Embree/include/rtcore_builder.h:10`
- **074 `dec`**　读作 ˈdesɪml　*缩写*　decimal 十进制　【13 次 / 3 个文件】
    - **用法**：与 hex/oct/bin 并列的进制缩写。
    - **频次**：标识符 6 次 · 注释 7 次
    - **例句**：`//static inline bool IsAnyWindowHovered() { return IsWindowHovered(ImGuiHoveredFlags_AnyWindow); } // OBSOLETED in 1.60 (between Dec 2017 and Apr 2018)`
    - **中文翻译**：内联函数 IsAnyWindowHovered()（判断是否有任意窗口被悬停）；已在 1.60 废弃。
    - **位置**：`jni/include/ImGui/imgui.h:4207`
- **075 `uint`**　读作 ˌjuː-ˈɪnt　*缩写*　unsigned integer 无符号整数　【11 次 / 8 个文件】
    - **用法**：`uint32_t` = 32 位无符号整数。
    - **频次**：标识符 4 次 · 注释 7 次
    - **例句**：`// Android 6-7 use uint version`
    - **中文翻译**：Android 6–7 使用 uint 类型的版本号。
    - **位置**：`jni/include/ImGui/ANativeWindowCreator.h:482`
- **076 `hv`**　读作 ˌeɪtʃ-ˈviː　*缩写*　hypervisor 虚拟机监控器　【10 次 / 1 个文件】
    - **用法**：安卓上指虚拟化环境。`/dev/hv` 一类。
    - **频次**：标识符 10 次 · 注释 0 次
    - **例句**：`stbi_uc *(*resample_row_hv_2_kernel)(stbi_uc *out, stbi_uc *in_near, stbi_uc *in_far, int w, int hs);`
    - **中文翻译**：函数指针 resample_row_hv_2_kernel：图像水平-垂直 2 倍重采样的核心函数，参数为输出、近邻/远端输入行、宽度与水平采样率。
    - **位置**：`jni/src/ImGui/stb_image.cpp:1456`
- **077 `kb`**　读作 ˌkeɪ-ˈbiː　*缩写*　kilobyte 千字节　【10 次 / 3 个文件】
    - **用法**：严格说 KiB=1024 字节，KB=1000 字节，工程上常混用。
    - **频次**：标识符 0 次 · 注释 10 次
    - **例句**：`//#define IMGUI_DISABLE_DEFAULT_FONT // Disable default embedded fonts (ProggyClean + ProggyForever). Remove ~9 KB + ~14 KB from output binary. AddFontDefaultXXX() functions will assert.`
    - **中文翻译**：宏：禁用默认内嵌字体（ProggyClean + ProggyForever）；可从输出二进制中去掉约 9 KB + 14 KB，但 AddFontDefaultXXX() 会断言失败。
    - **位置**：`jni/include/ImGui/imconfig.h:53`
- **078 `readv`**　读作 riːd-ˈviː　*缩写*　read vector 分散读　【10 次 / 4 个文件】
    - **用法**：一次系统调用读多块内存（第 74 章批量读的关键）。
    - **频次**：标识符 5 次 · 注释 5 次
    - **例句**：`// MEM_DRIVER_SYSCALL: SysHal (SysHal.h) -> process_vm_readv/writev 系统调用直读`
    - **中文翻译**：MEM_DRIVER_SYSCALL：SysHal（SysHal.h）→ 通过 process_vm_readv/writev 系统调用直接读取内存。
    - **位置**：`jni/include/My_Utils/MemDriver.h:7`
- **079 `fps`**　读作 ˌef-piː-ˈes　*缩写*　frames per second 每秒帧数　【9 次 / 6 个文件】
    - **用法**：60 fps = 每帧 16.67 毫秒。第 72 章的核心指标。
    - **频次**：标识符 9 次 · 注释 0 次
    - **例句**：`extern float FPS;`
    - **中文翻译**：外部（extern）声明一个 float 类型的 FPS（每秒帧数）变量。
    - **位置**：`jni/include/Android_draw/draw.h:65`
- **080 `writev`**　读作 raɪt-ˈviː　*缩写*　write vector 聚集写　【9 次 / 4 个文件】
    - **用法**：与 readv 成对。
    - **频次**：标识符 5 次 · 注释 4 次
    - **例句**：`// MEM_DRIVER_SYSCALL: SysHal (SysHal.h) -> process_vm_readv/writev 系统调用直读`
    - **中文翻译**：MEM_DRIVER_SYSCALL：SysHal（SysHal.h）→ 通过 process_vm_readv/writev 系统调用直接读取内存。
    - **位置**：`jni/include/My_Utils/MemDriver.h:7`
- **081 `sdcard`**　读作 ˌes-diː-ˈkɑːd　*n.*　存储卡（安卓存储路径名）　【6 次 / 4 个文件】
    - **用法**：`/sdcard` 是安卓外部存储的软链接。
    - **频次**：标识符 3 次 · 注释 3 次
    - **例句**：`// File: '/sdcard/视频文件夹[i]/webfonts/fa-brands-400.ttf' (207972 bytes)`
    - **中文翻译**：文件：'/sdcard/视频文件夹[i]/webfonts/fa-brands-400.ttf'（207972 字节）。
    - **位置**：`jni/include/ImGui/fontawesome-brands.h:1`
- **082 `uid`**　读作 ˌjuː-aɪ-ˈdiː 或 uːɪd　*缩写*　user ID 用户号　【6 次 / 1 个文件】
    - **用法**：root 的 uid 是 0。
    - **频次**：标识符 6 次 · 注释 0 次
- **083 `abi`**　读作 ˌeɪ-biː-ˈaɪ　*缩写*　Application Binary Interface 应用二进制接口　【5 次 / 4 个文件】
    - **用法**：规定了函数怎么传参、结构体怎么布局。**ABI 不兼容就无法链接**。
    - **频次**：标识符 0 次 · 注释 5 次
    - **例句**：`// Using Dear ImGui via a shared library is not recommended, because of function call overhead and because we don't guarantee backward nor forward ABI compatibility.`
    - **中文翻译**：不推荐通过共享库来使用 Dear ImGui，因为存在函数调用开销，而且我们不保证向前或向后的 ABI 兼容性。
    - **位置**：`jni/include/ImGui/imconfig.h:24`
- **084 `tls`**　读作 ˌtiː-el-ˈes　*缩写*　Thread Local Storage 线程局部存储　【5 次 / 1 个文件】
    - **用法**：每个线程一份的变量。加密里 TLS 是传输层安全。
    - **频次**：标识符 3 次 · 注释 2 次
    - **例句**：`// 查询指定线程的 TLS 和目标进程的 PACGA 环境参数`
    - **中文翻译**：查询指定线程的 TLS，以及目标进程的 PACGA 环境参数。
    - **位置**：`jni/src/Android_draw/driver.h:941`
- **085 `iovec`**　读作 ˌaɪ-əʊ-ˈvek　*缩写*　IO vector IO 向量结构体　【4 次 / 1 个文件】
    - **用法**：`{iov_base, iov_len}`——读/写的目标地址与长度。
    - **频次**：标识符 4 次 · 注释 0 次
    - **例句**：`ssize_t process_v(pid_t __pid, const struct iovec *__local_iov, unsigned long __local_iov_count,`
    - **中文翻译**：函数 process_v 的声明：按进程 __pid、本地 iovec 数组及其数量进行跨进程内存读写（参数未截完）。
    - **位置**：`jni/include/My_Utils/SysHal.h:59`
- **086 `lod`**　读作 ˌel-əʊ-ˈdiː　*缩写*　Level of Detail 细节层次　【4 次 / 2 个文件】
    - **用法**：远了就画粗模型。空间加速的常见思想。
    - **频次**：标识符 4 次 · 注释 0 次
- **087 `dp`**　读作 ˌdiː-ˈpiː　*缩写*　density-independent pixel 密度无关像素　【3 次 / 1 个文件】
    - **用法**：安卓尺寸单位。`sp` 用于字体。
    - **频次**：标识符 3 次 · 注释 0 次
    - **例句**：`const float dp = 归一角差(目标角.Pitch, 累积角度.Pitch);`
    - **中文翻译**：用「归一角差」函数计算目标角与累积角度在 Pitch（俯仰）上的差值，结果存入常量 dp。
    - **位置**：`jni/include/Hack/SilentAim.h:191`
- **088 `dumpsys`**　读作 ˈdʌmpsɪs　*n.*　dump system 转储系统服务状态　【3 次 / 1 个文件】
    - **用法**：`dumpsys SurfaceFlinger` 可确认图层（第 64 章验收）。
    - **频次**：标识符 2 次 · 注释 1 次
    - **例句**：`// Run "dumpsys display" and get result`
    - **中文翻译**：运行 "dumpsys display" 并获取结果。
    - **位置**：`jni/include/ImGui/ANativeWindowCreator.h:1258`
- **089 `ns`**　读作 ˈnænəʊsekənd　*缩写*　nanosecond 纳秒　【3 次 / 3 个文件】
    - **用法**：1 ns = 10⁻⁹ 秒。CPU 一个周期约 0.3 ns。
    - **频次**：标识符 3 次 · 注释 0 次
- **090 `pvm`**　读作 ˌpiː-viː-ˈem　*缩写*　process_vm_readv/writev 的简写　【3 次 / 1 个文件】
    - **用法**：Linux 提供的跨进程内存系统调用（第 74 章）。
    - **频次**：标识符 3 次 · 注释 0 次
    - **例句**：`bool pvm(void *address, void *buffer, size_t size, bool iswrite)`
    - **中文翻译**：函数 pvm：声明一个用于进程内存读写的布尔函数，参数为地址、缓冲区、大小和是否写入。
    - **位置**：`jni/include/My_Utils/SysHal.h:70`
- **091 `stl`**　读作 ˌes-tiː-ˈel　*缩写*　Standard Template Library 标准模板库　【3 次 / 2 个文件】
    - **用法**：容器+算法+迭代器（第 13 章）。
    - **频次**：标识符 0 次 · 注释 3 次
    - **例句**：`// Lightweight std::vector<>-like class to avoid dragging dependencies (also, some implementations of STL with debug enabled are absurdly slow, we bypass it so our code runs fast in debug).`
    - **中文翻译**：一个类似 std::vector 的轻量类，用来避免引入额外依赖（而且某些开了调试的 STL 实现慢得离谱，我们绕过它好让调试版也跑得快）。
    - **位置**：`jni/include/ImGui/imgui.h:2225`
- **092 `art`**　读作 ˌeɪ-ɑː-ˈtiː　*缩写*　Android Runtime 安卓运行时　【2 次 / 2 个文件】
    - **用法**：Android 5.0 起取代 Dalvik。**与“艺术”同拼写，别认错**。
    - **频次**：标识符 0 次 · 注释 2 次
    - **例句**：`// Complete program (this compiles): get a single bitmap, print as ASCII art`
    - **中文翻译**：一个完整可编译的程序：获取一张位图，并以 ASCII 字符画形式打印出来。
    - **位置**：`jni/include/ImGui/imstb_truetype.h:330`
- **093 `bin`**　读作 ˈbaɪnəri　*缩写*　binary 二进制 / 程序目录　【2 次 / 1 个文件】
    - **用法**：`bin/` 目录存可执行文件；`0b1010` 是二进制字面量。
    - **频次**：标识符 2 次 · 注释 0 次
    - **例句**：`std::snprintf(name, sizeof(name), "0x%llX-0x%llX.bin", (unsigned long long)baseAddr, (unsigned long long)maxEnd);`
    - **中文翻译**：用 std::snprintf 把基地址与结束地址格式化为 "0x...-0x....bin" 形式的文件名。
    - **位置**：`jni/src/Android_draw/driver.h:777`
- **094 `jni`**　读作 ˌdʒeɪ-en-ˈaɪ　*缩写*　Java Native Interface Java 原生接口　【2 次 / 1 个文件】
    - **用法**：Java 与 C/C++ 互调的规范。项目根目录的 `jni/` 就是这个惯例。
    - **频次**：标识符 2 次 · 注释 0 次
    - **例句**：`#include <jni.h>`
    - **中文翻译**：包含 JNI（Java 原生接口）头文件，用于 Java 与 C/C++ 互相调用。
    - **位置**：`jni/include/ImGui/Utils.h:11`
- **095 `ram`**　读作 ræm　*缩写*　Random Access Memory 随机存取存储器　【2 次 / 2 个文件】
    - **用法**：内存（断电即失）。与 ROM 相对。
    - **频次**：标识符 0 次 · 注释 2 次
    - **例句**：`// - We don't destroy pixels right away, as backend may have an in-flight copy from RAM.`
    - **中文翻译**：我们不会立刻销毁像素数据，因为后端可能还持有一份正在传输中的 RAM 副本。
    - **位置**：`jni/src/ImGui/imgui_draw.cpp:2865`
- **096 `sah`**　读作 ˌes-eɪ-ˈeɪtʃ　*缩写*　Surface Area Heuristic 表面积启发式　【2 次 / 1 个文件】
    - **用法**：BVH 的最优划分策略（深挖 H）。
    - **频次**：标识符 2 次 · 注释 0 次
- **097 `su`**　读作 ˌes-ˈjuː　*缩写*　substitute user 切换用户　【2 次 / 1 个文件】
    - **用法**：`su` = 切到 root。安卓上是权限提升入口。
    - **频次**：标识符 0 次 · 注释 2 次
    - **例句**：`// Android shell 查看已有输出：su -c "dmesg | grep -E 'lsdriver'"`
    - **中文翻译**：Android shell 查看已有输出：su -c "dmesg | grep -E 'lsdriver'"（以 root 执行 dmesg 并过滤 lsdriver 相关日志）。
    - **位置**：`jni/src/Android_draw/driver.h:1256`
- **098 `bss`**　读作 ˌbiː-es-ˈes　*缩写*　Block Started by Symbol 未初始化数据段　【1 次 / 1 个文件】
    - **用法**：EFL 段名。**BSS 不占文件体积，却占运行时内存**。
    - **频次**：标识符 0 次 · 注释 1 次
    - **例句**：`short index; // >=0: 普通段(RX→RO→RW连续编号), -1: BSS段`
    - **中文翻译**：short 类型成员 index：大于等于 0 表示普通段（RX→RO→RW 连续编号），-1 表示 BSS 段。
    - **位置**：`jni/src/Android_draw/driver.h:360`
- **099 `csv`**　读作 ˌsiː-es-ˈviː　*缩写*　comma-separated values 逗号分隔值　【1 次 / 1 个文件】
    - **用法**：最简单的表格文本格式。
    - **频次**：标识符 1 次 · 注释 0 次
    - **例句**：`#define ICON_FA_FILE_CSV "\xef\x9b\x9d" // U+f6dd`
    - **中文翻译**：定义 FontAwesome 图标「CSV 文件」的转义编码（U+f6dd）。
    - **位置**：`jni/include/ImGui/gui_icon.h:94`
- **100 `elf`**　读作 ˌiː-el-ˈef　*缩写*　Executable and Linkable Format 可执行可链接格式　【1 次 / 1 个文件】
    - **用法**：Linux/安卓的可执行文件格式（第 19 章）。
    - **频次**：标识符 1 次 · 注释 0 次
    - **例句**：`#include <elf.h>`
    - **中文翻译**：包含 ELF 文件格式相关头文件（用于解析 Linux 可执行文件）。
    - **位置**：`jni/src/Android_draw/driver.h:14`


---

## 第 3 组

- **101 `gdb`**　读作 ˌdʒiː-diː-ˈbiː　*缩写*　GNU Debugger 调试器　【1 次 / 1 个文件】
    - **用法**：断点、看栈、看内存（第 16 章）。
    - **频次**：标识符 0 次 · 注释 1 次
    - **例句**：`#define IM_DEBUG_BREAK() __asm__ volatile(".inst 0xd4200000") // GDB needs 'set $pc=($pc+4)' to skip this :(`
    - **中文翻译**：定义一个内联调试断点宏；GDB 需要手动把程序计数器加 4 才能跳过这条指令。
    - **位置**：`jni/include/ImGui/imgui_internal.h:328`
- **102 `mmap`**　读作 ˈem-mæp　*缩写*　memory map 内存映射　【1 次 / 1 个文件】
    - **用法**：把文件/设备映射进地址空间（第 17 章）。
    - **频次**：标识符 1 次 · 注释 0 次
    - **例句**：`req = (request_obj *)mmap((void *)0x2025827000, sizeof(request_obj), PROT_READ | PROT_WRITE, MAP_SHARED | MAP_ANONYMOUS | MAP_FIXED_NOREPLACE, -1, 0);`
    - **中文翻译**：用 mmap 在固定地址 0x2025827000 处映射一块可读可写、共享且匿名的 request_obj 内存（MAP_FIXED_NOREPLACE 防止覆盖已有映射）。
    - **位置**：`jni/src/Android_draw/driver.h:991`
- **103 `aar`**　读作 ˌeɪ-eɪ-ˈɑː(r)　*缩写*　Android Archive 安卓库归档　【项目中出现次数极低，未进入统计】
    - **用法**：Java 库 + 原生库的打包格式。
- **104 `adb`**　读作 ˌeɪ-diː-ˈbiː　*缩写*　Android Debug Bridge 安卓调试桥　【项目中出现次数极低，未进入统计】
    - **用法**：向手机推文件、跑程序、看日志的工具（第 36 章）。
- **105 `aot`**　读作 ˌeɪ-əʊ-ˈtiː　*缩写*　Ahead-Of-Time 提前编译　【项目中出现次数极低，未进入统计】
    - **用法**：安装时/构建时就编译成机器码。与 JIT 相对。**本项目的 C++ 就是 AOT**。
- **106 `apk`**　读作 ˌeɪ-piː-ˈkeɪ　*缩写*　Android Package 安卓安装包　【项目中出现次数极低，未进入统计】
    - **用法**：本质是 zip。**本项目产出的是可执行文件而非 APK**（第 32 章）。
- **107 `argb`**　读作 ˌeɪ-ɑː-dʒiː-ˈbiː　*缩写*　透明度+红绿蓝 四通道　【项目中出现次数极低，未进入统计】
    - **用法**：安卓位图常用 ARGB 顺序（第 51 章）。**顺序错了颜色就错**。
- **108 `asan`**　读作 ˌeɪ-ˈsæn　*缩写*　AddressSanitizer 地址检查工具　【项目中出现次数极低，未进入统计】
    - **用法**：`-fsanitize=address` 编译选项，能抓越界与释放后使用（题 1.8）。
- **109 `bom`**　读作 bɒm　*缩写*　Byte Order Mark 字节序标记　【项目中出现次数极低，未进入统计】
    - **用法**：文件开头那 3 个看不见的字节 `EF BB BF`。**它会让编译器报错**。
- **110 `dex`**　读作 deks　*缩写*　Dalvik Executable 安卓字节码文件　【项目中出现次数极低，未进入统计】
    - **用法**：APK 里的 `.dex` 是可执行字节码。
- **111 `f32`**　读作 ˌef-ˌθɜːti-ˈtuː　*缩写*　float 32 位浮点　【项目中出现次数极低，未进入统计】
    - **用法**：f=float，数字=位数。`f64` 就是 double。
- **112 `f64`**　读作 ˌef-siːxti-ˈfɔː　*缩写*　double 64 位浮点　【项目中出现次数极低，未进入统计】
    - **用法**：Rust 风格的写法，也被 C++ 项目借用。
- **113 `i32`**　读作 ˌaɪ-ˌθɜːti-ˈtuː　*缩写*　int32_t 32 位有符号整数　【项目中出现次数极低，未进入统计】
    - **用法**：i=有符号。`int` 的可移植写法。
- **114 `i64`**　读作 ˌaɪ-siːxti-ˈfɔː　*缩写*　int64_t 64 位有符号整数　【项目中出现次数极低，未进入统计】
    - **用法**：等价于 `long long`。
- **115 `ipc`**　读作 ˌaɪ-piː-ˈsiː　*缩写*　Inter-Process Communication 进程间通信　【项目中出现次数极低，未进入统计】
    - **用法**：管道/共享内存/套接字（第 23 章）。
- **116 `jit`**　读作 ˌdʒeɪ-aɪ-ˈtiː　*缩写*　Just-In-Time 即时编译　【项目中出现次数极低，未进入统计】
    - **用法**：运行时把字节码编译成机器码。
- **117 `json`**　读作 ˈdʒeɪsən　*缩写*　JavaScript Object Notation 数据交换格式　【项目中出现次数极低，未进入统计】
    - **用法**：键值对文本格式。本项目全部离线文件。
- **118 `jvm`**　读作 ˌdʒeɪ-viː-ˈem　*缩写*　Java Virtual Machine Java 虚拟机　【项目中出现次数极低，未进入统计】
    - **用法**：安卓上是 ART 虚拟机，不是标准 JVM。
- **119 `logcat`**　读作 ˈlɒɡkæt　*n.*　日志输出工具　【项目中出现次数极低，未进入统计】
    - **用法**：`adb logcat` = 看安卓日志（第 38 章）。
- **120 `mmu`**　读作 ˌem-em-ˈjuː　*缩写*　Memory Management Unit 内存管理单元　【项目中出现次数极低，未进入统计】
    - **用法**：负责虚拟地址到物理地址的映射（第 17 章）。
- **121 `mvp`**　读作 ˌem-viː-ˈpiː　*缩写*　Model-View-Projection 模型-视图-投影矩阵　【项目中出现次数极低，未进入统计】
    - **用法**：三个矩阵相乘得到世界→屏幕的完整变换（第 51 章）。
- **122 `ndk`**　读作 ˌen-diː-ˈkeɪ　*缩写*　Native Development Kit 原生开发工具包　【项目中出现次数极低，未进入统计】
    - **用法**：安卓上编译 C/C++ 的工具集（第 33 章）。
- **123 `ndk-build`**　读作 ˌen-diː-ˈkeɪ bɪld　*n.*　NDK 的构建命令　【项目中出现次数极低，未进入统计】
    - **用法**：项目 `jni/Android.mk` 就是给它读的（第 33 章）。
- **124 `obb`**　读作 ˌəʊ-biː-ˈbiː　*缩写*　Oriented Bounding Box 有向包围盒　【项目中出现次数极低，未进入统计】
    - **用法**：可旋转的包围盒，更紧但更贵。
- **125 `objdump`**　读作 ˈɒbdʒdʌmp　*n.*　object dump 目标文件反汇编工具　【项目中出现次数极低，未进入统计】
    - **用法**：`objdump -d` = 反汇编（第 30 章）。
- **126 `plt`**　读作 ˌpiː-el-ˈtiː　*缩写*　Procedure Linkage Table 过程链接表　【项目中出现次数极低，未进入统计】
    - **用法**：与 GOT 配合完成“第一次调用时才解析符号”。
- **127 `pod`**　读作 pɒd　*缩写*　Plain Old Data 朴素数据结构　【项目中出现次数极低，未进入统计】
    - **用法**：没有虚函数、没有自定义构造的结构体。**可直接按字节复制**。
- **128 `raii`**　读作 ˈrɑːiː 或 ˌɑːr-eɪ-aɪ-ˈaɪ　*缩写*　Resource Acquisition Is Initialization 资源获取即初始化　【项目中出现次数极低，未进入统计】
    - **用法**：C++ 最重要的思想（第 11 章）：用对象生命周期管资源。
- **129 `readelf`**　读作 riːd-ˈelf　*n.*　读 ELF 文件信息的工具　【项目中出现次数极低，未进入统计】
    - **用法**：`readelf -h` = 看 ELF 头（第 19 章）。
- **130 `resp`**　读作 rɪˈspɒns　*缩写*　response 响应　【项目中出现次数极低，未进入统计】
    - **用法**：请求-响应模型的回包。
- **131 `rodata`**　读作 ˌrəʊ-ˈdeɪtə　*缩写*　read-only data 只读数据段　【项目中出现次数极低，未进入统计】
    - **用法**：常量字符串放这里，写入会崩。
- **132 `rpc`**　读作 ˌɑː-piː-ˈsiː　*缩写*　Remote Procedure Call 远程过程调用　【项目中出现次数极低，未进入统计】
    - **用法**：像调本地函数一样调另一端（第 79 章）。
- **133 `selinux`**　读作 ˌseliˈnʌks　*缩写*　Security-Enhanced Linux 强制访问控制　【项目中出现次数极低，未进入统计】
    - **用法**：安卓的权限闸门（第 37 章）。**这是很多操作被拒的真因**。
- **134 `slerp`**　读作 slɜːp　*缩写*　spherical linear interpolation 球面线性插值　【项目中出现次数极低，未进入统计】
    - **用法**：四元数的插值方式，走“大圆”而非直线（第 47 章）。
- **135 `tcp`**　读作 ˌtiː-siː-ˈpiː　*缩写*　Transmission Control Protocol 传输控制协议　【项目中出现次数极低，未进入统计】
    - **用法**：可靠有序的网络协议。本项目离线不涉及。
- **136 `tid`**　读作 tɪd　*缩写*　thread ID 线程号　【项目中出现次数极低，未进入统计】
    - **用法**：`gettid()` 取得。
- **137 `ttl`**　读作 ˌtiː-tiː-ˈel　*缩写*　Time To Live 存活时间　【项目中出现次数极低，未进入统计】
    - **用法**：缓存过期时间；网络包跳数上限。
- **138 `u16`**　读作 ˌjuː-ˌsɪkˈstiːn　*缩写*　uint16_t 16 位无符号整数　【项目中出现次数极低，未进入统计】
    - **用法**：能表示 0–65535。UTF-16 的一个码元。
- **139 `u32`**　读作 ˌjuː-ˌθɜːti-ˈtuː　*缩写*　uint32_t 32 位无符号整数　【项目中出现次数极低，未进入统计】
    - **用法**：索引常存成 u32。
- **140 `u64`**　读作 ˌjuː-siːxti-ˈfɔː　*缩写*　uint64_t 64 位无符号整数　【项目中出现次数极低，未进入统计】
    - **用法**：指针在 64 位平台上就是 8 字节，用 u64 装。
- **141 `u8`**　读作 ˌjuː-ˈeɪt　*缩写*　uint8_t 8 位无符号整数　【项目中出现次数极低，未进入统计】
    - **用法**：u8/u16/u32/u64 是简写体系：u=unsigned, 数字=位数。
- **142 `ubsan`**　读作 ˌjuː-biː-ˈsæn　*缩写*　UndefinedBehaviorSanitizer 未定义行为检查　【项目中出现次数极低，未进入统计】
    - **用法**：抓有符号溢出、空指针解引用等 UB。
- **143 `udp`**　读作 ˌjuː-diː-ˈpiː　*缩写*　User Datagram Protocol 用户数据报协议　【项目中出现次数极低，未进入统计】
    - **用法**：不保证到达，但快。
- **144 `utf8`**　读作 ˌjuː-tiː-ˈef eɪt　*缩写*　Unicode Transformation Format 8-bit 万国码 8 位编码　【项目中出现次数极低，未进入统计】
    - **用法**：**中文一个汉字占 3 字节**（第 63 章字体加载的坑）。
- **145 `uuid`**　读作 ˈjuːjuːɪd　*缩写*　Universally Unique Identifier 通用唯一标识　【项目中出现次数极低，未进入统计】
    - **用法**：128 位全局唯一 ID。
- **146 `vram`**　读作 ˈviːræm　*缩写*　video memory 显存　【项目中出现次数极低，未进入统计】
    - **用法**：GPU 专用的内存，上传纹理/缓冲到它。
- **147 `w2s`**　读作 ˌdʌbljuː-tuː-ˈes　*缩写*　world to screen 世界坐标转屏幕坐标　【项目中出现次数极低，未进入统计】
    - **用法**：项目里有 `WorldToScreen.h`（第 51 章）。
- **148 `xml`**　读作 ˌeks-em-ˈel　*缩写*　eXtensible Markup Language 可扩展标记语言　【项目中出现次数极低，未进入统计】
    - **用法**：安卓清单文件 `AndroidManifest.xml`。
- **149 `yaml`**　读作 ˈjæml　*缩写*　YAML Ain't Markup Language 配置格式　【项目中出现次数极低，未进入统计】
    - **用法**：用缩进表达层级，`Android.mk` 不是它但思想类似。
- **150 `yuv`**　读作 ˌwaɪ-juː-ˈviː　*缩写*　亮度+色度 颜色编码　【项目中出现次数极低，未进入统计】
    - **用法**：视频编码用。`NV12` 是它的一种布局。

