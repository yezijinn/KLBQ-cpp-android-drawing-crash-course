---
tags: [教程, 词汇, 词频]
aliases: [W4, vocab L4]
---


# W4 · 第 4 级：状态与修饰词（145 词）

> [!abstract] 本册在整套词汇表里的位置
> 判断分支与注释里描述“当前是什么状态”的词
>
> **本册排序 = 项目源码里的真实出现次数**（从多到少）。
> 频次统计口径：`jni/` 下 97 个源文件、35 万行代码。
> 每个词条都附**这个项目里的真实例句**（含文件名与行号）。
> 音标采用**英式 IPA（DJ）**；与附录 K 的美式 IPA 差异见 [[词频词汇总览|W0]]。


## 本册词条

- [第 1 组（1–50）](#第-1-组)
- [第 2 组（51–100）](#第-2-组)
- [第 3 组（101–145）](#第-3-组)


---

## 第 1 组

- **001 `current`**　/ˈkʌrənt/　*adj.*　当前的　【1564 次 / 20 个文件】
    - **用法**：`current value` = 当前值。与 last/next 组成时序三元组。
    - **频次**：标识符 1208 次 · 注释 356 次
    - **例句**：`// if current ptr not contains or current ptr is height field, load it data!`
    - **中文翻译**：如果当前指针不含目标，或者当前指针是一个高度场，就加载它的数据！
    - **位置**：`jni/include/Embree/PhysX.h:1263`
- **002 `active`**　/ˈæktɪv/　*adj.*　活动的、有效的　【1179 次 / 12 个文件】
    - **用法**：`active tab` = 当前标签页。与 inactive 相对。
    - **频次**：标识符 1024 次 · 注释 155 次
    - **例句**：`// - Where 'ImGuiHoveredFlags_ForTooltip' itself is a shortcut to use 'style.HoverFlagsForTooltipMouse' or 'style.HoverFlagsForTooltipNav' depending on active input type. For mouse it defaults to 'ImGuiHoveredFlags_Stationary | ImGuiHoveredFlags_DelayShort'.`
    - **中文翻译**：'ImGuiHoveredFlags_ForTooltip' 本身是一个快捷方式，会根据当前输入类型选用 'style.HoverFlagsForTooltipMouse' 或 'style.HoverFlagsForTooltipNav'。对鼠标而言，默认是 'ImGuiHoveredFlags_Stationary | ImGuiHoveredFlags_DelayShort'。
    - **位置**：`jni/include/ImGui/imgui.h:833`
- **003 `hovered`**　/ˈhɒvəd/　*adj.*　鼠标悬停的　【820 次 / 7 个文件】
    - **用法**：`IsItemHovered()` = 鼠标是否在这个控件上。做 tooltip 必需。
    - **频次**：标识符 738 次 · 注释 82 次
    - **例句**：`// - A selectable highlights when hovered, and can display another color when selected.`
    - **中文翻译**：可选的（Selectable）控件在悬停时会高亮，并且在被选中时可以显示另一种颜色。
    - **位置**：`jni/include/ImGui/imgui.h:764`
- **004 `used`**　/juːzd/　*adj.*　被使用的　【623 次 / 47 个文件】
    - **用法**：`used` = 已被用掉（如已用内存）；反义是 free/available。
    - **频次**：标识符 100 次 · 注释 523 次
    - **例句**：`// contributors may be used to endorse or promote products derived`
    - **中文翻译**：贡献者的名字不得用于背书或推广其衍生产品……[BSD 许可证片段]
    - **位置**：`jni/include/Embree/foundation/Px.h:11`
- **005 `visible`**　/ˈvɪzəbl/　*adj.*　可见的　【609 次 / 14 个文件】
    - **用法**：`isVisible` = 是否可见。本项目可见性判定的核心（第 95 章）。
    - **频次**：标识符 419 次 · 注释 190 次
    - **例句**：`IMGUI_API void SetScrollHereX(float center_x_ratio = 0.5f); // adjust scrolling amount to make current cursor position visible. center_x_ratio=0.0: left, 0.5: center, 1.0: right. When using to make a "default/current item" visible, consider using SetItemDefaultFocus() instead.`
    - **中文翻译**：IMGUI_API 函数：调整横向滚动量，使当前光标位置可见；center_x_ratio=0.0 靠左、0.5 居中、1.0 靠右。若想让「默认/当前条目」可见，建议改用 SetItemDefaultFocus()。
    - **位置**：`jni/include/ImGui/imgui.h:502`
- **006 `inner`**　/ˈɪnə(r)/　*adj.*　内部的　【556 次 / 7 个文件】
    - **用法**：`inner loop` = 内层循环（执行次数最多，是优化重点）。与 outer 成对。
    - **频次**：标识符 510 次 · 注释 46 次
    - **例句**：`// - ImageButton() draws a background based on regular Button() color + optionally an inner background if specified.`
    - **中文翻译**：ImageButton() 会基于普通 Button() 的颜色绘制背景，也可选地在指定时再画一层内背景。
    - **位置**：`jni/include/ImGui/imgui.h:653`
- **007 `baked`**　/beɪkt/　*adj.*　已烘焙的　【531 次 / 7 个文件】
    - **用法**：字体图集把字形提前“烤”成纹理，运行时只查表——这就是 baking（第 63 章）。
    - **频次**：标识符 503 次 · 注释 28 次
    - **例句**：`struct ImFontBaked; // Baked data for a ImFont at a given size.`
    - **中文翻译**：结构体前向声明 ImFontBaked：某个字号下「烘焙」完成的字体数据。
    - **位置**：`jni/include/ImGui/imgui.h:182`
- **008 `default`**　/dɪˈfɔːlt/　*adj./n.*　默认的；默认值　【477 次 / 27 个文件】
    - **用法**：**477 次全部出现在注释里**。`default value` = 不传参数时用的值；C++ 的 `default` 还用于 `switch` 分支和 `= default`。
    - **频次**：标识符 0 次 · 注释 477 次
    - **例句**：`//! Default constructor`
    - **中文翻译**：默认构造函数（文档注释）。
    - **位置**：`jni/include/Embree/foundation/PxMat33.h:93`
- **009 `outer`**　/ˈaʊtə(r)/　*adj.*　外部的　【449 次 / 6 个文件】
    - **用法**：`outer loop` = 外层循环。与 inner 成对。
    - **频次**：标识符 377 次 · 注释 72 次
    - **例句**：`ImGuiChildFlags_Borders = 1 << 0, // Show an outer border and enable WindowPadding. (IMPORTANT: this is always == 1 == true for legacy reason)`
    - **中文翻译**：ImGuiChildFlags_Borders 枚举值（1<<0）：显示子窗口外边框并启用 WindowPadding。注意：出于历史兼容原因，它恒等于 1（即 true）。
    - **位置**：`jni/include/ImGui/imgui.h:1223`
- **010 `left`**　/left/　*adj.*　左、剩余　【445 次 / 20 个文件】
    - **用法**：`left edge` = 左边缘；`left over` = 剩下的。
    - **频次**：标识符 307 次 · 注释 138 次
    - **例句**：`// Left face`
    - **中文翻译**：左面。
    - **位置**：`jni/include/Embree/PhysX.h:876`
- **011 `right`**　/raɪt/　*adj.*　右、正确　【440 次 / 20 个文件】
    - **用法**：**两义**：右边、正确。`if right` 是“如果正确”。
    - **频次**：标识符 282 次 · 注释 158 次
    - **例句**：`// Right face`
    - **中文翻译**：右面。
    - **位置**：`jni/include/Embree/PhysX.h:873`
- **012 `internal`**　/ɪnˈtɜːnl/　*adj.*　内部的　【434 次 / 18 个文件】
    - **用法**：`internal state` = 内部状态。API 注释里表示“你别碰”。
    - **频次**：标识符 37 次 · 注释 397 次
    - **例句**：`// - In v1.92.0 (2025/06/11): added ImTextureRef which carry either a ImTextureID either a pointer to internal texture atlas. All user facing functions taking ImTextureID changed to ImTextureRef`
    - **中文翻译**：在 v1.92.0（2025/06/11）：新增了 ImTextureRef，它要么携带一个 ImTextureID，要么携带指向内部纹理图集的指针。所有面向用户、接收 ImTextureID 的函数都改成了接收 ImTextureRef。
    - **位置**：`jni/include/ImGui/imgui.h:338`
- **013 `temp`**　/temp/　*adj./n.*　临时的　【416 次 / 10 个文件】
    - **用法**：`temporary` 的缩写。`tmp` 也是缩写。临时变量/临时文件。
    - **频次**：标识符 404 次 · 注释 12 次
    - **例句**：`// a temp buffer and allocate from the temp buffer (without freeing),`
    - **中文翻译**：一个临时缓冲区，并从中分配（不释放），
    - **位置**：`jni/include/ImGui/imstb_truetype.h:251`
- **014 `selected`**　/sɪˈlektɪd/　*adj.*　已选中的　【405 次 / 10 个文件】
    - **用法**：`IsItemSelected` 判断选中状态。
    - **频次**：标识符 323 次 · 注释 82 次
    - **例句**：`// - Most widgets return true when the value has been changed or when pressed/selected`
    - **中文翻译**：多数控件在数值被改变、或被按下/选中时返回 true。
    - **位置**：`jni/include/ImGui/imgui.h:633`
- **015 `callable`**　/ˈkɔːləbl/　*adj.*　可调用的　【403 次 / 15 个文件】
    - **用法**：`std::function` 能装任何可调用对象（函数指针、lambda、仿函数）（第 13 章）。
    - **频次**：标识符 401 次 · 注释 2 次
    - **例句**：`// Linux/android and CUDA compilation does not work with std::isfnite, as it is not marked as CUDA callable`
    - **中文翻译**：Linux/Android 和 CUDA 编译下不能用 std::isfinite，因为它没有被标记为可在 CUDA 中调用。
    - **位置**：`jni/include/Embree/foundation/unix/PxUnixIntrinsics.h:41`
- **016 `back`**　/bæk/　*adj./adv.*　后面；回　【372 次 / 22 个文件】
    - **用法**：`back buffer` = 后台缓冲；`push_back` = 加到尾部。
    - **频次**：标识符 277 次 · 注释 95 次
    - **例句**：`// Back face`
    - **中文翻译**：背面。
    - **位置**：`jni/include/Embree/PhysX.h:864`
- **017 `pressed`**　/prest/　*adj.*　被按下的　【362 次 / 8 个文件】
    - **用法**：`IsItemActive` 表示控件正在被按住。
    - **频次**：标识符 323 次 · 注释 39 次
    - **例句**：`// - Most widgets return true when the value has been changed or when pressed/selected`
    - **中文翻译**：多数控件在数值被改变、或被按下/选中时返回 true。
    - **位置**：`jni/include/ImGui/imgui.h:633`
- **018 `true`**　/truː/　*adj.*　真、成立　【321 次 / 26 个文件】
    - **用法**：C++ 关键字 `true`。布尔值只有 true/false。
    - **频次**：标识符 0 次 · 注释 321 次
    - **例句**：`//! \brief returns true if the passed number is a finite floating point number as opposed to INF, NAN, etc.`
    - **中文翻译**：简介：如果传入的数是有限浮点数（而非 INF、NAN 等），则返回 true。
    - **位置**：`jni/include/Embree/foundation/PxMath.h:291`
- **019 `enabled`**　/ɪˈneɪbld/　*adj.*　已启用的　【318 次 / 11 个文件】
    - **用法**：`if enabled` = 如果启用。与 disabled 成对。
    - **频次**：标识符 246 次 · 注释 72 次
    - **例句**：`// Android 15+: use gui::TrustedOverlay enum (ENABLED=2, DISABLED=1)`
    - **中文翻译**：Android 15 及以上：使用 gui::TrustedOverlay 枚举（ENABLED=2，DISABLED=1）。
    - **位置**：`jni/include/ImGui/ANativeWindowCreator.h:618`
- **020 `backup`**　/ˈbækʌp/　*n./adj.*　备份　【316 次 / 6 个文件】
    - **用法**：备份文件；后备方案。
    - **频次**：标识符 272 次 · 注释 44 次
    - **例句**：`// Stacked color modifier, backup of modified data so we can restore it`
    - **中文翻译**：堆叠的颜色修改器，会备份被修改的数据以便之后还原。
    - **位置**：`jni/include/ImGui/imgui_internal.h:943`
- **021 `valid`**　/ˈvælɪd/　*adj.*　有效的、合法的　【267 次 / 20 个文件】
    - **用法**：`isValid` = 是否合法。第 89 章指针过滤的核心判断。
    - **频次**：标识符 181 次 · 注释 86 次
    - **例句**：`// - If your backend is using ImTextureID to store an index/offset and you need 0 to be valid, You can add '#define ImTextureID_Invalid ((ImTextureID)-1)' in your imconfig.h file.`
    - **中文翻译**：如果你的后端用 ImTextureID 存储索引/偏移，并且需要让 0 成为有效值，可以在 imconfig.h 里加 '#define ImTextureID_Invalid ((ImTextureID)-1)'。
    - **位置**：`jni/include/ImGui/imgui.h:344`
- **022 `center`**　/ˈsentə(r)/　*n./adj.*　中心　【258 次 / 9 个文件】
    - **用法**：英式也拼 `centre`。`center of the triangle` = 三角形重心。
    - **频次**：标识符 212 次 · 注释 46 次
    - **例句**：`would be center and dimensions.`
    - **中文翻译**：（注释片段）即（该盒子的）中心点与尺寸。
    - **位置**：`jni/include/Embree/foundation/PxBounds3.h:52`
- **023 `disabled`**　/dɪsˈeɪbld/　*adj.*　已禁用的　【251 次 / 12 个文件】
    - **用法**：`#ifdef` 里用来关掉某功能。
    - **频次**：标识符 176 次 · 注释 75 次
    - **例句**：`// use disabled geometry`
    - **中文翻译**：使用被禁用的几何体。
    - **位置**：`jni/include/Embree/PhysX.h:1531`
- **024 `zero`**　/ˈzɪərəʊ/　*num./n.*　零　【224 次 / 22 个文件】
    - **用法**：`zero-initialized` = 初始化为 0。C++ 里 0 与 nullptr 不同。
    - **频次**：标识符 136 次 · 注释 88 次
    - **例句**：`/** enum for zero constructor tag for vectors and matrices */`
    - **中文翻译**：用于向量和矩阵的「零构造函数标签」的枚举。
    - **位置**：`jni/include/Embree/foundation/Px.h:75`
- **025 `single`**　/ˈsɪŋɡl/　*adj.*　单个的　【211 次 / 17 个文件】
    - **用法**：`single byte` = 单字节。与 multiple 相对。
    - **频次**：标识符 94 次 · 注释 117 次
    - **例句**：`/* Ray structure for a single ray */`
    - **中文翻译**：单条光线的结构体。
    - **位置**：`jni/include/Embree/include/rtcore_ray.h:10`
- **026 `top`**　/tɒp/　*adj./n.*　顶部　【200 次 / 15 个文件】
    - **用法**：`top of stack` = 栈顶。屏幕坐标里 y=0 在**顶部**（第 51 章）。
    - **频次**：标识符 130 次 · 注释 70 次
    - **例句**：`// Top face`
    - **中文翻译**：顶面。
    - **位置**：`jni/include/Embree/PhysX.h:867`
- **027 `previous`**　/ˈpriːviəs/　*adj.*　上一个　【189 次 / 11 个文件】
    - **用法**：`previous frame` = 上一帧。与 current 相对。
    - **频次**：标识符 85 次 · 注释 104 次
    - **例句**：`/* Returns previous half edge. */`
    - **中文翻译**：返回上一条半边。
    - **位置**：`jni/include/Embree/include/rtcore_geometry.h:276`
- **028 `invalid`**　/ɪnˈvælɪd/　*adj.*　无效的　【183 次 / 17 个文件】
    - **用法**：`invalid pointer` = 野指针。读内存时首要排除项。
    - **频次**：标识符 132 次 · 注释 51 次
    - **例句**：`//! \brief method called with invalid parameter(s)`
    - **中文翻译**：简介：以无效参数被调用的方法。
    - **位置**：`jni/include/Embree/foundation/PxErrors.h:63`
- **029 `full`**　/fʊl/　*adj.*　满的、完整的　【181 次 / 15 个文件】
    - **用法**：`buffer full` = 缓冲满了；`full screen` = 全屏。
    - **频次**：标识符 126 次 · 注释 55 次
    - **例句**：`// - IMPORTANT: Notice that BeginPopupContextXXX takes ImGuiPopupFlags just like OpenPopup() and unlike BeginPopup(). For full consistency, we may add ImGuiWindowFlags to the BeginPopupContextXXX functions in the future.`
    - **中文翻译**：重要：注意 BeginPopupContextXXX 接收的是 ImGuiPopupFlags（和 OpenPopup() 一样，和 BeginPopup() 不同）。为完全一致，将来我们可能会给 BeginPopupContextXXX 也加上 ImGuiWindowFlags。
    - **位置**：`jni/include/ImGui/imgui.h:868`
- **030 `legacy`**　/ˈleɡəsi/　*adj.*　遗留的、老旧的　【176 次 / 10 个文件】
    - **用法**：`legacy code` = 历史遗留代码。看到它要格外小心。
    - **频次**：标识符 29 次 · 注释 147 次
    - **例句**：`// legacy define for PhysX`
    - **中文翻译**：PhysX 的旧式（legacy）宏定义。
    - **位置**：`jni/include/Embree/foundation/PxPreprocessor.h:249`
- **031 `fast`**　/fɑːst/　*adj.*　快的　【172 次 / 11 个文件】
    - **用法**：`fast path` = 快路径（常见情况走的分支）。与 slow path 成对。
    - **频次**：标识符 140 次 · 注释 32 次
    - **例句**：`// Lightweight std::vector<>-like class to avoid dragging dependencies (also, some implementations of STL with debug enabled are absurdly slow, we bypass it so our code runs fast in debug).`
    - **中文翻译**：一个类似 std::vector 的轻量类，用来避免引入额外依赖（而且某些开了调试的 STL 实现慢得离谱，我们绕过它好让调试版也跑得快）。
    - **位置**：`jni/include/ImGui/imgui.h:2225`
- **032 `custom`**　/ˈkʌstəm/　*adj.*　自定义的　【171 次 / 11 个文件】
    - **用法**：与 builtin（内置）相对。`custom allocator` = 自定义分配器。
    - **频次**：标识符 42 次 · 注释 129 次
    - **例句**：`// - When a texture is created by user code (e.g. custom images), we directly store the low-level ImTextureID.`
    - **中文翻译**：当纹理由用户代码（例如自定义图片）创建时，我们直接存储底层的 ImTextureID。
    - **位置**：`jni/include/ImGui/imgui.h:354`
- **033 `multiple`**　/ˈmʌltɪpl/　*adj.*　多个的　【170 次 / 18 个文件】
    - **用法**：`multiple threads` = 多线程。
    - **频次**：标识符 2 次 · 注释 168 次
    - **例句**：`//! constructor from a scalar, which generates a multiple of the identity matrix`
    - **中文翻译**：由标量构造（会生成一个单位矩阵的倍数）。
    - **位置**：`jni/include/Embree/foundation/PxMat33.h:117`
- **034 `false`**　/fɔːls/　*adj.*　假、不成立　【166 次 / 13 个文件】
    - **用法**：`returns false on failure` = 失败时返回假。
    - **频次**：标识符 0 次 · 注释 166 次
    - **例句**：`// return false;`
    - **中文翻译**：返回 false；
    - **位置**：`jni/include/Embree/PhysX.h:291`
- **035 `const`**　/kɒnst/　*adj.*　常量、不可修改　【161 次 / 13 个文件】
    - **用法**：C++ 关键字（第 12 章）。`const` 越靠左越管得宽。
    - **频次**：标识符 0 次 · 注释 161 次
    - **例句**：`// Android 17+ (const LayerMetadata& by reference)`
    - **中文翻译**：Android 17 及以上（按引用传入 const LayerMetadata&）。
    - **位置**：`jni/include/ImGui/ANativeWindowCreator.h:332`
- **036 `empty`**　/ˈempti/　*adj.*　空的　【161 次 / 22 个文件】
    - **用法**：`container.empty()` = 是否为空。**注意：empty 是“没元素”，不是“没内存”**。
    - **频次**：标识符 87 次 · 注释 74 次
    - **例句**：`/** enum for empty constructor tag*/`
    - **中文翻译**：用于「空构造函数标签」的枚举。
    - **位置**：`jni/include/Embree/foundation/Px.h:69`
- **037 `local`**　/ˈləʊkl/　*adj.*　局部的、本机的　【148 次 / 17 个文件】
    - **用法**：`local variable` = 局部变量；`local space` = 局部坐标系（第 48 章）。
    - **频次**：标识符 109 次 · 注释 39 次
    - **例句**：`/* Opaque thread local allocator type */`
    - **中文翻译**：不透明的线程局部内存分配器类型。
    - **位置**：`jni/include/Embree/include/rtcore_builder.h:22`
- **038 `bottom`**　/ˈbɒtəm/　*n./adj.*　底部　【146 次 / 15 个文件】
    - **用法**：`bottom of the buffer` = 缓冲底部。矩阵第 4 行也叫 bottom row。
    - **频次**：标识符 103 次 · 注释 43 次
    - **例句**：`// Bottom face`
    - **中文翻译**：底面。
    - **位置**：`jni/include/Embree/PhysX.h:870`
- **039 `host`**　/həʊst/　*n.*　主机　【143 次 / 11 个文件】
    - **用法**：`host memory` = 主机（CPU）内存，与 device（GPU）内存相对（第 58 章）。
    - **频次**：标识符 116 次 · 注释 27 次
    - **例句**：`/* Creates a new buffer using explicit host device memory. */`
    - **中文翻译**：使用显式指定的主机/设备内存来创建一个新的缓冲区。
    - **位置**：`jni/include/Embree/include/rtcore_buffer.h:41`
- **040 `fixed`**　/fɪkst/　*adj.*　固定的　【142 次 / 11 个文件】
    - **用法**：`fixed size` = 固定大小；`fixed-point` = 定点数。
    - **频次**：标识符 61 次 · 注释 81 次
    - **例句**：`// BeginXXX function returned true. Begin and BeginChild are the only odd ones out. Will be fixed in a future update.]`
    - **中文翻译**：BeginXXX 系列函数在返回 true 时情况特殊。Begin 和 BeginChild 是仅有的两个例外，会在将来的更新里修好。]
    - **位置**：`jni/include/ImGui/imgui.h:435`
- **041 `global`**　/ˈɡləʊbl/　*adj.*　全局的　【142 次 / 17 个文件】
    - **用法**：`global variable` = 全局变量。命名常加 `g_` 前缀（见第 6 级）。
    - **频次**：标识符 102 次 · 注释 40 次
    - **例句**：`// implements some convenient global operators.`
    - **中文翻译**：实现了一些方便的全局运算符。
    - **位置**：`jni/include/Embree/foundation/PxFlags.h:60`
- **042 `client`**　/ˈklaɪənt/　*n.*　客户端　【140 次 / 7 个文件】
    - **用法**：请求方。与 server（服务方）成对。
    - **频次**：标识符 132 次 · 注释 8 次
    - **例句**：`// make the selection/cursor state valid if client altered the string`
    - **中文翻译**：如果客户端改动过字符串，就让选择/光标状态重新有效。
    - **位置**：`jni/include/ImGui/imstb_textedit.h:615`
- **043 `dynamic`**　/daɪˈnæmɪk/　*adj.*　动态的　【137 次 / 15 个文件】
    - **用法**：`dynamic_cast`；`dynamic library` = 动态库（第 20 章）。与 static 相对。
    - **频次**：标识符 100 次 · 注释 37 次
    - **例句**：`// - If you want to use InputText() with std::string or any custom dynamic string type, use the wrapper in misc/cpp/imgui_stdlib.h/.cpp!`
    - **中文翻译**：如果你想让 InputText() 配合 std::string 或任何自定义动态字符串类型使用，请用 misc/cpp/imgui_stdlib.h/.cpp 里的封装！
    - **位置**：`jni/include/ImGui/imgui.h:715`
- **044 `following`**　/ˈfɒləʊɪŋ/　*adj.*　以下的、接下来的　【120 次 / 39 个文件】
    - **用法**：文档里 `the following steps` = 以下步骤。
    - **频次**：标识符 0 次 · 注释 120 次
    - **例句**：`// modification, are permitted provided that the following conditions`
    - **中文翻译**：在满足以下条件的前提下，允许进行修改……[BSD 许可证片段]
    - **位置**：`jni/include/Embree/foundation/Px.h:3`
- **045 `native`**　/ˈneɪtɪv/　*adj.*　原生的　【120 次 / 14 个文件】
    - **用法**：`ANativeWindow` = 安卓原生窗口；`native code` = 机器码。
    - **频次**：标识符 94 次 · 注释 26 次
    - **例句**：`// - When a Rendered Backend creates a texture, it store its native identifier into a ImTextureID value.`
    - **中文翻译**：当一个渲染后端创建纹理时，它会把底层的原生标识符存进一个 ImTextureID 值。
    - **位置**：`jni/include/ImGui/imgui.h:325`
- **046 `supported`**　/səˈpɔːtɪd/　*adj.*　受支持的　【120 次 / 18 个文件】
    - **用法**：`if not supported` = 如果不支持。特性检测分支。
    - **频次**：标识符 54 次 · 注释 66 次
    - **例句**：`//CUDA is currently supported only on windows`
    - **中文翻译**：CUDA 目前只在 Windows 上受支持。
    - **位置**：`jni/include/Embree/foundation/PxPreprocessor.h:530`
- **047 `inline`**　/ˈɪnlaɪn/　*adj.*　内联的　【114 次 / 10 个文件】
    - **用法**：`inline` 建议编译器把函数体展开到调用处（第 08 章）。头文件里的函数必须 inline。
    - **频次**：标识符 0 次 · 注释 114 次
    - **例句**：`// inline ImTextureRef ImTextureRefFromID(ImTextureID tex_id) { ImTextureRef tex_ref = { ._TexData = NULL, .TexID = tex_id }; return tex_ref; }`
    - **中文翻译**：内联函数：用给定的 ImTextureID 构造一个 ImTextureRef（纹理引用）。
    - **位置**：`jni/include/ImGui/imgui.h:363`
- **048 `raw`**　/rɔː/　*adj.*　原始的、未加工的　【110 次 / 9 个文件】
    - **用法**：`raw bytes` = 原始字节；`raw pointer` = 裸指针（不带智能指针）。
    - **频次**：标识符 96 次 · 注释 14 次
    - **例句**：`// Inputs Utilities: Raw Keyboard/Mouse/Gamepad Access`
    - **中文翻译**：输入工具：原始的键盘/鼠标/手柄访问。
    - **位置**：`jni/include/ImGui/imgui.h:1061`
- **049 `bad`**　/bæd/　*adj.*　坏的、错误的　【104 次 / 4 个文件】
    - **用法**：`bad_alloc` = 分配失败异常；`bad pointer` = 坏指针。
    - **频次**：标识符 99 次 · 注释 5 次
    - **例句**：`// discard the oldest entry in the redo list--it's bad if this`
    - **中文翻译**：丢弃重做列表里最旧的一项——如果……就不好了
    - **位置**：`jni/include/ImGui/imstb_textedit.h:1225`
- **050 `small`**　/smɔːl/　*adj.*　小的　【103 次 / 13 个文件】
    - **用法**：`small enough` = 足够小。BVH 叶子节点就是“够小就停”（第 96 章）。
    - **频次**：标识符 37 次 · 注释 66 次
    - **例句**：`IMGUI_API void Bullet(); // draw a small circle + keep the cursor on the same line. advance cursor x position by GetTreeNodeToLabelSpacing(), same distance that TreeNode() uses`
    - **中文翻译**：IMGUI_API 函数：画一个小圆点（bullet），并保持光标在同一行；光标 x 前进的距离与 TreeNode() 使用的标签间距相同。
    - **位置**：`jni/include/ImGui/imgui.h:645`


---

## 第 2 组

- **051 `initial`**　/ɪˈnɪʃl/　*adj.*　初始的　【98 次 / 14 个文件】
    - **用法**：`initial value` = 初值。与 final 相对。
    - **频次**：标识符 33 次 · 注释 65 次
    - **例句**：`// - Dragging operations are only reported after mouse has moved a certain distance away from the initial clicking position (see 'lock_threshold' and 'io.MouseDraggingThreshold')`
    - **中文翻译**：拖拽操作只有在鼠标离开初始点击位置一定距离后才会被上报（见 'lock_threshold' 和 'io.MouseDraggingThreshold'）。
    - **位置**：`jni/include/ImGui/imgui.h:1108`
- **052 `static`**　/ˈstætɪk/　*adj.*　静态的　【97 次 / 13 个文件】
    - **用法**：C++ 关键字（第 09 章）。`static library` = 静态库（第 28 章）。
    - **频次**：标识符 0 次 · 注释 97 次
    - **例句**：`// static assert`
    - **中文翻译**：静态断言（static_assert）。
    - **位置**：`jni/include/Embree/foundation/PxPreprocessor.h:435`
- **053 `dirty`**　/ˈdɜːti/　*adj.*　脏的（数据被改过）　【96 次 / 6 个文件】
    - **用法**：`dirty flag` = 数据改了要重算的标志（第 99 章）。**这是个关键概念**。
    - **频次**：标识符 86 次 · 注释 10 次
    - **例句**：`// Mark mirror cache dirty so ProcessMirrorDisplay recreates mirrors for the new surface`
    - **中文翻译**：把镜像缓存标记为「脏」，这样 ProcessMirrorDisplay 会为新 surface 重新创建镜像。
    - **位置**：`jni/include/ImGui/ANativeWindowCreator.h:1231`
- **054 `different`**　/ˈdɪfrənt/　*adj.*　不同的　【92 次 / 14 个文件】
    - **用法**：`differs from` = 与……不同。
    - **频次**：标识符 0 次 · 注释 92 次
    - **例句**：`// Handle scaling for different display sizes`
    - **中文翻译**：处理不同显示尺寸下的缩放。
    - **位置**：`jni/include/ImGui/ANativeWindowCreator.h:1342`
- **055 `optional`**　/ˈɒpʃənl/　*adj.*　可选的　【91 次 / 14 个文件】
    - **用法**：`optional parameter` = 可选参数；`std::optional<T>` 表示可能有值。
    - **频次**：标识符 19 次 · 注释 72 次
    - **例句**：`// - 'selection_size' and 'items_count' parameters are optional and used by a few features. If they are costly for you to compute, you may avoid them.`
    - **中文翻译**：'selection_size' 和 'items_count' 参数是可选的，只有少数功能会用到；如果计算成本高，你可以不使用它们。
    - **位置**：`jni/include/ImGui/imgui.h:775`
- **056 `hidden`**　/ˈhɪdn/　*adj.*　隐藏的　【85 次 / 6 个文件】
    - **用法**：`hidden surface removal` = 隐藏面消除（第 52 章）。
    - **频次**：标识符 52 次 · 注释 33 次
    - **例句**：`//#define IMGUI_API __attribute__((visibility("default"))) // GCC/Clang: override visibility when set is hidden`
    - **中文翻译**：宏定义 IMGUI_API：用 __attribute__((visibility("default")))（GCC/Clang：当默认可见性被设为 hidden 时，用它来覆盖）。
    - **位置**：`jni/include/ImGui/imconfig.h:29`
- **057 `ok`**　/ˌəʊ ˈkeɪ/　*adj.*　好的、成功的　【85 次 / 13 个文件】
    - **用法**：`return OK` = 成功。状态码常常 0 = OK。
    - **频次**：标识符 26 次 · 注释 59 次
    - **例句**：`// Design note: Its OK to use int for general loop variables and temps.`
    - **中文翻译**：设计说明：一般的循环变量和临时变量用 int 是可以的。
    - **位置**：`jni/include/Embree/foundation/PxSimpleTypes.h:38`
- **058 `packed`**　/pækt/　*adj.*　紧凑排列的（无填充）　【80 次 / 9 个文件】
    - **用法**：`__attribute__((packed))` = 取消结构体对齐填充（第 07 章）。**用错会导致未对齐访问崩溃**。
    - **频次**：标识符 49 次 · 注释 31 次
    - **例句**：`//#define IMGUI_USE_BGRA_PACKED_COLOR`
    - **中文翻译**：宏：使用 BGRA 打包颜色（而非 RGBA）。
    - **位置**：`jni/include/ImGui/imconfig.h:67`
- **059 `public`**　/ˈpʌblɪk/　*adj.*　公开的　【79 次 / 14 个文件】
    - **用法**：C++ 访问权限；`public API` = 对外接口。
    - **频次**：标识符 0 次 · 注释 79 次
    - **例句**：`// - You generally do NOT need to care or use this ever. But we need to make it available in imgui.h because some of our public structures are relying on it.`
    - **中文翻译**：通常你根本不需要关心或使用它，但我们得把它保留在 imgui.h 里，因为一些公开结构体依赖它。
    - **位置**：`jni/include/ImGui/imgui.h:2227`
- **060 `cached`**　/kæʃt/　*adj.*　已缓存的　【77 次 / 9 个文件】
    - **用法**：`cached result` = 缓存下来的结果。要配套失效机制。
    - **频次**：标识符 60 次 · 注释 17 次
    - **例句**：`// Apply transform to all cached surfaces if needed`
    - **中文翻译**：需要时，对所有缓存的 surface 应用变换。
    - **位置**：`jni/include/ImGui/ANativeWindowCreator.h:1385`
- **061 `runtime`**　/ˈrʌntaɪm/　*n.*　运行期　【72 次 / 12 个文件】
    - **用法**：`at runtime` = 运行时；`runtime error` = 运行时错误。与 compile-time 相对。
    - **频次**：标识符 37 次 · 注释 35 次
    - **例句**：`/* Helper functions to access ray packets of runtime size N */`
    - **中文翻译**：用于访问运行时长度为 N 的光线数据包的辅助函数。
    - **位置**：`jni/include/Embree/include/rtcore_ray.h:192`
- **062 `explicit`**　/ɪkˈsplɪsɪt/　*adj.*　显式的　【67 次 / 10 个文件】
    - **用法**：`explicit` 禁止隐式转换（第 11 章）。与 implicit 相对。
    - **频次**：标识符 0 次 · 注释 67 次
    - **例句**：`/* Creates a new buffer using explicit host device memory. */`
    - **中文翻译**：使用显式指定的主机/设备内存来创建一个新的缓冲区。
    - **位置**：`jni/include/Embree/include/rtcore_buffer.h:41`
- **063 `safe`**　/seɪf/　*adj.*　安全的　【64 次 / 13 个文件】
    - **用法**：`thread-safe` = 线程安全；`safe cast` = 安全转换。
    - **频次**：标识符 35 次 · 注释 29 次
    - **例句**：`uint8_t* mExtraTrigData{};// PT: WARNING: bounds must be followed by at least 32bits of data for safe SIMD loading`
    - **中文翻译**：uint8_t 指针 mExtraTrigData：附加的触发（trigger）数据；警告：其后至少要有 32 位数据，SIMD 加载才安全。
    - **位置**：`jni/include/Embree/PhysX.h:515`
- **064 `builtin`**　/ˌbɪlt ˈɪn/　*adj.*　内置的　【60 次 / 5 个文件】
    - **用法**：也写作 `built-in`。语言/编译器自带的。
    - **频次**：标识符 60 次 · 注释 0 次
    - **例句**：`#define PX_OFFSET_OF(X, Y) __builtin_offsetof(X, Y)`
    - **中文翻译**：定义一个宏，用来求结构体成员 Y 在类型 X 中的字节偏移量。
    - **位置**：`jni/include/Embree/foundation/PxPreprocessor.h:443`
- **065 `part`**　/pɑːt/　*n.*　部分　【59 次 / 13 个文件】
    - **用法**：`part of` = ……的一部分。
    - **频次**：标识符 1 次 · 注释 58 次
    - **例句**：`// - plutosvg is currently easier to install, as e.g. it is part of vcpkg. It will support more fonts and may load them faster. See misc/freetype/README for instructions.`
    - **中文翻译**：plutosvg 目前更好装，比如它已经是 vcpkg 的一部分。它会支持更多字体，并且加载可能更快。详见 misc/freetype/README。
    - **位置**：`jni/include/ImGui/imconfig.h:96`
- **066 `implicit`**　/ɪmˈplɪsɪt/　*adj.*　隐式的　【57 次 / 9 个文件】
    - **用法**：`implicit conversion` = 隐式转换（编译器自动做的，容易埋坑）。
    - **频次**：标识符 11 次 · 注释 46 次
    - **例句**：`//---- Define constructor and implicit cast operators to convert back<>forth between your math types and ImVec2/ImVec4.`
    - **中文翻译**：自定义：定义构造函数和隐式转换运算符，在你的数学类型与 ImVec2/ImVec4 之间互相转换。
    - **位置**：`jni/include/ImGui/imconfig.h:106`
- **067 `equal`**　/ˈiːkwəl/　*adj.*　相等的　【55 次 / 20 个文件】
    - **用法**：`==` 是相等比较。**浮点数不要用 ==**（第 26 章）。
    - **频次**：标识符 29 次 · 注释 26 次
    - **例句**：`//! Transform vector by matrix, equal to v' = M*v`
    - **中文翻译**：用矩阵变换向量，等价于 v' = M*v。
    - **位置**：`jni/include/Embree/foundation/PxMat33.h:325`
- **068 `required`**　/rɪˈkwaɪəd/　*adj.*　必需的　【55 次 / 14 个文件】
    - **用法**：`required parameter` = 必填参数。
    - **频次**：标识符 5 次 · 注释 50 次
    - **例句**：`// Headers are required to perform: reordering, sorting, and opening the context menu.`
    - **中文翻译**：表头（Headers）负责执行：重排、排序，以及打开上下文菜单。
    - **位置**：`jni/include/ImGui/imgui.h:916`
- **069 `front`**　/frʌnt/　*n./adj.*　前面　【53 次 / 10 个文件】
    - **用法**：`front()` = 首元素（与 `begin()` 区别：begin 是迭代器）。
    - **频次**：标识符 38 次 · 注释 15 次
    - **例句**：`// Front face`
    - **中文翻译**：正面。
    - **位置**：`jni/include/Embree/PhysX.h:861`
- **070 `simple`**　/ˈsɪmpl/　*adj.*　简单的　【53 次 / 14 个文件】
    - **用法**：`simple approach` = 简单做法。注释里常用来对比优化方案。
    - **频次**：标识符 15 次 · 注释 38 次
    - **例句**：`// A simple value type representing a two-dimensional size.`
    - **中文翻译**：一个表示二维尺寸的简单值类型。
    - **位置**：`jni/include/ImGui/ANativeWindowCreator.h:111`
- **071 `relative`**　/ˈrelətɪv/　*adj.*　相对的　【52 次 / 10 个文件】
    - **用法**：`relative offset` = 相对偏移。与 absolute 相对。
    - **频次**：标识符 14 次 · 注释 38 次
    - **例句**：`// - IMPORTANT: Popup identifiers are relative to the current ID stack, so OpenPopup and BeginPopup generally needs to be at the same level of the stack.`
    - **中文翻译**：重要：弹出层（popup）的标识符是相对于当前 ID 栈的，所以 OpenPopup 和 BeginPopup 通常要在栈的同一层级调用。
    - **位置**：`jni/include/ImGui/imgui.h:844`
- **072 `unsigned`**　/ˌʌnˈsaɪnd/　*adj.*　无符号的　【51 次 / 14 个文件】
    - **用法**：`unsigned int` = 无符号整数（第 24 章）。**与 signed 混用会出现诡异比较**。
    - **频次**：标识符 0 次 · 注释 51 次
    - **例句**：`/* 8-bit unsigned integer */`
    - **中文翻译**：8 位无符号整数。
    - **位置**：`jni/include/Embree/include/rtcore_common.h:61`
- **073 `silent`**　/ˈsaɪlənt/　*adj.*　静默的（不报错的）　【50 次 / 6 个文件】
    - **用法**：`silent failure` = 静默失败。**最危险的 bug 类型**。
    - **频次**：标识符 48 次 · 注释 2 次
    - **例句**：`// - By design, we do NOT allow error recovery to be 100% silent. One of the three options needs to be checked!`
    - **中文翻译**：按设计，我们不允许错误恢复完全静默；三种处理方式中必须有一种被勾选！
    - **位置**：`jni/include/ImGui/imgui.h:2504`
- **074 `blank`**　/blæŋk/　*adj.*　空白的　【48 次 / 8 个文件】
    - **用法**：`blank line` = 空行。`blank` 是“什么都没有”，与 empty 近义。
    - **频次**：标识符 44 次 · 注释 4 次
    - **例句**：`// A work of art lies ahead! (. = white layer, X = black layer, others are blank)`
    - **中文翻译**：前方是一幅「艺术品」！（. = 白色图层，X = 黑色图层，其余为空白）
    - **位置**：`jni/src/ImGui/imgui_draw.cpp:2617`
- **075 `aligned`**　/əˈlaɪnd/　*adj.*　已对齐的　【47 次 / 15 个文件】
    - **用法**：`aligned to 16 bytes` = 16 字节对齐。未对齐访问在 ARM 上可能直接崩。
    - **频次**：标识符 19 次 · 注释 28 次
    - **例句**：`void* alignment; // PT: Makes sure the class is at least aligned to pointer size. See DE6803.`
    - **中文翻译**：void* 成员 alignment：确保该类至少按指针大小对齐（参见 DE6803）。
    - **位置**：`jni/include/Embree/PhysX.h:583`
- **076 `temporary`**　/ˈtemprəri/　*adj.*　临时的　【47 次 / 8 个文件】
    - **用法**：`temporary object` = 临时对象。它的生命周期只到当前语句结束。
    - **频次**：标识符 0 次 · 注释 47 次
    - **例句**：`// You can use it as custom user storage for temporary values. Declare your own storage if, for example:`
    - **中文翻译**：你可以把它当作临时值的自定义用户存储来用。例如，如果需要，可以声明你自己的存储：
    - **位置**：`jni/include/ImGui/imgui.h:2829`
- **077 `standard`**　/ˈstændəd/　*adj.*　标准的　【46 次 / 13 个文件】
    - **用法**：`std` 就是 standard 的缩写。`standard layout` = 标准布局（第 07 章）。
    - **频次**：标识符 0 次 · 注释 46 次
    - **例句**：`// maximum extents defined such that floating point exceptions are avoided for standard use cases`
    - **中文翻译**：定义最大范围，以避免在标准用例下触发浮点异常。
    - **位置**：`jni/include/Embree/foundation/PxBounds3.h:45`
- **078 `correct`**　/kəˈrekt/　*adj.*　正确的　【44 次 / 11 个文件】
    - **用法**：`correct result` = 正确结果。与 expected 呼应。
    - **频次**：标识符 2 次 · 注释 42 次
    - **例句**：`// Select the correct CreateSurface API based on Android version`
    - **中文翻译**：根据 Android 版本选择正确的 CreateSurface API。
    - **位置**：`jni/include/ImGui/ANativeWindowCreator.h:294`
- **079 `deprecated`**　/ˈdeprəkeɪtɪd/　*adj.*　已弃用的　【42 次 / 13 个文件】
    - **用法**：还能用但别再用。编译器会警告。
    - **频次**：标识符 21 次 · 注释 21 次
    - **例句**：`Deprecated macro`
    - **中文翻译**：已废弃的宏。
    - **位置**：`jni/include/Embree/foundation/PxPreprocessor.h:420`
- **080 `upper`**　/ˈʌpə(r)/　*adj.*　上面的　【42 次 / 8 个文件】
    - **用法**：`upper bound` = 上界（开区间）。与 lower 成对。
    - **频次**：标识符 35 次 · 注释 7 次
    - **例句**：`// This is to let programs in the wild set an upper bound to prevent`
    - **中文翻译**：这是为了让外部程序能设一个上限，以防止……
    - **位置**：`jni/include/ImGui/stb_image.h:361`
- **081 `long`**　/lɒŋ/　*adj.*　长的　【40 次 / 11 个文件】
    - **用法**：`long` 也是类型名（至少 32 位）。`long long` 至少 64 位。
    - **频次**：标识符 0 次 · 注释 40 次
    - **例句**：`// - Wrapping style is not ideal. Wrapping of long words/sections (e.g. words larger than total available width) may be particularly unpleasing.`
    - **中文翻译**：换行样式并不理想。对超长单词/段落（比如比可用总宽度还长的单词）的换行尤其难看。
    - **位置**：`jni/include/ImGui/imgui.h:1308`
- **082 `final`**　/ˈfaɪnl/　*adj.*　最终的　【39 次 / 8 个文件】
    - **用法**：`final result`；C++ 的 `final` 禁止继承/重写。
    - **频次**：标识符 0 次 · 注释 39 次
    - **例句**：`// - Legacy: Pre-1.78 there are DragXXX() function signatures that take a final 'float power=1.0f' argument instead of the 'ImGuiSliderFlags flags=0' argument.`
    - **中文翻译**：旧式写法：在 1.78 之前，DragXXX() 函数末尾有一个 'float power=1.0f' 参数，而不是现在的 'ImGuiSliderFlags flags=0'。
    - **位置**：`jni/include/ImGui/imgui.h:678`
- **083 `readonly`**　/ˌriːd ˈəʊnli/　*adj.*　只读的　【38 次 / 4 个文件】
    - **用法**：`read-only`。只读内存写入会段错误（第 19 章 `.rodata` 段）。
    - **频次**：标识符 30 次 · 注释 8 次
    - **例句**：`ImGuiSliderFlags_ReadOnly = 1 << 21, // Consider using g.NextItemData.ItemFlags |= ImGuiItemFlags_ReadOnly instead.`
    - **中文翻译**：ImGuiSliderFlags_ReadOnly 标志（1<<21）：只读；建议改用 g.NextItemData.ItemFlags |= ImGuiItemFlags_ReadOnly。
    - **位置**：`jni/include/ImGui/imgui_internal.h:1096`
- **084 `absolute`**　/ˈæbsəluːt/　*adj.*　绝对的　【37 次 / 9 个文件】
    - **用法**：`absolute address` = 绝对地址。与相对地址相对（第 20 章 PIC）。
    - **频次**：标识符 0 次 · 注释 37 次
    - **例句**：`//! \brief platform-specific absolute value`
    - **中文翻译**：简介：平台相关的绝对值函数。
    - **位置**：`jni/include/Embree/foundation/unix/PxUnixIntrinsics.h:55`
- **085 `good`**　/ɡʊd/　*adj.*　好的　【37 次 / 10 个文件】
    - **用法**：`good enough` = 够好了。性能优化时的取舍标准。
    - **频次**：标识符 12 次 · 注释 25 次
    - **例句**：`// - Wiki ....................... https://github.com/ocornut/imgui/wiki (lots of good stuff there)`
    - **中文翻译**：维基：……（里面有很多好东西）。
    - **位置**：`jni/include/ImGui/imgui.h:14`
- **086 `lower`**　/ˈləʊə(r)/　*adj.*　下面的；较低的　【36 次 / 7 个文件】
    - **用法**：`lower bound` = 下界（闭区间）；`lowercase` = 小写。
    - **频次**：标识符 20 次 · 注释 16 次
    - **例句**：`uint32_t ApiVersion; // Fill with API version of Instance, e.g. VK_API_VERSION_1_3 or your value of VkApplicationInfo::apiVersion. May be lower than header version (VK_HEADER_VERSION_COMPLETE)`
    - **中文翻译**：uint32_t 成员 ApiVersion：填写实例的 API 版本（如 VK_API_VERSION_1_3 或 VkApplicationInfo::apiVersion），可低于头文件版本号 VK_HEADER_VERSION_COMPLETE。
    - **位置**：`jni/include/ImGui/imgui_impl_vulkan.h:106`
- **087 `whole`**　/həʊl/　*adj.*　整个的　【35 次 / 10 个文件】
    - **用法**：`the whole buffer` = 整个缓冲区。
    - **频次**：标识符 3 次 · 注释 32 次
    - **例句**：`// Possible optimization: align the whole struct to cache line`
    - **中文翻译**：可能的优化：把整个结构体按缓存行对齐。
    - **位置**：`jni/include/Embree/PhysX.h:502`
- **088 `little`**　/ˈlɪtl/　*adj.*　小的；少量的　【34 次 / 8 个文件】
    - **用法**：`little-endian` = 小端序（低字节在前，第 24 章）。ARM/x86 都是小端。
    - **频次**：标识符 0 次 · 注释 34 次
    - **例句**：`// Widgets: Color Editor/Picker (tip: the ColorEdit* functions have a little color square that can be left-clicked to open a picker, and right-clicked to open an option menu.)`
    - **中文翻译**：控件：颜色编辑器/拾取器（小提示：ColorEdit* 函数有一个小色块，左键点击可打开拾取器，右键点击可打开选项菜单）。
    - **位置**：`jni/include/ImGui/imgui.h:732`
- **089 `modified`**　/ˈmɒdɪfaɪd/　*adj.*　已修改的　【34 次 / 11 个文件】
    - **用法**：`modified time` = 修改时间（`stat` 的字段）。
    - **频次**：标识符 0 次 · 注释 34 次
    - **例句**：`// Used for user input/output data. Will not be read or modified internally.`
    - **中文翻译**：用于用户的输入/输出数据。内部不会读取或修改它。
    - **位置**：`jni/include/Embree/include/rtcore_common.h:450`
- **090 `wide`**　/waɪd/　*adj.*　宽的　【34 次 / 8 个文件】
    - **用法**：`wide char` = 宽字符；`width` 是它的名词。
    - **频次**：标识符 23 次 · 注释 11 次
    - **例句**：`// is always a rectangle 1 wide * the height of that line segment; this`
    - **中文翻译**：始终是一个宽 1、高等于该线段长度的矩形；这……
    - **位置**：`jni/include/ImGui/imstb_truetype.h:3211`
- **091 `experimental`**　/ɪkˌsperɪˈmentl/　*adj.*　实验性的　【32 次 / 6 个文件】
    - **用法**：`experimental feature` = 实验特性，可能随时变。生产别用。
    - **频次**：标识符 0 次 · 注释 32 次
    - **例句**：`// [EXPERIMENTAL] Draw lines connecting TreeNode hierarchy. Discuss in GitHub issue #2920.`
    - **中文翻译**：[实验性] 绘制连接 TreeNode 层级的连线。讨论见 GitHub issue #2920。
    - **位置**：`jni/include/ImGui/imgui.h:1343`
- **092 `floating`**　/ˈfləʊtɪŋ/　*adj.*　浮点的　【32 次 / 10 个文件】
    - **用法**：`floating point` = 浮点数（第 26 章）。`float` 是它的简称。
    - **频次**：标识符 19 次 · 注释 13 次
    - **例句**：`// maximum extents defined such that floating point exceptions are avoided for standard use cases`
    - **中文翻译**：定义最大范围，以避免在标准用例下触发浮点异常。
    - **位置**：`jni/include/Embree/foundation/PxBounds3.h:45`
- **093 `negative`**　/ˈneɡətɪv/　*adj.*　负的　【31 次 / 7 个文件】
    - **用法**：`negative index` = 负下标。C++ 支持但会越界，必须自己检查。
    - **频次**：标识符 3 次 · 注释 28 次
    - **例句**：`// if return is negative, returns the negative of the number of characters that fit`
    - **中文翻译**：如果返回值为负，则返回「能放下的字符数」的负值。
    - **位置**：`jni/include/ImGui/imstb_truetype.h:544`
- **094 `strict`**　/strɪkt/　*adj.*　严格的　【31 次 / 30 个文件】
    - **用法**：`strict aliasing` = 严格别名规则（第 11 章，编译器优化所依赖的假设）。
    - **频次**：标识符 2 次 · 注释 29 次
    - **例句**：`// OF LIABILITY, WHETHER IN CONTRACT, STRICT LIABILITY, OR TORT`
    - **中文翻译**：无论责任形式（合同、严格责任还是侵权）……[BSD 许可证片段]
    - **位置**：`jni/include/Embree/foundation/Px.h:22`
- **095 `unsafe`**　/ˌʌnˈseɪf/　*adj.*　不安全的　【31 次 / 7 个文件】
    - **用法**：`unsafe` 表示“你必须自己保证正确”，编译器不帮你查。
    - **频次**：标识符 13 次 · 注释 18 次
    - **例句**：`#pragma clang diagnostic ignored "-Wfloat-equal" // warning: comparing floating point with == or != is unsafe`
    - **中文翻译**：让 Clang 编译器忽略「用 == 或 != 比较浮点数不安全」的警告。
    - **位置**：`jni/include/ImGui/imgui.h:143`
- **096 `older`**　/ˈəʊldə(r)/　*adj.*　较旧的　【30 次 / 6 个文件】
    - **用法**：`older versions` = 旧版本。偏移适配要考虑（第 88 章）。
    - **频次**：标识符 24 次 · 注释 6 次
    - **例句**：`// In older versions (until Dear ImGui 1.77) the AddCircle functions defaulted to num_segments == 12.`
    - **中文翻译**：在旧版本（Dear ImGui 1.77 之前）中，AddCircle 系列函数默认 num_segments == 12。
    - **位置**：`jni/include/ImGui/imgui.h:3370`
- **097 `slow`**　/sləʊ/　*adj.*　慢的　【30 次 / 8 个文件】
    - **用法**：`slow path` = 慢路径（少见情况）。性能剖析时看哪条路走得多。
    - **频次**：标识符 13 次 · 注释 17 次
    - **例句**：`// (THIS WILL SLOW DOWN DEAR IMGUI. Only use occasionally and disable after use)`
    - **中文翻译**：（这会拖慢 Dear ImGui。请只在偶尔需要时启用，用完后关闭。）
    - **位置**：`jni/include/ImGui/imconfig.h:138`
- **098 `external`**　/ɪkˈstɜːnl/　*adj.*　外部的　【29 次 / 9 个文件】
    - **用法**：`extern` = 外部符号声明（第 09 章）。
    - **频次**：标识符 18 次 · 注释 11 次
    - **例句**：`// - Use your own external storage: e.g. std::set<MyObjectId>, std::vector<MyObjectId>, interval trees, intrusively stored selection etc.`
    - **中文翻译**：你可以用自己的外部存储：例如 std::set<MyObjectId>、std::vector<MyObjectId>、区间树，或侵入式存储的选择集等。
    - **位置**：`jni/include/ImGui/imgui.h:3139`
- **099 `backward`**　/ˈbækwəd/　*adj.*　向后的　【28 次 / 10 个文件】
    - **用法**：`backward compatibility` = 向后兼容。`backward pass` = 反向遍历。
    - **频次**：标识符 7 次 · 注释 21 次
    - **例句**：`// Keep the old function for backward compatibility`
    - **中文翻译**：保留旧函数以兼容老代码。
    - **位置**：`jni/include/ImGui/ANativeWindowCreator.h:1115`
- **100 `major`**　/ˈmeɪdʒə(r)/　*adj.*　主要的、大的　【28 次 / 8 个文件】
    - **用法**：`major version` = 主版本号（如 Android 14 的 14）。
    - **频次**：标识符 22 次 · 注释 6 次
    - **例句**：`// accumulated 4x4 column major matrices from world space to instance space.`
    - **中文翻译**：从世界空间到实例空间累加得到的 4×4 列主序矩阵。
    - **位置**：`jni/include/Embree/include/rtcore_common.h:407`


---

## 第 3 组

- **101 `master`**　/ˈmɑːstə(r)/　*adj./n.*　主的、总控的　【28 次 / 6 个文件】
    - **用法**：`master branch`；主设备。现代术语多改用 primary。
    - **频次**：标识符 0 次 · 注释 28 次
    - **例句**：`// - See FAQ https://github.com/ocornut/imgui/blob/master/docs/FAQ.md#q-about-the-id-stack-system`
    - **中文翻译**：参见常见问题：……（关于 ID 栈系统的问答）。
    - **位置**：`jni/include/ImGui/imgui.h:2525`
- **102 `middle`**　/ˈmɪdl/　*adj./n.*　中间的　【27 次 / 8 个文件】
    - **用法**：`middle point` = 中点。BVH 按中点划分（第 96 章）。
    - **频次**：标识符 13 次 · 注释 14 次
    - **例句**：`// - You can also use regular integer: it is forever guaranteed that 0=Left, 1=Right, 2=Middle.`
    - **中文翻译**：你也可以用普通整数：永久保证 0=左键、1=右键、2=中键。
    - **位置**：`jni/include/ImGui/imgui.h:1107`
- **103 `double`**　/ˈdʌbl/　*adj./n.*　双的；双精度浮点　【25 次 / 9 个文件】
    - **用法**：`double` 是 64 位浮点，`float` 是 32 位（第 26 章）。也与 single 相对。
    - **频次**：标识符 0 次 · 注释 25 次
    - **例句**：`#define PX_MAX_F64 DBL_MAX // maximum possible double value`
    - **中文翻译**：定义 64 位浮点数的最大值（DBL_MAX），即双精度能表示的最大数值。
    - **位置**：`jni/include/Embree/foundation/PxSimpleTypes.h:88`
- **104 `hard`**　/hɑːd/　*adj.*　硬的；难的　【25 次 / 9 个文件】
    - **用法**：`hard-coded` = 硬编码（写死数字，是坏味道）；`hardware` = 硬件。
    - **频次**：标识符 11 次 · 注释 14 次
    - **例句**：`// (we need to scan previous line the hard way. maybe we could expose this as a new API function?)`
    - **中文翻译**：我们需要用笨办法扫描上一行；也许可以把这个做成一个新的 API 函数？
    - **位置**：`jni/include/ImGui/imstb_textedit.h:1070`
- **105 `short`**　/ʃɔːt/　*adj.*　短的　【25 次 / 8 个文件】
    - **用法**：也是类型名 `short`（至少 16 位）。
    - **频次**：标识符 0 次 · 注释 25 次
    - **例句**：`// - Short version: ID are hashes of the entire ID stack. If you are creating widgets in a loop you most likely`
    - **中文翻译**：简短版：ID 是整个 ID 栈的哈希值。如果你在循环里创建控件，你很可能……
    - **位置**：`jni/include/ImGui/imgui.h:600`
- **106 `sparse`**　/spɑːs/　*adj.*　稀疏的　【25 次 / 4 个文件】
    - **用法**：`sparse data` = 稀疏数据（大部分是 0）。与 dense 相对。
    - **频次**：标识符 21 次 · 注释 4 次
    - **例句**：`// 1.07 (2015-08-01) allow PackFontRanges to accept arrays of sparse codepoints;`
    - **中文翻译**：1.07（2015-08-01）：让 PackFontRanges 能接受稀疏码点的数组；
    - **位置**：`jni/include/ImGui/imstb_truetype.h:85`
- **107 `wrong`**　/rɒŋ/　*adj.*　错的　【24 次 / 6 个文件】
    - **用法**：`wrong order` = 顺序错了。调试注释高频。
    - **频次**：标识符 10 次 · 注释 14 次
    - **例句**：`// coverage. Thus, anti-aliasing of intersecting shapes may look wrong. If`
    - **中文翻译**：覆盖率。因此，相交图形的抗锯齿可能看上去不对。如果……
    - **位置**：`jni/include/ImGui/imstb_truetype.h:140`
- **108 `signed`**　/saɪnd/　*adj.*　有符号的　【23 次 / 10 个文件】
    - **用法**：`signed int` 能表示负数。默认 int 就是 signed（第 24 章）。
    - **频次**：标识符 0 次 · 注释 23 次
    - **例句**：`/* 8-bit signed integer */`
    - **中文翻译**：8 位有符号整数。
    - **位置**：`jni/include/Embree/include/rtcore_common.h:67`
- **109 `unsupported`**　/ˌʌnsəˈpɔːtɪd/　*adj.*　不支持的　【22 次 / 5 个文件】
    - **用法**：`unsupported` = 明确不支持，别浪费时间试。
    - **频次**：标识符 17 次 · 注释 5 次
    - **例句**：`default: return -1; // unsupported`
    - **中文翻译**：（switch 的）default 分支：返回 -1，表示不支持该情况。
    - **位置**：`jni/include/ImGui/imstb_truetype.h:2446`
- **110 `inactive`**　/ɪnˈæktɪv/　*adj.*　不活动的　【20 次 / 5 个文件】
    - **用法**：`inactive window` = 失焦窗口。
    - **频次**：标识符 8 次 · 注释 12 次
    - **例句**：`IMGUI_API const ImGuiPayload* GetDragDropPayload(); // peek directly into the current payload from anywhere. returns NULL when drag and drop is finished or inactive. use ImGuiPayload::IsDataType() to test for the payload type.`
    - **中文翻译**：IMGUI_API 函数：从任意位置直接窥视（peek）当前拖放载荷；拖放结束或未激活时返回 NULL，可用 ImGuiPayload::IsDataType() 判断载荷类型。
    - **位置**：`jni/include/ImGui/imgui.h:984`
- **111 `higher`**　/ˈhaɪə(r)/　*adj.*　更高的　【19 次 / 8 个文件】
    - **用法**：`higher address` = 更高地址。栈通常向低地址增长（第 08 章）。
    - **频次**：标识符 0 次 · 注释 19 次
    - **例句**：`You need to have an instance of this class to instance the higher level SDKs.`
    - **中文翻译**：你需要先拥有这个类的一个实例，才能实例化更高层的 SDK。
    - **位置**：`jni/include/Embree/foundation/PxFoundation.h:48`
- **112 `clean`**　/kliːn/　*adj.*　干净的、未改的　【18 次 / 7 个文件】
    - **用法**：`clean` 与 dirty 相对：clean = 数据没被改过。
    - **频次**：标识符 8 次 · 注释 10 次
    - **例句**：`// Clean up mirror surface`
    - **中文翻译**：清理镜像 surface。
    - **位置**：`jni/include/ImGui/ANativeWindowCreator.h:888`
- **113 `fail`**　/feɪl/　*v./n.*　失败　【18 次 / 9 个文件】
    - **用法**：`returns fail` = 返回失败。**每个可能失败的调用都要检查**（附录 U 的 S1）。
    - **频次**：标识符 2 次 · 注释 16 次
    - **例句**：`// (Note: some language parsers may fail to convert the bitfield members, in this case maybe drop store a single u32 or we can rework this)`
    - **中文翻译**：注意：某些语言解析器可能无法转换位域成员，遇到这种情况可以改存一个单独的 u32，或者我们再调整。
    - **位置**：`jni/include/ImGui/imgui.h:3657`
- **114 `light`**　/laɪt/　*adj.*　轻的、轻量的　【18 次 / 7 个文件】
    - **用法**：`lightweight` = 轻量级。
    - **频次**：标识符 17 次 · 注释 1 次
    - **例句**：`// Those light colors are better suited with a thicker font than the default one + FrameBorder`
    - **中文翻译**：那些浅色更适合配比默认更粗的字体，再加上 FrameBorder（边框）。
    - **位置**：`jni/src/ImGui/imgui_draw.cpp:323`
- **115 `minor`**　/ˈmaɪnə(r)/　*adj.*　次要的、小的　【18 次 / 13 个文件】
    - **用法**：`minor version` = 次版本号。
    - **频次**：标识符 5 次 · 注释 13 次
    - **例句**：`// Minor features`
    - **中文翻译**：小功能 / 次要特性。
    - **位置**：`jni/include/ImGui/imstb_rectpack.h:38`
- **116 `original`**　/əˈrɪdʒənl/　*adj.*　原来的　【18 次 / 6 个文件】
    - **用法**：`original value` = 原值（修改前的）。与 modified 对应。
    - **频次**：标识符 14 次 · 注释 4 次
    - **例句**：`// Code in this file that uses those functions is modified for [DEAR IMGUI] and deviates from the original stb_textedit.`
    - **中文翻译**：本文件中使用那些函数的代码是为 [DEAR IMGUI] 修改过的，与原始 stb_textedit 有出入。
    - **位置**：`jni/include/ImGui/imstb_textedit.h:418`
- **117 `exact`**　/ɪɡˈzækt/　*adj.*　精确的　【17 次 / 9 个文件】
    - **用法**：`exact match` = 完全匹配。与 approximate 相对。
    - **频次**：标识符 0 次 · 注释 17 次
    - **例句**：`\brief tests for exact zero vector`
    - **中文翻译**：（Doxygen 简述）检测是否为精确的零向量。
    - **位置**：`jni/include/Embree/foundation/PxVec2.h:144`
- **118 `positive`**　/ˈpɒzətɪv/　*adj.*　正的、积极的　【17 次 / 7 个文件】
    - **用法**：`positive value` = 正值。与 negative 相对。
    - **频次**：标识符 2 次 · 注释 15 次
    - **例句**：`// if return is positive, the first unused row of the bitmap`
    - **中文翻译**：如果返回值为正，则是位图中第一个未使用的行。
    - **位置**：`jni/include/ImGui/imstb_truetype.h:543`
- **119 `remote`**　/rɪˈməʊt/　*adj.*　远程的　【15 次 / 3 个文件】
    - **用法**：`remote process` = 远端进程（你要读内存的那个）。本项目核心场景。
    - **频次**：标识符 9 次 · 注释 6 次
    - **例句**：`// g.NavActivateId is not necessarily == g.NavId, in the case of remote activation (e.g. shortcuts)`
    - **中文翻译**：g.NavActivateId 不一定等于 g.NavId，在远程激活（例如快捷键）的情况下尤为如此。
    - **位置**：`jni/src/ImGui/imgui.cpp:11542`
- **120 `blocking`**　/ˈblɒkɪŋ/　*adj.*　阻塞的　【14 次 / 5 个文件】
    - **用法**：`blocking call` = 阻塞调用（会卡住线程）。与 non-blocking 相对。
    - **频次**：标识符 6 次 · 注释 8 次
    - **例句**：`//ImGuiHoveredFlags_AllowWhenBlockedByModal = 1 << 6, // Return true even if a modal popup window is normally blocking access to this item/window. FIXME-TODO: Unavailable yet.`
    - **中文翻译**：悬停标志：即使模态弹窗通常会挡住对该条目/窗口的访问，也返回 true。待办：尚未实现。
    - **位置**：`jni/include/ImGui/imgui.h:1473`
- **121 `private`**　/ˈpraɪvət/　*adj.*　私有的　【14 次 / 6 个文件】
    - **用法**：C++ 访问权限；`libgui` 是**私有库**（App 不可 dlopen，第 65 章）。
    - **频次**：标识符 0 次 · 注释 14 次
    - **例句**：`// - We don't use strongly typed enums much because they add constraints (can't extend in private code, can't store typed in bit fields, extra casting on iteration)`
    - **中文翻译**：我们很少用强类型枚举，因为它们会带来约束（无法在私有代码里扩展、无法以类型形式存进位域、迭代时还要额外转换）。
    - **位置**：`jni/include/ImGui/imgui.h:216`
- **122 `atomic`**　/əˈtɒmɪk/　*adj.*　原子的（不可打断）　【11 次 / 3 个文件】
    - **用法**：`std::atomic<T>` = 无锁的原子操作（第 82 章）。
    - **频次**：标识符 11 次 · 注释 0 次
    - **例句**：`#include <atomic>`
    - **中文翻译**：包含 C++ 标准原子操作头文件（用于无锁、线程安全的原子变量）。
    - **位置**：`jni/include/ImGui/spinlock.h:3`
- **123 `secondary`**　/ˈsekəndri/　*adj.*　次要的、次级的　【11 次 / 5 个文件】
    - **用法**：与 primary 成对。
    - **频次**：标识符 2 次 · 注释 9 次
    - **例句**：`// 1.5 (2014-09-10) add support for secondary keys for OS X`
    - **中文翻译**：1.5（2014-09-10）：为 OS X 增加副键支持。
    - **位置**：`jni/include/ImGui/imstb_textedit.h:53`
- **124 `greater`**　/ˈɡreɪtə(r)/　*adj.*　更大的　【10 次 / 7 个文件】
    - **用法**：`greater than` = 大于。比较运算符的自然语言描述。
    - **频次**：标识符 2 次 · 注释 8 次
    - **例句**：`// note that start may be less than or greater than end (e.g. when`
    - **中文翻译**：注意 start 可能小于也可能大于 end（例如当……
    - **位置**：`jni/include/ImGui/imstb_textedit.h:355`
- **125 `primary`**　/ˈpraɪməri/　*adj.*　主要的、首选的　【10 次 / 6 个文件】
    - **用法**：`primary monitor` = 主显示器（第 68 章多屏）。
    - **频次**：标识符 5 次 · 注释 5 次
    - **例句**：`// A primary data type`
    - **中文翻译**：一种基本数据类型。
    - **位置**：`jni/include/ImGui/imgui.h:1530`
- **126 `big`**　/bɪɡ/　*adj.*　大的　【9 次 / 5 个文件】
    - **用法**：`big-endian` = 大端序（高字节在前，第 24 章）。网络字节序就是大端。
    - **频次**：标识符 0 次 · 注释 9 次
    - **例句**：`// stbtt_GetCodepointBitmapBox() -- how big the bitmap must be`
    - **中文翻译**：stbtt_GetCodepointBitmapBox() —— 位图必须有多大。
    - **位置**：`jni/include/ImGui/imstb_truetype.h:128`
- **127 `sequential`**　/sɪˈkwenʃl/　*adj.*　顺序的　【9 次 / 5 个文件】
    - **用法**：`sequential access` = 顺序访问（比随机访问快，缓存友好）。
    - **频次**：标识符 4 次 · 注释 5 次
    - **例句**：`// Each .ttf/.ttc file may have more than one font. Each font has a sequential`
    - **中文翻译**：每个 .ttf/.ttc 文件可能包含不止一个字体。每个字体有一个连续的……
    - **位置**：`jni/include/ImGui/imstb_truetype.h:710`
- **128 `stable`**　/ˈsteɪbl/　*adj.*　稳定的　【9 次 / 6 个文件】
    - **用法**：`stable sort` = 稳定排序（相等元素保持原序）。
    - **频次**：标识符 3 次 · 注释 6 次
    - **例句**：`// Those values are guaranteed to be stable and we frequently use 0/1 directly. Named enums provided for convenience.`
    - **中文翻译**：那些值保证稳定，我们经常直接用 0/1。提供具名枚举只是为了使用方便。
    - **位置**：`jni/include/ImGui/imgui.h:1978`
- **129 `success`**　/səkˈses/　*n.*　成功　【9 次 / 5 个文件】
    - **用法**：`on success` = 成功时。与 failure 成对（`on failure` = 失败时）。
    - **频次**：标识符 4 次 · 注释 5 次
    - **例句**：`// 0.11 (2017-03-03) return packing success/fail result`
    - **中文翻译**：0.11（2017-03-03）：打包函数改为返回成功/失败结果。
    - **位置**：`jni/include/ImGui/imstb_rectpack.h:51`
- **130 `complex`**　/ˈkɒmpleks/　*adj.*　复杂的；复数　【8 次 / 5 个文件】
    - **用法**：数学里 complex number = 复数。形容词义是“复杂”。
    - **频次**：标识符 0 次 · 注释 8 次
    - **例句**：`// - Important! Sizing policies have complex and subtle side effects, much more so than you would expect.`
    - **中文翻译**：重要！尺寸策略有复杂而细微的副作用，远比你想象的多。
    - **位置**：`jni/include/ImGui/imgui.h:2035`
- **131 `consecutive`**　/kənˈsekjətɪv/　*adj.*　连续的　【7 次 / 4 个文件】
    - **用法**：`consecutive bytes` = 连续字节。合并相邻内存区间时用（题 2.4）。
    - **频次**：标识符 0 次 · 注释 7 次
    - **例句**：`\param[in] stride stride for accessing consecutive elements, defaults to the size of one element.`
    - **中文翻译**：（Doxygen 参数）stride：访问连续元素时的步长，默认等于单个元素的大小。
    - **位置**：`jni/include/Embree/foundation/PxStrideIterator.h:105`
- **132 `critical`**　/ˈkrɪtɪkl/　*adj.*　关键的、危急的　【7 次 / 4 个文件】
    - **用法**：`critical section` = 临界区（必须互斥的代码段，第 82 章）。也可以指日志最高级别。
    - **频次**：标识符 1 次 · 注释 6 次
    - **例句**：`//#pragma omp critical`
    - **中文翻译**：编译指示：#pragma omp critical（OpenMP 临界区）。
    - **位置**：`jni/include/Embree/PhysX.h:890`
- **133 `thread-safe`**　/ˌθred ˈseɪf/　*adj.*　线程安全的　【7 次 / 3 个文件】
    - **用法**：多线程同时调用不出错。**写“无锁”代码前先问自己是否真需要**（第 82 章）。
    - **频次**：标识符 0 次 · 注释 7 次
- **134 `parallel`**　/ˈpærəlel/　*adj.*　并行的　【6 次 / 4 个文件】
    - **用法**：`parallel loop` = 并行循环。与 serial（串行）相对。
    - **频次**：标识符 1 次 · 注释 5 次
    - **例句**：`//#pragma omp parallel for`
    - **中文翻译**：编译指示：#pragma omp parallel for（OpenMP 并行 for 循环）。
    - **位置**：`jni/include/Embree/PhysX.h:802`
- **135 `precise`**　/prɪˈsaɪs/　*adj.*　精确的　【6 次 / 4 个文件】
    - **用法**：`precise timing` = 精确定时。
    - **频次**：标识符 2 次 · 注释 4 次
    - **例句**：`// also more precise AA rasterizer, except if shapes overlap`
    - **中文翻译**：还有更精确的抗锯齿（AA）光栅化器，除非图形互相重叠。
    - **位置**：`jni/include/ImGui/imstb_truetype.h:5010`
- **136 `deep`**　/diːp/　*adj.*　深的　【5 次 / 4 个文件】
    - **用法**：`deep copy` = 深拷贝（连内部数据一起复制，第 14 章）。
    - **频次**：标识符 0 次 · 注释 5 次
    - **例句**：`// - Each route may be granted to a single owner. When multiple requests are made we have policies to select the winning route (e.g. deep most window).`
    - **中文翻译**：每条路由只能授予一个所有者。当有多个请求时，我们有一套策略来选出胜出的路由（例如最深层的窗口）。
    - **位置**：`jni/include/ImGui/imgui_internal.h:3640`
- **137 `lazy`**　/ˈleɪzi/　*adj.*　惰性的（用到才算）　【5 次 / 2 个文件】
    - **用法**：`lazy evaluation` = 惰性求值。第 99 章缓存就是惰性重算。
    - **频次**：标识符 5 次 · 注释 0 次
    - **例句**：`auto libgui = symbolMethod.Open("/system/lib64/libgui.so", RTLD_LAZY);`
    - **中文翻译**：用 symbolMethod.Open 以延迟绑定（RTLD_LAZY）方式打开系统库 /system/lib64/libgui.so，得到 libgui 句柄。
    - **位置**：`jni/include/ImGui/ANativeWindowCreator.h:268`
- **138 `loose`**　/luːs/　*adj.*　宽松的　【5 次 / 3 个文件】
    - **用法**：`loose coupling` = 松耦合。与 tight 相对。
    - **频次**：标识符 0 次 · 注释 5 次
    - **例句**：`// (e.g. inside menu bar vs loose menu items) based on parent ID.`
    - **中文翻译**：例如根据父级 ID 来区分「在菜单栏内」与「零散的菜单项」。
    - **位置**：`jni/src/ImGui/imgui_widgets.cpp:9380`
- **139 `volatile`**　/ˈvɒlətaɪl/　*adj.*　易变的（禁止优化掉读写）　【4 次 / 1 个文件】
    - **用法**：C++ 关键字。**volatile 不等于线程安全**，这是最常见的误解。
    - **频次**：标识符 0 次 · 注释 4 次
    - **例句**：`volatile 约束对象的每次访问，防止编译器省略、合并或用寄存器缓存该字段`
    - **中文翻译**：（中文代码注释）用 volatile 约束该字段的每次访问，防止编译器把它省略、合并或用寄存器缓存。
    - **位置**：`jni/src/Android_draw/driver.h:439`
- **140 `adjacent`**　/əˈdʒeɪsnt/　*adj.*　相邻的　【3 次 / 2 个文件】
    - **用法**：`adjacent regions` = 相邻区间（合并内存映射时用）。
    - **频次**：标识符 0 次 · 注释 3 次
    - **例句**：`// Note technically focus related but rather adjacent and close to BringWindowToFocusFront()`
    - **中文翻译**：注意：严格说是与焦点相关，但更贴近 BringWindowToFocusFront() 旁边。
    - **位置**：`jni/src/ImGui/imgui.cpp:13151`
- **141 `mandatory`**　/ˈmændətəri/　*adj.*　强制性的　【3 次 / 3 个文件】
    - **用法**：与 optional 相对。
    - **频次**：标识符 0 次 · 注释 3 次
    - **例句**：`// - USING THIS IS NOT MANDATORY. This is only a helper and not a required API.`
    - **中文翻译**：使用它不是强制的。它只是个辅助工具，并非必需的 API。
    - **位置**：`jni/include/ImGui/imgui.h:3136`
- **142 `virtual`**　/ˈvɜːtʃuəl/　*adj.*　虚的　【3 次 / 3 个文件】
    - **用法**：`virtual function` = 虚函数（第 11 章），靠 vtable 实现动态派发。也是“虚拟内存”的形容词。
    - **频次**：标识符 0 次 · 注释 3 次
    - **例句**：`// Transactional state of physical or virtual display. Note that libgui defines`
    - **中文翻译**：物理或虚拟显示的事务性状态。注意 libgui 定义了……
    - **位置**：`jni/include/ImGui/ANativeWindowCreator.h:118`
- **143 `mutable`**　/ˈmjuːtəbl/　*adj.*　可变的　【2 次 / 2 个文件】
    - **用法**：与 const 相对。C++ 的 `mutable` 允许在 const 函数里改某字段。
    - **频次**：标识符 0 次 · 注释 2 次
    - **例句**：`// Per Spec Format and View Format are expected to be the same unless VK_IMAGE_CREATE_MUTABLE_BIT was set at image creation`
    - **中文翻译**：按规范，Format 和 View Format 应当一致，除非在创建图像时设置了 VK_IMAGE_CREATE_MUTABLE_BIT。
    - **位置**：`jni/src/ImGui/imgui_impl_vulkan.cpp:1535`
- **144 `endian`**　/ˈendiən/　*n.*　字节序　【1 次 / 1 个文件】
    - **用法**：`endianness` = 多字节数据在内存里的排列顺序（第 24 章）。
    - **频次**：标识符 0 次 · 注释 1 次
    - **例句**：`0.58 fix bug: zlib uncompressed mode len/nlen was wrong endian`
    - **中文翻译**：0.58 修复：zlib 未压缩模式下 len/nlen 的大小端（endian）处理错误。
    - **位置**：`jni/src/ImGui/stb_image.cpp:7391`
- **145 `integral`**　/ˈɪntɪɡrəl/　*adj.*　整型的；积分的　【1 次 / 1 个文件】
    - **用法**：`integral type` = 整数类型（区别于浮点）。数学里是“积分”。
    - **频次**：标识符 0 次 · 注释 1 次
    - **例句**：`// move to integral bboxes (treating pixels as little squares, what pixels get touched)?`
    - **中文翻译**：改用整数包围盒（把像素当作小方块，看哪些像素会被碰到）？
    - **位置**：`jni/include/ImGui/imstb_truetype.h:2736`

