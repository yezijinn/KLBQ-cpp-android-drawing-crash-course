---
tags: [教程, 词汇, 词频]
aliases: [W6, vocab L6]
---


# W6 · 第 6 级：库前缀·命名约定·拼写陷阱（112 词）

> [!abstract] 本册在整套词汇表里的位置
> 看懂“这个名字为什么长这样”；本册不按词频，按结构分组
>
> **本册排序 = 项目源码里的真实出现次数**（从多到少）。
> 频次统计口径：`jni/` 下 97 个源文件、35 万行代码。
> 每个词条都附**这个项目里的真实例句**（含文件名与行号）。
> 音标采用**英式 IPA（DJ）**；与附录 K 的美式 IPA 差异见 [[词频词汇总览|W0]]。


## 本册词条

- [第 1 组（1–50）](#第-1-组)
- [第 2 组（51–100）](#第-2-组)
- [第 3 组（101–112）](#第-3-组)


---

## 第 1 组

- **001 `Im / ImGui`**　读作 ˌaɪ-em-ˈɡuːi　*前缀*　Immediate GUI（立即模式界面）　【21455 次 / 25 个文件】
    - **用法**：`Im` 是所有 ImGui 符号的前缀：`ImVec2`、`ImDrawList`、`ImGuiWindow`（第 61 章）。
    - **频次**：标识符 21324 次 · 注释 131 次
    - **例句**：`//#define IM_ASSERT(_EXPR) MyAssert(_EXPR)`
    - **中文翻译**：宏：把 IM_ASSERT(_EXPR) 替换成你自己的 MyAssert(_EXPR)。
    - **位置**：`jni/include/ImGui/imconfig.h:20`
- **002 `is / has / can 前缀`**　读作 ɪz / hæz / kæn　*命名约定*　布尔含义前缀　【4353 次 / 63 个文件】
    - **用法**：`isValid`、`hasTexture`、`canDraw`。**函数名直接是问题的答案**，最好读。
    - **频次**：标识符 1889 次 · 注释 2464 次
    - **例句**：`// if current ptr not contains or current ptr is height field, load it data!`
    - **中文翻译**：如果当前指针不含目标，或者当前指针是一个高度场，就加载它的数据！
    - **位置**：`jni/include/Embree/PhysX.h:1263`
- **003 `fa / fas / far / fab`**　读作 ˌef-ˈeɪ　*前缀*　FontAwesome 字体族（solid/regular/brands）　【3228 次 / 3 个文件】
    - **用法**：`fa-solid`、`fa-brands`。图标常量的名字前缀。
    - **频次**：标识符 3213 次 · 注释 15 次
    - **例句**：`#define ICON_FA_TABLET_SCREEN_BUTTON "\xef\x8f\xba" // U+f3fa`
    - **中文翻译**：定义 FontAwesome 图标「平板屏幕按钮」的转义编码（U+f3fa）。
    - **位置**：`jni/include/ImGui/gui_icon.h:156`
- **004 `Px / PxVec3`**　读作 ˌpiː-ˈeks　*前缀*　PhysX 命名空间前缀　【2694 次 / 40 个文件】
    - **用法**：`PxShape`、`PxRigidActor`、`PxBounds3`。碰撞体数据（第 93 章）。
    - **频次**：标识符 2648 次 · 注释 46 次
    - **例句**：`// screenX = px + row0(x) / camera * px (水平焦距 px/t)`
    - **中文翻译**：screenX = px + row0(x) / camera * px（水平焦距 px/t）。
    - **位置**：`jni/include/Android_draw/WorldToScreen.h:49`
- **005 `u / v`**　读作 juː / viː　*单字母*　纹理坐标；重心坐标　【2498 次 / 11 个文件】
    - **用法**：`u,v` 是三角形重心坐标，同时也是纹理 UV（第 95 章）。
    - **频次**：标识符 0 次 · 注释 2498 次
    - **例句**：`/* Interpolates vertex data to some u/v location and optionally calculates all derivatives. */`
    - **中文翻译**：把顶点数据插值到某个 u/v 坐标位置，并可选择性地计算所有导数。
    - **位置**：`jni/include/Embree/include/rtcore_geometry.h:301`
- **006 `vk / VK_`**　读作 ˌviː-ˈkeɪ　*前缀*　Vulkan API 前缀　【2436 次 / 8 个文件】
    - **用法**：函数 `vkQueueSubmit`、类型 `VkDevice`、枚举 `VK_SUCCESS`（第 58 章）。
    - **频次**：标识符 2379 次 · 注释 57 次
    - **例句**：`// [!] Renderer: User texture binding. Use a VK_DESCRIPTOR_TYPE_SAMPLED_IMAGE 'VkDescriptorSet' as texture identifier. Call ImGui_ImplVulkan_AddTexture() to register one. Read the FAQ about ImTextureID/ImTextureRef + https://github.com/ocornut/imgui/pull/914 for discussions.`
    - **中文翻译**：[!] 渲染器：用户纹理绑定。用 VK_DESCRIPTOR_TYPE_SAMPLED_IMAGE 类型的 'VkDescriptorSet' 作为纹理标识符。调用 ImGui_ImplVulkan_AddTexture() 来注册一个。详见关于 ImTextureID/ImTextureRef 的常见问题，以及……的讨论。
    - **位置**：`jni/include/ImGui/imgui_impl_vulkan.h:5`
- **007 `xef / xee`**　读作 eks-ˌiː-ˈef　*编码前缀*　FontAwesome 图标字节编码的前缀　【1988 次 / 1 个文件】
    - **用法**：`gui_icon.h` 里成片的 `ï½`、`î½` 这类反斜杠十六进制转义。每个图标由 3 个字节组成，反斜杠后面跟的 `ef`/`ee` 说明它在 Unicode 私有区。**这不是乱码，是设计如此**。
    - **频次**：标识符 1988 次 · 注释 0 次
    - **例句**：`#define ICON_FA_FILL_DRIP "\xef\x95\xb6" // U+f576`
    - **中文翻译**：定义 FontAwesome 图标「填充滴落」的转义编码（U+f576）。
    - **位置**：`jni/include/ImGui/gui_icon.h:20`
- **008 `width（拼写）`**　读作 wɪdθ　*拼写*　宽度　【1688 次 / 28 个文件】
    - **用法**：**常见错拼：widht、withd、wight**。正确拼写 w-i-d-t-h。
    - **频次**：标识符 1395 次 · 注释 293 次
    - **例句**：`\brief returns the dimensions (width/height/depth) of this axis aligned box.`
    - **中文翻译**：（Doxygen 简述）返回该轴对齐盒子的尺寸（宽/高/深）。
    - **位置**：`jni/include/Embree/foundation/PxBounds3.h:209`
- **009 `rtc / RTC_`**　读作 ˌɑː-tiː-ˈsiː　*前缀*　Embree 的 Ray Tracing Core（光追内核）　【1502 次 / 12 个文件】
    - **用法**：`rtcNewScene`、`rtcIntersect1`、`rtcOccluded1`、`rtcCommitScene`（第 97 章）。
    - **频次**：标识符 1499 次 · 注释 3 次
    - **例句**：`//rtcSetDeviceProperty(device, RTC_DEVICE_PROPERTY_TASKING_SYSTEM, 1); // USE TBB`
    - **中文翻译**：设置设备属性（任务系统）为 1，即使用 TBB（Intel 线程构建块）。
    - **位置**：`jni/include/Embree/PhysX.h:1472`
- **010 `assert / static_assert`**　读作 əˈsɜːt　*宏/关键字*　断言　【1101 次 / 27 个文件】
    - **用法**：`assert` 运行时检查（Release 会被去掉）；`static_assert` 编译期检查。
    - **频次**：标识符 969 次 · 注释 132 次
    - **例句**：`/* Base class to handle assert failures */`
    - **中文翻译**：用于处理断言失败情况的基类。
    - **位置**：`jni/include/Embree/foundation/PxAssert.h:43`
- **011 `stb / stbi / stbtt`**　读作 ˌes-tiː-ˈbiː　*前缀*　stb 单文件库家族　【997 次 / 12 个文件】
    - **用法**：`stb_image`（读图）、`stb_truetype`（解析 TTF 字体，第 63 章）。名字来自作者 Sean Barrett。
    - **频次**：标识符 734 次 · 注释 263 次
    - **例句**：`//---- Avoid multiple STB libraries implementations, or redefine path/filenames to prioritize another version`
    - **中文翻译**：提示：避免同时包含多个 STB 库的实现，或者重定义路径/文件名来优先使用另一个版本。
    - **位置**：`jni/include/ImGui/imconfig.h:75`
- **012 `height（拼写）`**　读作 haɪt　*拼写*　高度　【874 次 / 28 个文件】
    - **用法**：**常见错拼：hight、heigth、hieght**。正确是 h-e-i-g-h-t。**读音里没有 g 的音，但拼写有**。
    - **频次**：标识符 703 次 · 注释 171 次
    - **例句**：`// if current ptr not contains or current ptr is height field, load it data!`
    - **中文翻译**：如果当前指针不含目标，或者当前指针是一个高度场，就加载它的数据！
    - **位置**：`jni/include/Embree/PhysX.h:1263`
- **013 `g / b / a`**　读作 dʒiː / biː / eɪ　*单字母*　绿/蓝/透明度通道　【749 次 / 18 个文件】
    - **用法**：RGBA 的后三个通道。`a` = alpha 透明度。
    - **频次**：标识符 0 次 · 注释 749 次
    - **例句**：`//std::isfinite not recommended as of Feb 2017, since it doesn't work with g++/clang's floating point optimization.`
    - **中文翻译**：自 2017 年 2 月起不再推荐 std::isfinite，因为它和 g++/clang 的浮点优化不兼容。
    - **位置**：`jni/include/Embree/foundation/unix/PxUnixIntrinsics.h:129`
- **014 `g_ 前缀`**　读作 ˌdʒiː-ˈʌndəskɔː　*命名约定*　全局变量（global）　【749 次 / 18 个文件】
    - **用法**：`g_camera`、`g_settings`。一眼分清全局与局部。
    - **频次**：标识符 0 次 · 注释 749 次
    - **例句**：`//std::isfinite not recommended as of Feb 2017, since it doesn't work with g++/clang's floating point optimization.`
    - **中文翻译**：自 2017 年 2 月起不再推荐 std::isfinite，因为它和 g++/clang 的浮点优化不兼容。
    - **位置**：`jni/include/Embree/foundation/unix/PxUnixIntrinsics.h:129`
- **015 `_ptr 后缀`**　读作 ˈpɔɪntə(r)　*命名后缀*　指针　【627 次 / 28 个文件】
    - **用法**：`shared_ptr`、`unique_ptr`（第 14 章）。
    - **频次**：标识符 615 次 · 注释 12 次
    - **例句**：`// if current ptr not contains or current ptr is height field, load it data!`
    - **中文翻译**：如果当前指针不含目标，或者当前指针是一个高度场，就加载它的数据！
    - **位置**：`jni/include/Embree/PhysX.h:1263`
- **016 `x / y / z / w`**　读作 eks / waɪ / zed / ˈdʌbljuː　*单字母*　坐标/分量　【571 次 / 33 个文件】
    - **用法**：`w` 是齐次坐标的第 4 分量，**不是“宽度”**（第 46 章）。透视除法就是除以 w。
    - **频次**：标识符 0 次 · 注释 571 次
    - **例句**：`// camera = matrix[3]*x + matrix[7]*y + matrix[11]*z + matrix[15] (forward 深度)`
    - **中文翻译**：相机深度 = matrix[3]*x + matrix[7]*y + matrix[11]*z + matrix[15]（前向深度）。
    - **位置**：`jni/include/Android_draw/WorldToScreen.h:48`
- **017 `str* 家族`**　读作 ˌes-tiː-ˈɑː(r)　*函数族*　C 字符串函数　【522 次 / 16 个文件】
    - **用法**：`strlen`（长度）、`strcpy/strncpy`（复制）、`strcmp/strncmp`（比较）、`strstr`（查找）、`strtok`（切分）、`strtol/strtod`（转数值）。
    - **频次**：标识符 486 次 · 注释 36 次
    - **例句**：`// whereas "str_id" denote a string that is only used as an ID and not normally displayed.`
    - **中文翻译**：而 "str_id" 指的是只用作 ID、通常不会被显示出来的字符串。
    - **位置**：`jni/include/ImGui/imgui.h:604`
- **018 `c / d / e / f`**　读作 siː / diː / iː / ef　*单字母*　字符/距离/误差/浮点　【482 次 / 50 个文件】
    - **用法**：`c` = char；`d` = distance 或 double；`e` = error 或自然常数；`f` = float 或 function。
    - **频次**：标识符 0 次 · 注释 482 次
    - **例句**：`// Copyright (c) 2008-2018 NVIDIA Corporation. All rights reserved.`
    - **中文翻译**：版权所有 (c) 2008-2018 NVIDIA 公司。保留所有权利。
    - **位置**：`jni/include/Embree/foundation/Px.h:26`
- **019 `_impl / _internal 后缀`**　读作 ɪmpl / ɪnˈtɜːnl　*命名后缀*　实现细节（不要直接调）　【448 次 / 11 个文件】
    - **用法**：看到就知道是内部实现，改版本可能消失。
    - **频次**：标识符 381 次 · 注释 67 次
    - **例句**：`// IMGUI_API is used for core imgui functions, IMGUI_IMPL_API is used for the default backends files (imgui_impl_xxx.h)`
    - **中文翻译**：IMGUI_API 用于 ImGui 核心函数；IMGUI_IMPL_API 用于默认的后端文件（imgui_impl_xxx.h）。
    - **位置**：`jni/include/ImGui/imgui.h:83`
- **020 `b 前缀`**　读作 ˌbiː-ˈʌndəskɔː　*命名约定*　布尔（bool）　【355 次 / 24 个文件】
    - **用法**：`bVisible`。更推荐的写法是 `isVisible`（能读成句子）。
    - **频次**：标识符 0 次 · 注释 355 次
    - **例句**：`extern long int GName;// GNames/FNamePool 静态指针 (libUE4 + 0xb236b00 读出)`
    - **中文翻译**：外部（extern）声明一个 long int 类型的 GName（即 GNames/FNamePool 的静态指针，从 libUE4 的 +0xb236b00 处读出）。
    - **位置**：`jni/include/Android_draw/draw.h:39`
- **021 `length（拼写）`**　读作 leŋθ　*拼写*　长度　【267 次 / 16 个文件】
    - **用法**：**常见错拼：lenght、lenth**。正确 l-e-n-g-t-h。
    - **频次**：标识符 226 次 · 注释 41 次
    - **例句**：`// The CPU cost is very roughly proportional to text length, so a 10 KB buffer should cost about ten times less.`
    - **中文翻译**：CPU 开销大致与文本长度成正比，所以一个 10 KB 的缓冲区开销大约少十倍。
    - **位置**：`jni/include/ImGui/imgui.h:1312`
- **022 `h / l / o / p / q`**　读作 eɪtʃ / el / əʊ / piː / kjuː　*单字母*　高/长/输出/指针/四元数　【242 次 / 54 个文件】
    - **用法**：`h` = height 或 handle；`p` = pointer；`q` = quaternion。**单字母含义靠上下文，没有硬规定**。
    - **频次**：标识符 0 次 · 注释 242 次
    - **例句**：`#endif //NATIVESURFACE_DRAW_H`
    - **中文翻译**：结束条件编译（对应头文件 NATIVESURFACE_DRAW_H 的包含守卫）。
    - **位置**：`jni/include/Android_draw/draw.h:84`
- **023 `new / delete`**　读作 njuː / dɪˈliːt　*关键字*　C++ 堆分配/释放　【242 次 / 20 个文件】
    - **用法**：**必须配对**。`new[]` 要配 `delete[]`，`new` 配 `delete`。
    - **频次**：标识符 0 次 · 注释 242 次
    - **例句**：`// create new geometry`
    - **中文翻译**：创建新的几何体。
    - **位置**：`jni/include/Embree/PhysX.h:1537`
- **024 `n / m`**　读作 en / em　*单字母*　数量；行数　【240 次 / 15 个文件】
    - **用法**：`n` = 个数（`nVertices`）；矩阵里 `m×n` = 行数×列数。
    - **频次**：标识符 0 次 · 注释 240 次
    - **例句**：`//printf("ShapeData.m_shape_flags %d, ShapeData.mType %d \n",ShapeData.m_shape_flags,ShapeData.mType);`
    - **中文翻译**：printf 打印形状数据的标志和类型（ShapeData.m_shape_flags、ShapeData.mType）。
    - **位置**：`jni/include/Embree/PhysX.h:720`
- **025 `n 前缀`**　读作 en　*命名约定*　数量（number of）　【240 次 / 15 个文件】
    - **用法**：`nVertices` = 顶点个数。**注意不要和 `num` 混着用同一种含义**。
    - **频次**：标识符 0 次 · 注释 240 次
    - **例句**：`//printf("ShapeData.m_shape_flags %d, ShapeData.mType %d \n",ShapeData.m_shape_flags,ShapeData.mType);`
    - **中文翻译**：printf 打印形状数据的标志和类型（ShapeData.m_shape_flags、ShapeData.mType）。
    - **位置**：`jni/include/Embree/PhysX.h:720`
- **026 `sz / lpsz`**　读作 ˌes-ˈzed / ˌel-piː-ˈes-zed　*前缀*　以 0 结尾的字符串 / 指向它的指针　【223 次 / 6 个文件】
    - **用法**：Windows 风格。**看到 sz 就知道结尾有 `\0`**。
    - **频次**：标识符 221 次 · 注释 2 次
    - **例句**：`// Render an arrow. 'pos' is position of the arrow tip. half_sz.x is length from base to tip. half_sz.y is length on each side.`
    - **中文翻译**：绘制一个箭头。'pos' 是箭尖位置。half_sz.x 是从底部到尖端的长度，half_sz.y 是两侧各自的长度。
    - **位置**：`jni/src/ImGui/imgui_draw.cpp:6101`
- **027 `_view / _span 后缀`**　读作 vjuː / spæn　*命名后缀*　不拥有内存的视图类型　【176 次 / 19 个文件】
    - **用法**：`string_view`、`span`。**零拷贝的关键**（题 1.7）。
    - **频次**：标识符 152 次 · 注释 24 次
    - **例句**：`// - ImGuiSelectionUserData is often used to store your item index within the current view (but may store something else).`
    - **中文翻译**：ImGuiSelectionUserData 常用来存储当前视图中你的条目索引（但也可以存别的东西）。
    - **位置**：`jni/include/ImGui/imgui.h:771`
- **028 `i / j / k`**　读作 aɪ / dʒeɪ / keɪ　*单字母*　循环下标；四元数虚部　【164 次 / 24 个文件】
    - **用法**：`i/j/k` 做多层循环下标；四元数里 `i/j/k` 是三个虚单位（第 47 章）。
    - **频次**：标识符 0 次 · 注释 164 次
    - **例句**：`//MeshData.Vertices[i] = ScaleVal.transform(MeshData.Vertices[i]);`
    - **中文翻译**：MeshData.Vertices[i] = ScaleVal.transform(MeshData.Vertices[i]);（用 ScaleVal 对第 i 个顶点做变换）。
    - **位置**：`jni/include/Embree/PhysX.h:1020`
- **029 `p 前缀`**　读作 ˌpiː-ˈʌndəskɔː　*命名约定*　指针（pointer）　【114 次 / 11 个文件】
    - **用法**：`pData`、`pNext`。**现在更推荐用类型表达**（如 `T*`），前缀反而容易过时。
    - **频次**：标识符 0 次 · 注释 114 次
    - **例句**：`PxPlaneT mPlane; //!< Plane equation for this polygon //Could drop 4th elem as it can be computed from any vertex as: d = - p.dot(n);`
    - **中文翻译**：PxPlaneT 类型的 mPlane 成员：该多边形的平面方程；第 4 个分量可省略，因为它可由任意顶点算出：d = -p.dot(n)。
    - **位置**：`jni/include/Embree/PhysX.h:363`
- **030 `preferred（拼写）`**　读作 prɪˈfɜːd　*拼写*　首选的　【113 次 / 10 个文件】
    - **用法**：**双 r**：pre-fer-red。
    - **频次**：标识符 92 次 · 注释 21 次
    - **例句**：`// preferred solution: omit the parameter's name from the declaration`
    - **中文翻译**：推荐做法：在声明中省略参数的名字。
    - **位置**：`jni/include/Embree/foundation/PxPreprocessor.h:469`
- **031 `until（拼写）`**　读作 ənˈtɪl　*拼写*　直到　【107 次 / 10 个文件】
    - **用法**：**双 l**：un-til。常见错拼 untill（多一个 l 反而错）。
    - **频次**：标识符 35 次 · 注释 72 次
    - **例句**：`IMGUI_API void NewFrame(); // start a new Dear ImGui frame, you can submit any command from this point until Render()/EndFrame().`
    - **中文翻译**：IMGUI_API 函数：开启新的一帧（NewFrame）；从此刻起直到 Render()/EndFrame()，你可以提交任何绘制指令。
    - **位置**：`jni/include/ImGui/imgui.h:403`
- **032 `available（拼写）`**　读作 əˈveɪləbl　*拼写*　可用的　【104 次 / 16 个文件】
    - **用法**：**双 l 在中间**：avail-able。常见错拼 avaliable。
    - **频次**：标识符 8 次 · 注释 96 次
    - **例句**：`//#define IMGUI_DISABLE_SSE // Disable use of SSE intrinsics even if available`
    - **中文翻译**：宏：即使可用也禁用 SSE 指令集内建函数。
    - **位置**：`jni/include/ImGui/imconfig.h:56`
- **033 `malloc / calloc / realloc / free`**　读作 ˈmælɒk / ˈkælɒk / ˌriːˈælək / friː　*函数族*　C 堆内存四件套　【102 次 / 11 个文件】
    - **用法**：`malloc` 分配、`calloc` 分配并清零、`realloc` 扩容、`free` 释放。
    - **频次**：标识符 85 次 · 注释 17 次
    - **例句**：`//#define IMGUI_DISABLE_DEFAULT_ALLOCATORS // Don't implement default allocators calling malloc()/free() to avoid linking with them. You will need to call ImGui::SetAllocatorFunctions().`
    - **中文翻译**：宏：禁用默认分配器（不实现调用 malloc()/free() 的默认分配器，以避免链接它们）。你需要改用 ImGui::SetAllocatorFunctions()。
    - **位置**：`jni/include/ImGui/imconfig.h:52`
- **034 `initial（拼写）`**　读作 ɪˈnɪʃl　*拼写*　初始的　【98 次 / 14 个文件】
    - **用法**：**-itial**：in-i-tial。动词是 initialize（英式也写 initialise）。
    - **频次**：标识符 33 次 · 注释 65 次
    - **例句**：`// - Dragging operations are only reported after mouse has moved a certain distance away from the initial clicking position (see 'lock_threshold' and 'io.MouseDraggingThreshold')`
    - **中文翻译**：拖拽操作只有在鼠标离开初始点击位置一定距离后才会被上报（见 'lock_threshold' 和 'io.MouseDraggingThreshold'）。
    - **位置**：`jni/include/ImGui/imgui.h:1108`
- **035 `address（拼写）`**　读作 əˈdres　*拼写*　地址　【89 次 / 13 个文件】
    - **用法**：**双 d 双 s**：ad-dress。本项目天天用这个词，写错会很显眼。
    - **频次**：标识符 78 次 · 注释 11 次
    - **例句**：`// the array syntax is just a way to document the number of elements that are expected to be accessible. You can pass address of your first element out of a contiguous set, e.g. &myvector.x`
    - **中文翻译**：数组语法只是一种写法，用来说明预期可访问的元素个数。你可以传一段连续数据中第一个元素的地址，例如 &myvector.x。
    - **位置**：`jni/include/ImGui/imgui.h:671`
- **036 `argument（拼写）`**　读作 ˈɑːɡjumənt　*拼写*　参数　【81 次 / 13 个文件】
    - **用法**：**arg-u-ment，没有 e**：不是 arguement。
    - **频次**：标识符 22 次 · 注释 59 次
    - **例句**：`// The (world space) query object that was passed as an argument of rtcPointQuery. The`
    - **中文翻译**：（世界空间中的）查询对象，它是作为 rtcPointQuery 的参数传入的。该……
    - **位置**：`jni/include/Embree/include/rtcore_common.h:444`
- **037 `mem* 家族`**　读作 mem　*函数族*　内存函数　【80 次 / 11 个文件】
    - **用法**：`memcpy`（复制）、`memmove`（可重叠复制）、`memset`（填充）、`memcmp`（比较）。**`memcpy` 处理重叠区是 UB，要用 `memmove`**。
    - **频次**：标识符 74 次 · 注释 6 次
    - **例句**：`// to "guard" illegal mem access, for example.`
    - **中文翻译**：用来「防范」非法的内存访问，例如。
    - **位置**：`jni/include/Embree/foundation/PxAssert.h:68`
- **038 `s / r`**　读作 es / ɑː(r)　*单字母*　标量 / 半径　【78 次 / 12 个文件】
    - **用法**：`s` 常作标量或秒；`r` 常作半径或“读端”。
    - **频次**：标识符 0 次 · 注释 78 次
    - **例句**：`//! \brief method called with invalid parameter(s)`
    - **中文翻译**：简介：以无效参数被调用的方法。
    - **位置**：`jni/include/Embree/foundation/PxErrors.h:63`
- **039 `s_ 前缀`**　读作 ˌes-ˈʌndəskɔː　*命名约定*　静态变量（static）　【78 次 / 12 个文件】
    - **用法**：`s_instance` = 单例的静态实例。
    - **频次**：标识符 0 次 · 注释 78 次
    - **例句**：`//! \brief method called with invalid parameter(s)`
    - **中文翻译**：简介：以无效参数被调用的方法。
    - **位置**：`jni/include/Embree/foundation/PxErrors.h:63`
- **040 `integer（拼写）`**　读作 ˈɪntɪdʒə(r)　*拼写*　整数　【67 次 / 9 个文件】
    - **用法**：**-eger 不是 -iger**：int-e-ger。形容词是 integral。
    - **频次**：标识符 12 次 · 注释 55 次
    - **例句**：`/* 8-bit unsigned integer */`
    - **中文翻译**：8 位无符号整数。
    - **位置**：`jni/include/Embree/include/rtcore_common.h:61`
- **041 `_t 后缀`**　读作 ˌʌndəskɔː-ˈtiː　*类型后缀*　类型名（type）　【61 次 / 20 个文件】
    - **用法**：`size_t`、`uint32_t`、`time_t`、`pthread_t`。POSIX 惯例。
    - **频次**：标识符 0 次 · 注释 61 次
    - **例句**：`// screenX = px + row0(x) / camera * px (水平焦距 px/t)`
    - **中文翻译**：screenX = px + row0(x) / camera * px（水平焦距 px/t）。
    - **位置**：`jni/include/Android_draw/WorldToScreen.h:49`
- **042 `t`**　读作 tiː　*单字母*　参数（插值比例/射线距离）　【61 次 / 20 个文件】
    - **用法**：`Lerp(a,b,t)` 里 t∈[0,1]；射线 `origin + dir*t` 里 t 是距离。
    - **频次**：标识符 0 次 · 注释 61 次
    - **例句**：`// screenX = px + row0(x) / camera * px (水平焦距 px/t)`
    - **中文翻译**：screenX = px + row0(x) / camera * px（水平焦距 px/t）。
    - **位置**：`jni/include/Android_draw/WorldToScreen.h:49`
- **043 `k 前缀`**　读作 keɪ　*命名约定*　常量（konstant）　【58 次 / 11 个文件】
    - **用法**：`kMaxVertices`、`kEpsilon`。Google C++ 风格推荐。
    - **频次**：标识符 0 次 · 注释 58 次
    - **例句**：`//---- Use 32-bit vertex indices (default is 16-bit) is one way to allow large meshes with more than 64K vertices.`
    - **中文翻译**：用 32 位顶点索引（默认 16 位）是支持超过 64K 顶点的大型网格的一种方式。
    - **位置**：`jni/include/ImGui/imconfig.h:120`
- **044 `library（拼写）`**　读作 ˈlaɪbrəri　*拼写*　库　【52 次 / 15 个文件】
    - **用法**：**有 r**：li-brar-y。常见错拼 libary。**读音里那个 r 常常被吞掉，但拼写必须在**。
    - **频次**：标识符 0 次 · 注释 52 次
    - **例句**：`/** \brief The library failed for some reason.`
    - **中文翻译**：（文档注释）简述：库由于某种原因初始化或操作失败。
    - **位置**：`jni/include/Embree/foundation/PxErrors.h:72`
- **045 `gl / GL_`**　读作 ˌdʒiː-ˈel　*前缀*　OpenGL 前缀　【47 次 / 3 个文件】
    - **用法**：`glDrawArrays`、`GL_TRIANGLES`。安卓上由驱动实现。
    - **频次**：标识符 39 次 · 注释 8 次
    - **例句**：`// Some graphics API such as GL ES 1/2 don't have a way to offset the starting vertex so it is not supported for them.`
    - **中文翻译**：某些图形 API（如 GL ES 1/2）没有偏移起始顶点的办法，所以对它们不支持。
    - **位置**：`jni/src/ImGui/imgui_draw.cpp:2325`
- **046 `recommended（拼写）`**　读作 ˌrekəˈmendɪd　*拼写*　推荐的　【37 次 / 10 个文件】
    - **用法**：**双 m 单 c**：rec-om-mend-ed。记忆点：“com” 后面接 “mend”。
    - **频次**：标识符 0 次 · 注释 37 次
    - **例句**：`//std::isfinite not recommended as of Feb 2017, since it doesn't work with g++/clang's floating point optimization.`
    - **中文翻译**：自 2017 年 2 月起不再推荐 std::isfinite，因为它和 g++/clang 的浮点优化不兼容。
    - **位置**：`jni/include/Embree/foundation/unix/PxUnixIntrinsics.h:129`
- **047 `printf 家族`**　读作 ˈprɪntef　*函数族*　格式化输出　【32 次 / 10 个文件】
    - **用法**：`printf`（输出）、`sprintf`（写字符串，**危险**）、`snprintf`（带长度上限，**推荐**）、`fprintf`（写文件）。
    - **频次**：标识符 16 次 · 注释 16 次
    - **例句**：`//printf("ShapeData.m_shape_flags %d, ShapeData.mType %d \n",ShapeData.m_shape_flags,ShapeData.mType);`
    - **中文翻译**：printf 打印形状数据的标志和类型（ShapeData.m_shape_flags、ShapeData.mType）。
    - **位置**：`jni/include/Embree/PhysX.h:720`
- **048 `writing / written（拼写）`**　读作 ˈraɪtɪŋ / ˈrɪtn　*拼写*　写（动名词/过去分词）　【32 次 / 9 个文件】
    - **用法**：**双 t**：writ-ting、writ-ten（重音在后，辅音双写）。
    - **频次**：标识符 0 次 · 注释 32 次
    - **例句**：`// - Important: By default, AddFontFromMemoryTTF() takes ownership of the data. Even though we are not writing to it, we will free the pointer on destruction.`
    - **中文翻译**：重要：默认情况下 AddFontFromMemoryTTF() 会接管数据的所有权。即使我们不往里写，销毁时也会释放该指针。
    - **位置**：`jni/include/ImGui/imgui.h:3729`
- **049 `sizeof / offsetof / alignof`**　读作 ˈsaɪzɒv / ɒfˈsetɒv / əˈlaɪnɒv　*运算符*　取大小 / 取字段偏移 / 取对齐　【30 次 / 10 个文件】
    - **用法**：`offsetof` 是算结构体字段偏移的标准工具（第 07 章）。**这是你找偏移的理论基础**。
    - **频次**：标识符 0 次 · 注释 30 次
    - **例句**：`// Sorting specification for one column of a table (sizeof == 12 bytes)`
    - **中文翻译**：表格某一列的排序规格（sizeof == 12 字节）。
    - **位置**：`jni/include/ImGui/imgui.h:2183`
- **050 `beginning（拼写）`**　读作 bɪˈɡɪnɪŋ　*拼写*　开始　【29 次 / 9 个文件】
    - **用法**：**双 n**：be-gin-ning。因为重音在后，辅音要双写。
    - **频次**：标识符 0 次 · 注释 29 次
    - **例句**：`// - Any change of Scroll will be applied at the beginning of next frame in the first call to Begin().`
    - **中文翻译**：对滚动位置的任何改动，都会在下一帧第一次调用 Begin() 时生效。
    - **位置**：`jni/include/ImGui/imgui.h:494`


---

## 第 2 组

- **051 `m_ 前缀`**　读作 ˌem-ˈʌndəskɔː　*命名约定*　类成员（member）　【28 次 / 8 个文件】
    - **用法**：`m_offset` = 这个类的成员字段。另一种写法是尾随下划线 `offset_`。
    - **频次**：标识符 0 次 · 注释 28 次
    - **例句**：`//printf("ShapeData.m_shape_flags %d, ShapeData.mType %d \n",ShapeData.m_shape_flags,ShapeData.mType);`
    - **中文翻译**：printf 打印形状数据的标志和类型（ShapeData.m_shape_flags、ShapeData.mType）。
    - **位置**：`jni/include/Embree/PhysX.h:720`
- **052 `necessary（拼写）`**　读作 ˈnesəsəri　*拼写*　必要的　【27 次 / 7 个文件】
    - **用法**：**一 c 双 s**：ne-ces-sa-ry。记住口诀“one collar, two sleeves”。
    - **频次**：标识符 0 次 · 注释 27 次
    - **例句**：`// (this is because the visible order of columns have subtle but necessary effects on how they react to manual resizing).`
    - **中文翻译**：这是因为列的可见顺序对它们如何响应手动调整大小有细微但必要的影响。
    - **位置**：`jni/include/ImGui/imgui.h:2047`
- **053 `separate（拼写）`**　读作 ˈseprət　*拼写*　分开的　【27 次 / 10 个文件】
    - **用法**：**中间是 a 不是 e**：sep-a-rate。常见错拼 seperate。
    - **频次**：标识符 1 次 · 注释 26 次
    - **例句**：`// (Note that ImGui:: being a namespace, you can add extra ImGui:: functions in your own separate file. Please don't modify imgui source files!)`
    - **中文翻译**：注意 ImGui:: 是一个命名空间，你可以在自己的文件里额外添加 ImGui:: 函数；请不要修改 imgui 的源文件！
    - **位置**：`jni/include/ImGui/imgui.h:385`
- **054 `guarantee（拼写）`**　读作 ˌɡærənˈtiː　*拼写*　保证　【22 次 / 7 个文件】
    - **用法**：**有 u 有 a**：guar-an-tee。常见错拼 garantee。
    - **频次**：标识符 0 次 · 注释 22 次
    - **例句**：`// Using Dear ImGui via a shared library is not recommended, because of function call overhead and because we don't guarantee backward nor forward ABI compatibility.`
    - **中文翻译**：不推荐通过共享库来使用 Dear ImGui，因为存在函数调用开销，而且我们不保证向前或向后的 ABI 兼容性。
    - **位置**：`jni/include/ImGui/imconfig.h:24`
- **055 `acquire（拼写）`**　读作 əˈkwaɪə(r)　*拼写*　获取　【20 次 / 10 个文件】
    - **用法**：**有 c 没有 cq 之后的 u**：ac-quire。常见错拼 aquire。
    - **频次**：标识符 14 次 · 注释 6 次
    - **例句**：`// by making the caller acquire a temp buffer token, with either explicit or destructor release, e.g.`
    - **中文翻译**：做法是让调用方获取一个临时缓冲区令牌，再用显式或析构的方式释放，例如：
    - **位置**：`jni/src/ImGui/imgui.cpp:2400`
- **056 `similar（拼写）`**　读作 ˈsɪmələ(r)　*拼写*　相似的　【19 次 / 9 个文件】
    - **用法**：**中间是 i 不是 e**：sim-i-lar。常见错拼 similiar。
    - **频次**：标识符 0 次 · 注释 19 次
    - **例句**：`// - Only dispatch error if _EXPR: evaluate as assert (similar to an assert macro).`
    - **中文翻译**：只有在 _EXPR 为真时才上报错误：其求值方式类似断言（assert）宏。
    - **位置**：`jni/include/ImGui/imgui_internal.h:2113`
- **057 `accessible（拼写）`**　读作 əkˈsesəbl　*拼写*　可访问的　【16 次 / 6 个文件】
    - **用法**：**双 c 双 s**：ac-cess-ible。
    - **频次**：标识符 1 次 · 注释 15 次
    - **例句**：`// the array syntax is just a way to document the number of elements that are expected to be accessible. You can pass address of your first element out of a contiguous set, e.g. &myvector.x`
    - **中文翻译**：数组语法只是一种写法，用来说明预期可访问的元素个数。你可以传一段连续数据中第一个元素的地址，例如 &myvector.x。
    - **位置**：`jni/include/ImGui/imgui.h:671`
- **058 `temperature（拼写）`**　读作 ˈtemprətʃə(r)　*拼写*　温度　【16 次 / 1 个文件】
    - **用法**：**中间是 -pera-**：temp-er-ature。常见错拼 temperture。
    - **频次**：标识符 16 次 · 注释 0 次
    - **例句**：`#define ICON_FA_TEMPERATURE_HALF "\xef\x8b\x89" // U+f2c9`
    - **中文翻译**：定义 FontAwesome 图标「半温度」的转义编码（U+f2c9）。
    - **位置**：`jni/include/ImGui/gui_icon.h:365`
- **059 `receive（拼写）`**　读作 rɪˈsiːv　*拼写*　接收　【13 次 / 6 个文件】
    - **用法**：**铁律：i before e except after c**——所以是 rec-ei-ve，不是 rec-ie-ve。
    - **频次**：标识符 5 次 · 注释 8 次
    - **例句**：`// (The implicit fallback window is NOT automatically ended allowing it to always be able to receive commands without crashing)`
    - **中文翻译**：隐式的兜底窗口不会被自动结束，从而让它始终能接收指令而不崩溃。
    - **位置**：`jni/src/ImGui/imgui.cpp:8328`
- **060 `dependency（拼写）`**　读作 dɪˈpendənsi　*拼写*　依赖　【12 次 / 3 个文件】
    - **用法**：**结尾是 -ency 不是 -ancy**。动词是 depend。
    - **频次**：标识符 10 次 · 注释 2 次
    - **例句**：`// No specific ordering/dependency support, will see as needed`
    - **中文翻译**：不支持特定的排序/依赖，需要时用再看。
    - **位置**：`jni/src/ImGui/imgui.cpp:4634`
- **061 `successful（拼写）`**　读作 səkˈsesfl　*拼写*　成功的　【10 次 / 5 个文件】
    - **用法**：**双 c 双 s**：suc-cess-ful。同理 success、successfully。
    - **频次**：标识符 5 次 · 注释 5 次
    - **例句**：`// Check if Surface creation was successful`
    - **中文翻译**：检查 Surface 是否创建成功。
    - **位置**：`jni/include/ImGui/ANativeWindowCreator.h:794`
- **062 `maintenance（拼写）`**　读作 ˈmeɪntənəns　*拼写*　维护　【8 次 / 4 个文件】
    - **用法**：**有三段**：main-ten-ance。常见错拼 maintenence。
    - **频次**：标识符 0 次 · 注释 8 次
    - **例句**：`// 1) they facilitate the readability and maintenance of the multiple main.cpp examples files.`
    - **中文翻译**：1）它们能提升多个 main.cpp 示例文件的可读性和可维护性。
    - **位置**：`jni/include/ImGui/imgui_impl_vulkan.h:187`
- **063 `ImRect`**　读作 ˌaɪ-em-ˈrekt　*类型*　ImGui 的矩形　【7 次 / 4 个文件】
    - **用法**：`Min/Max` 两个点表示。
    - **频次**：标识符 0 次 · 注释 7 次
    - **例句**：`// - Helper: ImRect`
    - **中文翻译**：辅助类：ImRect（矩形）。
    - **位置**：`jni/include/ImGui/imgui_internal.h:362`
- **064 `parallel（拼写）`**　读作 ˈpærəlel　*拼写*　平行的　【6 次 / 4 个文件】
    - **用法**：**一个 r 两对 l**：par-al-lel。常见错拼 parrallel。
    - **频次**：标识符 1 次 · 注释 5 次
    - **例句**：`//#pragma omp parallel for`
    - **中文翻译**：编译指示：#pragma omp parallel for（OpenMP 并行 for 循环）。
    - **位置**：`jni/include/Embree/PhysX.h:802`
- **065 `pthread_`**　读作 ˌpiː-ˈθred　*前缀*　POSIX Threads 线程库　【6 次 / 2 个文件】
    - **用法**：`pthread_create`、`pthread_join`、`pthread_mutex_*`（第 82 章）。
    - **频次**：标识符 4 次 · 注释 2 次
    - **例句**：`for (pthread_t thread: touchThreads) {`
    - **中文翻译**：（代码）遍历 touchThreads 容器中的每个 pthread_t 线程。
    - **位置**：`jni/src/ImGui/TouchHelperA.cpp:487`
- **066 `independent（拼写）`**　读作 ˌɪndɪˈpendənt　*拼写*　独立的　【5 次 / 2 个文件】
    - **用法**：**结尾 -ent 不是 -ant**：independ-ent。
    - **频次**：标识符 0 次 · 注释 5 次
    - **例句**：`// - Use child windows to begin into a self-contained independent scrolling/clipping regions within a host window. Child windows can embed their own child.`
    - **中文翻译**：用子窗口来开辟一块自包含的、独立的滚动/裁剪区域（位于宿主窗口内）。子窗口还可以再嵌套自己的子窗口。
    - **位置**：`jni/include/ImGui/imgui.h:441`
- **067 `noticeable（拼写）`**　读作 ˈnəʊtɪsəbl　*拼写*　明显的　【5 次 / 3 个文件】
    - **用法**：**保留 e**：notice-able（对比：以辅音+y 结尾才变 i）。
    - **频次**：标识符 0 次 · 注释 5 次
    - **例句**：`// This should have no noticeable visual effect. When the window reappear however, expect new allocation/buffer growth/copy cost.`
    - **中文翻译**：这应当不会有明显的视觉影响。不过当窗口重新出现时，要预料到会有新的分配/缓冲区增长/拷贝开销。
    - **位置**：`jni/src/ImGui/imgui.cpp:4740`
- **068 `schedule（拼写）`**　读作 ˈʃedjuːl　*拼写*　调度、日程　【4 次 / 2 个文件】
    - **用法**：**sch 开头**：s-c-h-e-dule。常见错拼 shedule。
    - **频次**：标识符 0 次 · 注释 4 次
    - **例句**：`// RENAMED ImGuiKeyModFlags -> ImGuiModFlags in 1.88 (from April 2022). Exceptionally commented out ahead of obsolescence schedule to reduce confusion and because they were not meant to be used in the first place.`
    - **中文翻译**：已在 1.88（2022 年 4 月）把 ImGuiKeyModFlags 改名为 ImGuiModFlags。这里特意提前注释掉，是为了减少混淆，而且它们本来就不该被使用。
    - **位置**：`jni/include/ImGui/imgui.h:4266`
- **069 `dlopen / dlsym`**　读作 ˌdiː-el-ˈəʊpən / ˌdiː-el-ˈsɪm　*函数*　动态打开库 / 动态取符号　【3 次 / 3 个文件】
    - **用法**：不链接也能调用库函数（第 60 章）。**本项目跨版本适配的核心手段**。
    - **频次**：标识符 3 次 · 注释 0 次
    - **例句**：`static const Functionals &GetInstance(const SymbolMethod &symbolMethod = {.Open = dlopen, .Find = dlsym, .Close = dlclose})`
    - **中文翻译**：静态函数 GetInstance：获取单例（Functionals 的引用），默认用 dlopen/dlsym/dlclose 作为符号操作方法。
    - **位置**：`jni/include/ImGui/ANativeWindowCreator.h:490`
- **070 `existence（拼写）`**　读作 ɪɡˈzɪstəns　*拼写*　存在　【3 次 / 3 个文件】
    - **用法**：**没有 a**：exist-ence。常见错拼 existance。
    - **频次**：标识符 0 次 · 注释 3 次
    - **例句**：`//void ClearInputCharacters() { InputQueueCharacters.resize(0); } // [Obsoleted in 1.89.8] Clear the current frame text input buffer. Now included within ClearInputKeys(). Removed this as it is ambiguous/misleading and generally incorrect to use with the existence of a higher-level input queue.`
    - **中文翻译**：ClearInputCharacters()（清空当前帧的文本输入缓冲区）；已在 1.89.8 废弃，其功能并入 ClearInputKeys()，因与高层输入队列并存时语义含糊且通常不正确而移除。
    - **位置**：`jni/include/ImGui/imgui.h:2660`
- **071 `publicly（拼写）`**　读作 ˈpʌblɪkli　*拼写*　公开地　【3 次 / 3 个文件】
    - **用法**：**没有 al**：public-ly。常见错拼 publically。
    - **频次**：标识符 0 次 · 注释 3 次
    - **例句**：`// The following structure is defined publicly so you can declare one on`
    - **中文翻译**：下面这个结构体是公开定义的，所以你可以在……上声明一个
    - **位置**：`jni/include/ImGui/imstb_truetype.h:716`
- **072 `cl_ / CL_`**　读作 ˌsiː-ˈel　*前缀*　OpenCL 前缀　【2 次 / 1 个文件】
    - **用法**：GPU 通用计算 API。本项目未用，但要认得出。
    - **频次**：标识符 2 次 · 注释 0 次
    - **例句**：`sycl::buffer<cl_ulong> fptr_buf(1);`
    - **中文翻译**：声明一个 SYCL 缓冲区 fptr_buf，元素类型为 cl_ulong，容量为 1。
    - **位置**：`jni/include/Embree/include/rtcore_common.h:482`
- **073 `surprise（拼写）`**　读作 səˈpraɪz　*拼写*　惊讶　【2 次 / 1 个文件】
    - **用法**：**开头是 sur- 不是 sup-**：sur-prise。常见错拼 suprise。
    - **频次**：标识符 2 次 · 注释 0 次
    - **例句**：`#define ICON_FA_FACE_SURPRISE "\xef\x97\x82" // U+f5c2`
    - **中文翻译**：定义 FontAwesome 图标「惊讶表情」的转义编码（U+f5c2）。
    - **位置**：`jni/include/ImGui/gui_icon.h:880`
- **074 `committed（拼写）`**　读作 kəˈmɪtɪd　*拼写*　已提交的　【1 次 / 1 个文件】
    - **用法**：**双 t**：com-mit-ted。同理 committing。
    - **频次**：标识符 0 次 · 注释 1 次
    - **例句**：`// FIXME: For patterns of drag and drop across OS windows, we may need to rework/remove this test (first committed 311c0ca9 on 2015/02)`
    - **中文翻译**：待办：对于跨操作系统窗口的拖放模式，我们可能需要重做或移除这个测试（最初提交于 2015/02 的 311c0ca9）。
    - **位置**：`jni/src/ImGui/imgui.cpp:5539`
- **075 `interrupt（拼写）`**　读作 ˌɪntəˈrʌpt　*拼写*　中断　【1 次 / 1 个文件】
    - **用法**：**双 r**：in-ter-rupt。
    - **频次**：标识符 0 次 · 注释 1 次
    - **例句**：`// It makes sense in the vast majority of cases to never interrupt a drag and drop.`
    - **中文翻译**：在绝大多数情况下，绝不打断一次拖放操作是合理的。
    - **位置**：`jni/src/ImGui/imgui.cpp:8988`
- **076 `presence（拼写）`**　读作 ˈprezns　*拼写*　存在　【1 次 / 1 个文件】
    - **用法**：**-ence 结尾**：pres-ence。常见错拼 presance。
    - **频次**：标识符 0 次 · 注释 1 次
    - **例句**：`// then test for their presence in e.g. STB_TEXTEDIT_K_WORDLEFT. For example,`
    - **中文翻译**：然后在例如 STB_TEXTEDIT_K_WORDLEFT 中检测它们是否存在。例如，
    - **位置**：`jni/include/ImGui/imstb_textedit.h:198`
- **077 `proceed（拼写）`**　读作 prəˈsiːd　*拼写*　继续　【1 次 / 1 个文件】
    - **用法**：**pro-ceed（双 e）**。区分：precede（先于）、proceeds（收入）。
    - **频次**：标识符 0 次 · 注释 1 次
    - **例句**：`if (page_up_held == page_down_held && home_pressed == end_pressed) // Proceed if either (not both) are pressed, otherwise early out`
    - **中文翻译**：（代码）判断 page_up 与 page_down 是否同按、home 与 end 是否同按；只有「恰好其一按下」才继续，否则提前退出。
    - **位置**：`jni/src/ImGui/imgui.cpp:14578`
- **078 `relevant（拼写）`**　读作 ˈreləvənt　*拼写*　相关的　【1 次 / 1 个文件】
    - **用法**：**-ant 结尾**：relev-ant。常见错拼 relevent。
    - **频次**：标识符 0 次 · 注释 1 次
    - **例句**：`// Most of the relevant font logic is in imgui_draw.cpp.`
    - **中文翻译**：大部分与字体相关的逻辑都在 imgui_draw.cpp 里。
    - **位置**：`jni/src/ImGui/imgui.cpp:9059`
- **079 `AAsset / AAssetManager`**　读作 ˌeɪ-ˈæset　*类型*　安卓资源管理器　【本册按结构分组，不按词频】
    - **用法**：读 APK 内资源用；本项目读文件用普通 `open`。
- **080 `ALOOPER_`**　读作 ˌeɪ-ˈluːpə　*前缀*　安卓事件循环前缀　【本册按结构分组，不按词频】
    - **用法**：`ALOOPER_EVENT_INPUT` = 有输入事件可读（第 69 章）。
- **081 `ANativeWindow`**　读作 ˌeɪ-ˈneɪtɪv-ˈwɪndəʊ　*类型*　安卓原生窗口对象　【本册按结构分组，不按词频】
    - **用法**：`A` 前缀 = Android 原生 API（第 65 章）。
- **082 `Hungarian notation`**　读作 hʌŋˈɡeəriən nəʊˈteɪʃn　*命名风格*　匈牙利命名法（加类型前缀）　【本册按结构分组，不按词频】
    - **用法**：`szName`（sz = zero-terminated string）、`lpParam`（lp = long pointer）。Windows 老代码里成片。
- **083 `ICON_FA_`**　读作 ˈaɪkɒn ˌef-ˈeɪ　*宏前缀*　FontAwesome 图标常量　【本册按结构分组，不按词频】
    - **用法**：`ICON_FA_COG` = 齿轮图标。用宏把图标编码塞进字符串（第 63 章）。
- **084 `ImVec2 / ImVec4`**　读作 ˌaɪ-em-ˈvek-tuː　*类型*　ImGui 的 2/4 分量向量　【本册按结构分组，不按词频】
    - **用法**：与项目的 `Vector3A` 不是同一个类型，**混用要手写转换**。
- **085 `PascalCase`**　读作 ˈpæskl-keɪs　*命名风格*　大驼峰　【本册按结构分组，不按词频】
    - **用法**：`ReadProcessMemory`。类名与类型名的通行风格（也就是“帕斯卡命名”）。
- **086 `SCREAMING_SNAKE_CASE`**　读作 ˈskriːmɪŋ sneɪk keɪs　*命名风格*　全大写下划线　【本册按结构分组，不按词频】
    - **用法**：常量与宏：`MAX_BUFFER_SIZE`。
- **087 `STB_*_IMPLEMENTATION`**　读作 ɪmplɪmenˈteɪʃn　*宏*　单文件库的开启实现宏　【本册按结构分组，不按词频】
    - **用法**：**必须在一个 .cpp 里定义一次**，否则函数只有声明没有实现，链接报 `undefined reference`。
- **088 `VK_SUCCESS`**　读作 ˌviː-ˈkeɪ səkˈses　*枚举值*　Vulkan 调用成功返回码　【本册按结构分组，不按词频】
    - **用法**：**Vulkan 函数返回 0 才算成功**，与 POSIX 的“0 成功”一致（第 58 章）。
- **089 `__android_log_print`**　读作 ˌændrɔɪd lɒɡ prɪnt　*函数*　安卓原生日志输出　【本册按结构分组，不按词频】
    - **用法**：`LOGW/LOGI` 宏最终都走到它（第 38 章）。
- **090 `ato* 家族`**　读作 ˈætəʊ　*函数族*　ASCII to number（字符串转数值）　【本册按结构分组，不按词频】
    - **用法**：`atoi`→int、`atof`→double、`atol`→long。**出错时返回 0，无法区分“真是 0”和“转换失败”**。
- **091 `camelCase`**　读作 ˈkæml-keɪs　*命名风格*　小驼峰　【本册按结构分组，不按词频】
    - **用法**：`readProcessMemory`。Java/安卓与多数 C++ 项目的函数名风格。
- **092 `definite（拼写）`**　读作 ˈdefɪnət　*拼写*　确定的　【本册按结构分组，不按词频】
    - **用法**：**结尾是 -ite 不是 -ate**：def-i-nite。副词 definitely 更常被错拼。
- **093 `deg2rad / rad2deg`**　读作 ˌdeɡ-tuː-ˈræd　*函数名*　度转弧度 / 弧度转度　【本册按结构分组，不按词频】
    - **用法**：`rad = deg * π / 180`（第 47 章）。
- **094 `environment（拼写）`**　读作 ɪnˈvaɪrənmənt　*拼写*　环境　【本册按结构分组，不按词频】
    - **用法**：**有 n**：en-vi-ron-ment。常见错拼 enviroment。
- **095 `exceed（拼写）`**　读作 ɪkˈsiːd　*拼写*　超出　【本册按结构分组，不按词频】
    - **用法**：**双 e 双 e**：ex-ceed。
- **096 `kebab-case`**　读作 kɪˈbɑːb-keɪs　*命名风格*　连字符小写　【本册按结构分组，不按词频】
    - **用法**：`my-file.txt`。**C++ 标识符里不能用连字符**，只有文件名/URL 能用。
- **097 `mat2 / mat3 / mat4`**　读作 mæt-tuː / mæt-θriː / mæt-fɔː　*类型名*　2×2 / 3×3 / 4×4 矩阵　【本册按结构分组，不按词频】
    - **用法**：3D 图形用 `mat4`（第 46 章）：**4×4 才能同时容纳旋转+平移+缩放**。
- **098 `occasion（拼写）`**　读作 əˈkeɪʒn　*拼写*　场合　【本册按结构分组，不按词频】
    - **用法**：**双 c 单 s**：oc-ca-sion。
- **099 `occurred（拼写）`**　读作 əˈkɜːd　*拼写*　发生了　【本册按结构分组，不按词频】
    - **用法**：**双 c 双 r**：oc-cur-red。名词 occurrence 同理。
- **100 `perceive（拼写）`**　读作 pəˈsiːv　*拼写*　感知　【本册按结构分组，不按词频】
    - **用法**：**ei 在 c 之后是例外**——所以是 perc-ei-ve（与 receive 同规则）。


---

## 第 3 组

- **101 `permanent（拼写）`**　读作 ˈpɜːmənənt　*拼写*　永久的　【本册按结构分组，不按词频】
    - **用法**：**结尾 -ent**：perman-ent。常见错拼 permenant。
- **102 `possess（拼写）`**　读作 pəˈzes　*拼写*　拥有　【本册按结构分组，不按词频】
    - **用法**：**双 s 双 s**：pos-sess。
- **103 `precede（拼写）`**　读作 prɪˈsiːd　*拼写*　先于　【本册按结构分组，不按词频】
    - **用法**：**-cede 结尾**：pre-cede。同理 proceed（不同词）是 pro-ceed。
- **104 `privilege（拼写）`**　读作 ˈprɪvəlɪdʒ　*拼写*　权限　【本册按结构分组，不按词频】
    - **用法**：**没有 d**：priv-i-lege。常见错拼 priviledge。
- **105 `resistance（拼写）`**　读作 rɪˈzɪstəns　*拼写*　阻力　【本册按结构分组，不按词频】
    - **用法**：**-ance 结尾**：resist-ance。
- **106 `rtcOccluded1`**　读作 ˌɑː-tiː-ˈsiː əˈkluːdɪd wʌn　*函数*　遮挡查询（只要有没有，不要最近是谁）　【本册按结构分组，不按词频】
    - **用法**：比 `rtcIntersect1` **快 2–3 倍**，因为它找到第一个遮挡就能停（附录 U 优化项）。
- **107 `sincerely（拼写）`**　读作 sɪnˈsɪəli　*拼写*　真诚地　【本册按结构分组，不按词频】
    - **用法**：**-ely 结尾**：sincere-ly。
- **108 `snake_case`**　读作 sneɪk-keɪs　*命名风格*　下划线小写　【本册按结构分组，不按词频】
    - **用法**：`read_process_memory`。C/C++ 系统编程与 Linux 内核的主流风格。
- **109 `std::`**　读作 ˌes-tiː-ˈdiː　*命名空间*　C++ 标准库命名空间　【本册按结构分组，不按词频】
    - **用法**：看到 `std::` 就知道是标准库，不是项目代码。
- **110 `strength（拼写）`**　读作 streŋθ　*拼写*　强度　【本册按结构分组，不按词频】
    - **用法**：**有 g**：stre-n-g-th。常见错拼 strenght。
- **111 `thorough（拼写）`**　读作 ˈθʌrə　*拼写*　彻底的　【本册按结构分组，不按词频】
    - **用法**：**别与 through（穿过）混**：thorough 更长，多了 o 和 o。
- **112 `vec2 / vec3 / vec4`**　读作 vek-tuː / vek-θriː / vek-fɔː　*类型名*　2/3/4 分量向量　【本册按结构分组，不按词频】
    - **用法**：数学库的统一命名（第 53 章）。

