---
tags: [教程, 词汇, 词频]
aliases: [W2, vocab L2]
---


# W2 · 第 2 级：动作词（动词）（176 词）

> [!abstract] 本册在整套词汇表里的位置
> 看函数名就知道它干什么：全部为项目源码里真实出现的动词
>
> **本册排序 = 项目源码里的真实出现次数**（从多到少）。
> 频次统计口径：`jni/` 下 97 个源文件、35 万行代码。
> 每个词条都附**这个项目里的真实例句**（含文件名与行号）。
> 音标采用**英式 IPA（DJ）**；与附录 K 的美式 IPA 差异见 [[词频词汇总览|W0]]。


## 本册词条

- [第 1 组（1–50）](#第-1-组)
- [第 2 组（51–100）](#第-2-组)
- [第 3 组（101–150）](#第-3-组)
- [第 4 组（151–176）](#第-4-组)


---

## 第 1 组

- **001 `get`**　/ɡet/　*v.*　获取、取得　【2495 次 / 59 个文件】
    - **用法**：最高频。`GetSize()` = 取大小。命名规范里 get 表示“只读不改”。
    - **频次**：标识符 2330 次 · 注释 165 次
    - **例句**：`//! Get transposed matrix`
    - **中文翻译**：获取转置矩阵。
    - **位置**：`jni/include/Embree/foundation/PxMat33.h:189`
- **002 `draw`**　/drɔː/　*v.*　绘制；抽取　【2316 次 / 26 个文件】
    - **用法**：**本项目排名第 22 的词（2316 次）**。`DrawList` = 绘制指令列表（第 61 章）；`draw call` = 一次绘制调用。
    - **频次**：标识符 2044 次 · 注释 272 次
    - **例句**：`//3d转2d 类型 (由 draw_Gui.cpp 使用, 定义于此供各文件共享)`
    - **中文翻译**：3D 转 2D 的类型（由 draw_Gui.cpp 使用，定义在此供各文件共享）。
    - **位置**：`jni/include/Android_draw/draw.h:15`
- **003 `set`**　/set/　*v./n.*　设置；集合　【2068 次 / 43 个文件】
    - **用法**：`SetOffset()` = 设偏移。与 get 成对。也是数据结构“集合”。
    - **频次**：标识符 1425 次 · 注释 643 次
    - **例句**：`// set vertices buffer`
    - **中文翻译**：设置顶点缓冲区。
    - **位置**：`jni/include/Embree/PhysX.h:1543`
- **004 `use`**　/juːz/　*v.*　使用　【1335 次 / 50 个文件】
    - **用法**：`use it as a key` = 把它当键用。注释里几乎必现。
    - **频次**：标识符 163 次 · 注释 1172 次
    - **例句**：`//rtcSetDeviceProperty(device, RTC_DEVICE_PROPERTY_TASKING_SYSTEM, 1); // USE TBB`
    - **中文翻译**：设置设备属性（任务系统）为 1，即使用 TBB（Intel 线程构建块）。
    - **位置**：`jni/include/Embree/PhysX.h:1472`
- **005 `begin`**　/bɪˈɡɪn/　*v.*　开始　【879 次 / 29 个文件】
    - **用法**：`begin()` = 取首迭代器（与 `front()` 区别：begin 是迭代器，front 是元素引用）。
    - **频次**：标识符 694 次 · 注释 185 次
    - **例句**：`// - Begin() = push window to the stack and start appending to it. End() = pop window from the stack.`
    - **中文翻译**：Begin() 是把窗口压入栈并开始往里追加内容；End() 是把窗口从栈中弹出。
    - **位置**：`jni/include/ImGui/imgui.h:426`
- **006 `format`**　/ˈfɔːmæt/　*v./n.*　格式化；格式　【818 次 / 24 个文件】
    - **用法**：`Format string` = 格式字符串；`snprintf` 家族都在格式化。
    - **频次**：标识符 655 次 · 注释 163 次
    - **例句**：`// Data, see above for format!`
    - **中文翻译**：数据（格式见上文！）。
    - **位置**：`jni/include/Embree/foundation/PxMat33.h:351`
- **007 `add`**　/æd/　*v.*　添加　【810 次 / 24 个文件】
    - **用法**：`AddVertex()` = 加顶点。与 remove 相对。
    - **频次**：标识符 615 次 · 注释 195 次
    - **例句**：`// add geometry or enable`
    - **中文翻译**：添加几何体，或启用……
    - **位置**：`jni/include/Embree/PhysX.h:1520`
- **008 `move`**　/muːv/　*v.*　移动；移动语义　【742 次 / 21 个文件】
    - **用法**：`std::move` = 转移所有权（第 14 章）。也是几何上的“位移”。
    - **频次**：标识符 569 次 · 注释 173 次
    - **例句**：`//// PT: extended pruner structure. We might want to move the additional data to the pruner itself later.`
    - **中文翻译**：PT（作者缩写）：扩展后的裁剪（pruner）结构。也许以后我们会把附加数据移到 pruner 自身里去。
    - **位置**：`jni/include/Embree/PhysX.h:645`
- **009 `create`**　/kriˈeɪt/　*v.*　创建　【729 次 / 28 个文件】
    - **用法**：`CreateWindow()` = 创建窗口。与 destroy 成对。
    - **频次**：标识符 577 次 · 注释 152 次
    - **例句**：`// create new geometry`
    - **中文翻译**：创建新的几何体。
    - **位置**：`jni/include/Embree/PhysX.h:1537`
- **010 `log`**　/lɒɡ/　*v./n.*　记录日志；对数　【718 次 / 16 个文件】
    - **用法**：`LOGW()` = 打警告日志（第 38 章）。数学里 `log` 是对数函数。
    - **频次**：标识符 642 次 · 注释 76 次
    - **例句**：`//! \brief An unrecoverable error, execution should be halted and log output flushed`
    - **中文翻译**：简介：一个不可恢复的错误，应当停止执行并刷新日志输出。
    - **位置**：`jni/include/Embree/foundation/PxErrors.h:77`
- **011 `select`**　/sɪˈlekt/　*v.*　选择　【694 次 / 17 个文件】
    - **用法**：`Select()` = 下拉框控件。系统编程里 `select()` 是多路复用系统调用。
    - **频次**：标识符 628 次 · 注释 66 次
    - **例句**：`//! \brief platform-specific select float`
    - **中文翻译**：简介：平台相关的 float 选择（select）函数。
    - **位置**：`jni/include/Embree/foundation/unix/PxUnixIntrinsics.h:61`
- **012 `scroll`**　/skrəʊl/　*v./n.*　滚动　【680 次 / 7 个文件】
    - **用法**：`ScrollY` = 纵向滚动量。ImGui 的滚动是自动的，不用手写。
    - **频次**：标识符 585 次 · 注释 95 次
    - **例句**：`// - Any change of Scroll will be applied at the beginning of next frame in the first call to Begin().`
    - **中文翻译**：对滚动位置的任何改动，都会在下一帧第一次调用 Begin() 时生效。
    - **位置**：`jni/include/ImGui/imgui.h:494`
- **013 `render`**　/ˈrendə(r)/　*v.*　渲染、绘制　【677 次 / 19 个文件】
    - **用法**：`RenderFrame()` = 渲染一帧。图形学核心动词。
    - **频次**：标识符 476 次 · 注释 201 次
    - **例句**：`//---- Use FreeType + plutosvg or lunasvg to render OpenType SVG fonts (SVGinOT)`
    - **中文翻译**：用 FreeType + plutosvg 或 lunasvg 来渲染 OpenType SVG 字体（SVGinOT）。
    - **位置**：`jni/include/ImGui/imconfig.h:94`
- **014 `request`**　/rɪˈkwest/　*v./n.*　请求　【661 次 / 13 个文件】
    - **用法**：`Request` 结构体 = 跨进程请求包（第 79 章）。与 response 成对。
    - **频次**：标识符 540 次 · 注释 121 次
    - **例句**：`// - 3. Optionally call TableSetupScrollFreeze() to request scroll freezing of columns/rows.`
    - **中文翻译**：第 3 步（可选）：调用 TableSetupScrollFreeze() 来冻结某些列/行的滚动。
    - **位置**：`jni/include/ImGui/imgui.h:892`
- **015 `focus`**　/ˈfəʊkəs/　*v./n.*　聚焦、获得焦点　【640 次 / 5 个文件】
    - **用法**：`focused window` = 有焦点的窗口。ImGui 用 `SetItemDefaultFocus()`。
    - **频次**：标识符 494 次 · 注释 146 次
    - **例句**：`// - Tooltips are windows following the mouse. They do not take focus away.`
    - **中文翻译**：工具提示（tooltip）是跟随鼠标的窗口，它们不会抢走焦点。
    - **位置**：`jni/include/ImGui/imgui.h:822`
- **016 `clear`**　/klɪə(r)/　*v.*　清空、清除　【638 次 / 20 个文件】
    - **用法**：`Clear()` = 清空容器或清屏。注意：清空 ≠ 释放内存。
    - **频次**：标识符 471 次 · 注释 167 次
    - **例句**：`// If this is the last Surface, clear all mirror surfaces`
    - **中文翻译**：如果这是最后一个 Surface，就清空所有镜像 surface。
    - **位置**：`jni/include/ImGui/ANativeWindowCreator.h:1234`
- **017 `open`**　/ˈəʊpən/　*v.*　打开　【617 次 / 16 个文件】
    - **用法**：`open()` 系统调用返回 fd（第 22 章）。与 close 成对。
    - **频次**：标识符 478 次 · 注释 139 次
    - **例句**：`// - Passing 'bool* p_open != NULL' shows a window-closing widget in the upper-right corner of the window,`
    - **中文翻译**：传入 'bool* p_open != NULL' 会在窗口右上角显示一个「关闭窗口」的小控件，
    - **位置**：`jni/include/ImGui/imgui.h:427`
- **018 `start`**　/stɑːt/　*v.*　开始、启动　【605 次 / 26 个文件】
    - **用法**：`StartThread()` = 启动线程。
    - **频次**：标识符 507 次 · 注释 98 次
    - **例句**：`ray.tnear = 0.0f; // start distance`
    - **中文翻译**：把光线（ray）的起始距离 tnear 设为 0.0f。
    - **位置**：`jni/include/Embree/PhysX.h:1634`
- **019 `resize`**　/ˌriːˈsaɪz/　*v.*　调整大小　【593 次 / 17 个文件】
    - **用法**：`Resize()` = 改变窗口/缓冲尺寸。**缓冲 resize 后旧指针会失效**。
    - **频次**：标识符 491 次 · 注释 102 次
    - **例句**：`// Fitting/Resize policy`
    - **中文翻译**：适配 / 缩放策略。
    - **位置**：`jni/include/ImGui/imgui.h:1422`
- **020 `drag`**　/dræɡ/　*v./n.*　拖拽　【557 次 / 7 个文件】
    - **用法**：`DragFloat` = 可拖动的数值控件。与 click 的区别：drag 会持续发事件。
    - **频次**：标识符 454 次 · 注释 103 次
    - **例句**：`// Widgets: Drag Sliders`
    - **中文翻译**：控件：拖拽滑块（Drag Sliders）。
    - **位置**：`jni/include/ImGui/imgui.h:668`
- **021 `call`**　/kɔːl/　*v./n.*　调用；调用（名词）　【515 次 / 22 个文件】
    - **用法**：`function call` = 函数调用；`syscall` = 系统调用。
    - **频次**：标识符 57 次 · 注释 458 次
    - **例句**：`This version is safe to call for empty bounds.`
    - **中文翻译**：这个版本（函数）对空包围盒调用也是安全的。
    - **位置**：`jni/include/Embree/foundation/PxBounds3.h:107`
- **022 `disable`**　/dɪsˈeɪbl/　*v.*　禁用　【505 次 / 17 个文件】
    - **用法**：`DisableFlag()` = 清掉标志位。与 enable 成对。
    - **频次**：标识符 237 次 · 注释 268 次
    - **例句**：`// remove geometry use disable`
    - **中文翻译**：移除几何体，用 disable（禁用）。
    - **位置**：`jni/include/Embree/PhysX.h:1494`
- **023 `need`**　/niːd/　*v.*　需要　【485 次 / 18 个文件】
    - **用法**：`we need to` = 我们需要。注释里引出下一步动作。
    - **频次**：标识符 49 次 · 注释 436 次
    - **例句**：`// This assert works on win32/win64, but may need further specialization on other platforms.`
    - **中文翻译**：这个断言在 win32/win64 上有效，但在其他平台上可能需要进一步特化。
    - **位置**：`jni/include/Embree/foundation/PxPreprocessor.h:477`
- **024 `sort`**　/sɔːt/　*v.*　排序　【485 次 / 10 个文件】
    - **用法**：`std::sort` = 快排封装。`sort by address` = 按地址排序。
    - **频次**：标识符 403 次 · 注释 82 次
    - **例句**：`// - TreeNode() is technically supported but... using this correctly is more complicated. You need some sort of linear/random access to your tree,`
    - **中文翻译**：TreeNode() 技术上仍受支持，但……要正确使用它更复杂，你需要某种对树的线性/随机访问能力，
    - **位置**：`jni/include/ImGui/imgui.h:773`
- **025 `find`**　/faɪnd/　*v.*　查找　【472 次 / 22 个文件】
    - **用法**：`FindModule()` = 查找模块基址（第 76 章）。
    - **频次**：标识符 370 次 · 注释 102 次
    - **例句**：`// find combination of base vectors that produces max. distance for each component = sum of abs()`
    - **中文翻译**：寻找基向量的某种组合，使得每个分量上的距离（= 绝对值之和）最大。
    - **位置**：`jni/include/Embree/foundation/PxBounds3.h:297`
- **026 `write`**　/raɪt/　*v.*　写、写入　【472 次 / 23 个文件】
    - **用法**：`write()` 系统调用；`WriteProcessMemory` 一类。与 read 成对。
    - **频次**：标识符 414 次 · 注释 58 次
    - **例句**：`// (this block used to be written by backend, since 1.87 it is best to NOT write to those directly, call the AddXXX functions above instead)`
    - **中文翻译**：这个数据块过去由后端写入；自 1.87 起最好不要直接写它，而是调用上面的 AddXXX 函数。
    - **位置**：`jni/include/ImGui/imgui.h:2603`
- **027 `read`**　/riːd/　*v.*　读、读取　【470 次 / 28 个文件】
    - **用法**：`ReadProcessMemory` / `process_vm_readv`（第 74 章）。本项目核心动作。
    - **频次**：标识符 288 次 · 注释 182 次
    - **例句**：`The user needs to supply a PxInputStream implementation to a number of methods to allow the SDK to read data.`
    - **中文翻译**：用户需要为若干方法提供 PxInputStream 的实现，以便 SDK 读取数据。
    - **位置**：`jni/include/Embree/foundation/PxIO.h:47`
- **028 `push`**　/pʊʃ/　*v.*　压入、推送　【456 次 / 22 个文件】
    - **用法**：`push_back()` = 尾部追加。与 pop 成对。
    - **频次**：标识符 421 次 · 注释 35 次
    - **例句**：`// - Begin() = push window to the stack and start appending to it. End() = pop window from the stack.`
    - **中文翻译**：Begin() 是把窗口压入栈并开始往里追加内容；End() 是把窗口从栈中弹出。
    - **位置**：`jni/include/ImGui/imgui.h:426`
- **029 `want`**　/wɒnt/　*v.*　想要　【425 次 / 16 个文件】
    - **用法**：`if you want to` = 如果你想。**注释语气词，说明这是可选项**。
    - **频次**：标识符 189 次 · 注释 236 次
    - **例句**：`//// PT: extended pruner structure. We might want to move the additional data to the pruner itself later.`
    - **中文翻译**：PT（作者缩写）：扩展后的裁剪（pruner）结构。也许以后我们会把附加数据移到 pruner 自身里去。
    - **位置**：`jni/include/Embree/PhysX.h:645`
- **030 `update`**　/ˌʌpˈdeɪt/　*v.*　更新　【423 次 / 20 个文件】
    - **用法**：`Update()` = 每帧刷新。与 create 相对：改已有的，不是造新的。
    - **频次**：标识符 265 次 · 注释 158 次
    - **例句**：`// space by dividing with the similarity scale, for example, to update the`
    - **中文翻译**：空间，即除以相似度缩放因子，例如用来更新……
    - **位置**：`jni/include/Embree/include/rtcore_common.h:466`
- **031 `edit`**　/ˈedɪt/　*v./n.*　编辑　【413 次 / 9 个文件】
    - **用法**：`InputText` 用于输入，`ColorEdit` 用于调色。
    - **频次**：标识符 385 次 · 注释 28 次
    - **例句**：`// A) You may edit imconfig.h (and not overwrite it when updating Dear ImGui, or maintain a patch/rebased branch with your modifications to it)`
    - **中文翻译**：A）你可以编辑 imconfig.h（更新 Dear ImGui 时不要覆盖它，或者把你的修改维护在一个打了补丁/变基的分支上）
    - **位置**：`jni/include/ImGui/imconfig.h:6`
- **032 `cast`**　/kɑːst/　*v.*　类型转换　【390 次 / 31 个文件】
    - **用法**：`static_cast<int>` = 静态转换。注释里 `cast to pointer` = 转成指针。
    - **频次**：标识符 355 次 · 注释 35 次
    - **例句**：`//---- Define constructor and implicit cast operators to convert back<>forth between your math types and ImVec2/ImVec4.`
    - **中文翻译**：自定义：定义构造函数和隐式转换运算符，在你的数学类型与 ImVec2/ImVec4 之间互相转换。
    - **位置**：`jni/include/ImGui/imconfig.h:106`
- **033 `drop`**　/drɒp/　*v.*　丢弃、落下　【387 次 / 7 个文件】
    - **用法**：`drop 4th elem` = 省掉第 4 个元素；`drop the packet` = 丢包。
    - **频次**：标识符 328 次 · 注释 59 次
    - **例句**：`// Drag and Drop`
    - **中文翻译**：拖放（Drag and Drop）。
    - **位置**：`jni/include/ImGui/imgui.h:973`
- **034 `shift`**　/ʃɪft/　*v./n.*　移位；按住 Shift　【383 次 / 11 个文件】
    - **用法**：**位运算**：`1 << 3` 就是 shift left 3 位（第 24 章）。也是键盘修饰键。
    - **频次**：标识符 287 次 · 注释 96 次
    - **例句**：`// - This enables standard multi-selection/range-selection idioms (Ctrl+Mouse/Keyboard, Shift+Mouse/Keyboard, etc.) in a way that also allow a clipper to be used.`
    - **中文翻译**：这让标准的「多选/区间选择」习惯用法（Ctrl+鼠标/键盘、Shift+鼠标/键盘等）得以启用，同时仍能使用裁剪器（clipper）。
    - **位置**：`jni/include/ImGui/imgui.h:770`
- **035 `allow`**　/əˈlaʊ/　*v.*　允许　【374 次 / 17 个文件】
    - **用法**：`allows null` = 允许为空。`allow` 出现说明有约束放宽。
    - **频次**：标识符 181 次 · 注释 193 次
    - **例句**：`The user needs to supply a PxInputStream implementation to a number of methods to allow the SDK to read data.`
    - **中文翻译**：用户需要为若干方法提供 PxInputStream 的实现，以便 SDK 读取数据。
    - **位置**：`jni/include/Embree/foundation/PxIO.h:47`
- **036 `init`**　/ɪˈnɪt/　*v.*　初始化　【373 次 / 28 个文件】
    - **用法**：`initialize` 的缩写。`InitDriver()` = 初始化驱动（第 80 章）。
    - **频次**：标识符 308 次 · 注释 65 次
    - **例句**：`// init result var`
    - **中文翻译**：初始化结果变量。
    - **位置**：`jni/include/Embree/PhysX.h:1639`
- **037 `load`**　/ləʊd/　*v.*　加载　【371 次 / 25 个文件】
    - **用法**：`dlopen` 是动态加载（第 60 章）；`LoadFont()` = 加载字体。
    - **频次**：标识符 285 次 · 注释 86 次
    - **例句**：`// if current ptr not contains or current ptr is height field, load it data!`
    - **中文翻译**：如果当前指针不含目标，或者当前指针是一个高度场，就加载它的数据！
    - **位置**：`jni/include/Embree/PhysX.h:1263`
- **038 `return`**　/rɪˈtɜːn/　*v./n.*　返回；返回值　【350 次 / 28 个文件】
    - **用法**：`return 0` = 返回 0。注释里 `returns true on success` = 成功时返回真。
    - **频次**：标识符 0 次 · 注释 350 次
    - **例句**：`// return false;`
    - **中文翻译**：返回 false；
    - **位置**：`jni/include/Embree/PhysX.h:291`
- **039 `destroy`**　/dɪˈstrɔɪ/　*v.*　销毁　【345 次 / 14 个文件】
    - **用法**：`Destroy()` = 释放并注销（Vulkan 对象、窗口）。与 create 成对。
    - **频次**：标识符 315 次 · 注释 30 次
    - **例句**：`// Destroy main Surface`
    - **中文翻译**：销毁主 Surface。
    - **位置**：`jni/include/ImGui/ANativeWindowCreator.h:1227`
- **040 `handle`**　/ˈhændl/　*v./n.*　处理；句柄　【335 次 / 22 个文件】
    - **用法**：动词“处理”，名词“句柄”。`HWND` 里的 H 就是 handle。
    - **频次**：标识符 192 次 · 注释 143 次
    - **例句**：`/* Base class to handle assert failures */`
    - **中文翻译**：用于处理断言失败情况的基类。
    - **位置**：`jni/include/Embree/foundation/PxAssert.h:43`
- **041 `skip`**　/skɪp/　*v.*　跳过　【335 次 / 11 个文件】
    - **用法**：`skip invalid entries` = 跳过非法条目。过滤循环里常见。
    - **频次**：标识符 270 次 · 注释 65 次
    - **例句**：`// - Recovery after running an exception handler or any error processing which may skip code after an error has been detected.`
    - **中文翻译**：在运行了异常处理或任何可能跳过「错误检测之后代码」的错误处理之后进行恢复。
    - **位置**：`jni/include/ImGui/imgui.h:2500`
- **042 `check`**　/tʃek/　*v.*　检查　【330 次 / 23 个文件】
    - **用法**：`CheckValid()` = 检查合法性。多数 bug 都缺这一步。
    - **频次**：标识符 209 次 · 注释 121 次
    - **例句**：`// check that exactly one of NDEBUG and _DEBUG is defined`
    - **中文翻译**：检查 NDEBUG 和 _DEBUG 中恰好只定义了其中一个。
    - **位置**：`jni/include/Embree/foundation/PxPreprocessor.h:452`
- **043 `test`**　/test/　*v./n.*　测试　【327 次 / 15 个文件】
    - **用法**：`unit test` = 单元测试；`test if null` = 判空。
    - **频次**：标识符 161 次 · 注释 166 次
    - **例句**：`//---- Enable Test Engine / Automation features.`
    - **中文翻译**：启用测试引擎 / 自动化功能。
    - **位置**：`jni/include/ImGui/imconfig.h:58`
- **044 `free`**　/friː/　*v./adj.*　释放；空闲的　【326 次 / 22 个文件】
    - **用法**：`free()` = 释放堆内存。与 malloc 成对。形容词义是“空闲”。
    - **频次**：标识符 266 次 · 注释 60 次
    - **例句**：`//#define IMGUI_DISABLE_DEFAULT_ALLOCATORS // Don't implement default allocators calling malloc()/free() to avoid linking with them. You will need to call ImGui::SetAllocatorFunctions().`
    - **中文翻译**：宏：禁用默认分配器（不实现调用 malloc()/free() 的默认分配器，以避免链接它们）。你需要改用 ImGui::SetAllocatorFunctions()。
    - **位置**：`jni/include/ImGui/imconfig.h:52`
- **045 `query`**　/ˈkwɪəri/　*v./n.*　查询　【326 次 / 15 个文件】
    - **用法**：`rtcIntersect` 是 Embree 的查询接口（第 97 章）。
    - **频次**：标识符 237 次 · 注释 89 次
    - **例句**：`/* Ray query flags */`
    - **中文翻译**：光线查询标志位。
    - **位置**：`jni/include/Embree/include/rtcore_common.h:300`
- **046 `support`**　/səˈpɔːt/　*v.*　支持　【303 次 / 22 个文件】
    - **用法**：`not supported` = 不支持。读特性检测代码时的关键词。
    - **频次**：标识符 59 次 · 注释 244 次
    - **例句**：`// Apparently the apple-clang-llvm compiler doesn't support malign-double.`
    - **中文翻译**：看来 apple-clang-llvm 编译器不支持 malign-double（强制双精度对齐）。
    - **位置**：`jni/include/Embree/foundation/PxPreprocessor.h:479`
- **047 `pop`**　/pɒp/　*v.*　弹出、取出　【293 次 / 13 个文件】
    - **用法**：`pop_back()` = 去尾。与 push 成对。
    - **频次**：标识符 286 次 · 注释 7 次
    - **例句**：`// - Begin() = push window to the stack and start appending to it. End() = pop window from the stack.`
    - **中文翻译**：Begin() 是把窗口压入栈并开始往里追加内容；End() 是把窗口从栈中弹出。
    - **位置**：`jni/include/ImGui/imgui.h:426`
- **048 `copy`**　/ˈkɒpi/　*v./n.*　复制　【290 次 / 30 个文件】
    - **用法**：`memcpy` = 内存复制。注意深拷贝 vs 浅拷贝（第 14 章）。
    - **频次**：标识符 150 次 · 注释 140 次
    - **例句**：`// copy indices buffer`
    - **中文翻译**：复制索引缓冲区。
    - **位置**：`jni/include/Embree/PhysX.h:1590`
- **049 `build`**　/bɪld/　*v./n.*　构建；构建产物　【274 次 / 22 个文件】
    - **用法**：`build the BVH` = 建 BVH 树（第 96 章）。也是“编译”的口语说法。
    - **频次**：标识符 183 次 · 注释 91 次
    - **例句**：`/* Input build primitives for the builder */`
    - **中文翻译**：提供给构建器的输入图元。
    - **位置**：`jni/include/Embree/include/rtcore_builder.h:13`
- **050 `close`**　/kləʊz/　*v.*　关闭　【273 次 / 17 个文件】
    - **用法**：`close(fd)` = 关闭文件描述符。与 open 成对。
    - **频次**：标识符 171 次 · 注释 102 次
    - **例句**：`\brief returns true if finite and magnitude is close to unit`
    - **中文翻译**：（Doxygen 简述）若值为有限数且模长接近单位值，则返回 true。
    - **位置**：`jni/include/Embree/foundation/PxQuat.h:130`


---

## 第 2 组

- **051 `clamp`**　/klæmp/　*v.*　钳制、限制到区间　【270 次 / 12 个文件】
    - **用法**：`Clamp(x, 0, 1)` = 把 x 限制在 [0,1]。数学库必备函数（第 53 章）。
    - **频次**：标识符 232 次 · 注释 38 次
    - **例句**：`// - Ctrl+Click on any drag box to turn them into an input box. Manually input values aren't clamped by default and can go off-bounds. Use ImGuiSliderFlags_AlwaysClamp to always clamp.`
    - **中文翻译**：在任意拖拽框上按住 Ctrl 点击可把它变成输入框。手动输入的值默认不限制范围，可能超出边界；用 ImGuiSliderFlags_AlwaysClamp 可始终夹紧。
    - **位置**：`jni/include/ImGui/imgui.h:669`
- **052 `make`**　/meɪk/　*v.*　制作、使　【259 次 / 22 个文件】
    - **用法**：`MakeBox()` = 造一个盒子网格；`make sure` = 确保。
    - **频次**：标识符 59 次 · 注释 200 次
    - **例句**：`// make sure PX_CHECKED is defined in all _DEBUG configurations as well`
    - **中文翻译**：确保在所有 _DEBUG 配置下也定义了 PX_CHECKED。
    - **位置**：`jni/include/Embree/foundation/PxPreprocessor.h:457`
- **053 `enable`**　/ɪˈneɪbl/　*v.*　启用　【247 次 / 23 个文件】
    - **用法**：`EnableAlpha()` = 开启透明。与 disable 成对。
    - **频次**：标识符 126 次 · 注释 121 次
    - **例句**：`// add geometry or enable`
    - **中文翻译**：添加几何体，或启用……
    - **位置**：`jni/include/Embree/PhysX.h:1520`
- **054 `apply`**　/əˈplaɪ/　*v.*　应用、施加　【244 次 / 10 个文件】
    - **用法**：`apply transform` = 施加变换（第 48 章）。
    - **频次**：标识符 125 次 · 注释 119 次
    - **例句**：`// Transaction Apply method - version specific selection`
    - **中文翻译**：事务应用（Apply）方法——按版本分别选择。
    - **位置**：`jni/include/ImGui/ANativeWindowCreator.h:432`
- **055 `lock`**　/lɒk/　*v./n.*　加锁；锁　【239 次 / 12 个文件】
    - **用法**：`std::lock_guard` = RAII 锁（第 82 章）。本项目自写 sinkhole 锁。
    - **频次**：标识符 165 次 · 注释 74 次
    - **例句**：`//std::lock_guard<std::mutex> lock(mutex_);`
    - **中文翻译**：std::lock_guard<std::mutex> lock(mutex_);（用 RAII 锁保护 mutex_，离开作用域自动解锁）。
    - **位置**：`jni/include/Embree/PhysX.h:1692`
- **056 `map`**　/mæp/　*v./n.*　映射；映射表　【231 次 / 17 个文件】
    - **用法**：`/proc/pid/maps` = 内存映射表（第 76 章）。`std::map` 是字典。
    - **频次**：标识符 212 次 · 注释 19 次
    - **例句**：`// Legacy: before 1.87, we required backend to fill io.KeyMap[] (imgui->native map) during initialization and io.KeysDown[] (native indices) every frame.`
    - **中文翻译**：旧式做法：在 1.87 之前，我们要求后端在初始化时填充 io.KeyMap[]（imgui→原生映射），并在每帧填充 io.KeysDown[]（原生索引）。
    - **位置**：`jni/include/ImGui/imgui.h:2641`
- **057 `highlight`**　/ˈhaɪlaɪt/　*v.*　高亮　【226 次 / 7 个文件】
    - **用法**：`highlight the target` = 把目标画成醒目颜色。
    - **频次**：标识符 174 次 · 注释 52 次
    - **例句**：`//---- Debug Tools: Enable highlight ID conflicts _before_ hovering items. When io.ConfigDebugHighlightIdConflicts is set.`
    - **中文翻译**：调试工具：在悬停条目「之前」就高亮显示 ID 冲突。需在 io.ConfigDebugHighlightIdConflicts 开启时生效。
    - **位置**：`jni/include/ImGui/imconfig.h:137`
- **058 `wrap`**　/ræp/　*v.*　包装、封装　【226 次 / 9 个文件】
    - **用法**：`wrapper` = 封装层。`wrap around` = 回绕（索引越界后从 0 继续）。
    - **频次**：标识符 189 次 · 注释 37 次
    - **例句**：`// 'wrap_width' enable automatic word-wrapping across multiple lines to fit into given width. 0.0f to disable.`
    - **中文翻译**：'wrap_width'（自动换行宽度）用于启用跨多行的自动单词换行以适配给定宽度；设为 0.0f 则关闭。
    - **位置**：`jni/include/ImGui/imgui.h:3966`
- **059 `transform`**　/trænsˈfɔːm/　*v./n.*　变换；坐标变换　【224 次 / 21 个文件】
    - **用法**：`TransformPoint(m, p)` = 用矩阵变换点（第 48 章）。
    - **频次**：标识符 180 次 · 注释 44 次
    - **例句**：`// column0 = transform(other.column0) etc`
    - **中文翻译**：column0 = 对 other.column0 做变换，其余列同理。
    - **位置**：`jni/include/Embree/PhysX.h:231`
- **060 `avoid`**　/əˈvɔɪd/　*v.*　避免　【214 次 / 16 个文件】
    - **用法**：`avoid a copy` = 避免一次拷贝。性能注释的常见理由。
    - **频次**：标识符 28 次 · 注释 186 次
    - **例句**：`// avoid unreferenced parameter warning`
    - **中文翻译**：避免「未引用参数」的警告。
    - **位置**：`jni/include/Embree/foundation/PxPreprocessor.h:468`
- **061 `round`**　/raʊnd/　*v./adj.*　取整；圆形的　【204 次 / 11 个文件】
    - **用法**：`round down` = 向下取整；`round` 也是“轮次”。
    - **频次**：标识符 167 次 · 注释 37 次
    - **例句**：`RTC_GEOMETRY_TYPE_ROUND_LINEAR_CURVE = 16, // Round (rounded cone like) linear curves`
    - **中文翻译**：RTC_GEOMETRY_TYPE_ROUND_LINEAR_CURVE（枚举值 16）：圆角（类似圆锥体）线性曲线。
    - **位置**：`jni/include/Embree/include/rtcore_geometry.h:27`
- **062 `pass`**　/pɑːs/　*v.*　传递；通过　【198 次 / 15 个文件】
    - **用法**：`pass by value` = 按值传参；`pass` 也是“通过测试”。
    - **频次**：标识符 87 次 · 注释 111 次
    - **例句**：`// - Each context create its own ImFontAtlas by default. You may instance one yourself and pass it to CreateContext() to share a font atlas between contexts.`
    - **中文翻译**：默认每个上下文会创建自己的 ImFontAtlas。你也可以自己实例化一个并传给 CreateContext()，从而在多个上下文间共享字体图集。
    - **位置**：`jni/include/ImGui/imgui.h:391`
- **063 `remove`**　/rɪˈmuːv/　*v.*　移除　【198 次 / 21 个文件】
    - **用法**：`remove_if` = 条件删除。与 add 成对。
    - **频次**：标识符 118 次 · 注释 80 次
    - **例句**：`// remove geometry use disable`
    - **中文翻译**：移除几何体，用 disable（禁用）。
    - **位置**：`jni/include/Embree/PhysX.h:1494`
- **064 `define`**　/dɪˈfaɪn/　*v.*　定义　【190 次 / 16 个文件】
    - **用法**：`#define` = 宏定义（第 10 章）。也可指“界定含义”。
    - **频次**：标识符 0 次 · 注释 190 次
    - **例句**：`// define ray`
    - **中文翻译**：定义光线（ray）。
    - **位置**：`jni/include/Embree/PhysX.h:1619`
- **065 `filter`**　/ˈfɪltə(r)/　*v./n.*　过滤；滤波器　【186 次 / 16 个文件】
    - **用法**：`filter out invalid` = 滤掉非法项。第 89 章核心动作。
    - **频次**：标识符 145 次 · 注释 41 次
    - **例句**：`// not change, filter it.`
    - **中文翻译**：没变化，就把它过滤掉。
    - **位置**：`jni/include/Embree/PhysX.h:1421`
- **066 `align`**　/əˈlaɪn/　*v.*　对齐　【184 次 / 19 个文件】
    - **用法**：`alignas(16)` = 16 字节对齐（第 07 章）。显存/内存都有对齐要求。
    - **频次**：标识符 137 次 · 注释 47 次
    - **例句**：`// Possible optimization: align the whole struct to cache line`
    - **中文翻译**：可能的优化：把整个结构体按缓存行对齐。
    - **位置**：`jni/include/Embree/PhysX.h:502`
- **067 `reset`**　/ˌriːˈset/　*v.*　重置　【177 次 / 13 个文件】
    - **用法**：`Reset()` = 恢复到初始状态。不是“删除”。
    - **频次**：标识符 126 次 · 注释 51 次
    - **例句**：`// Use those function to benefit from the CallbackResize behaviors. Calling those function reset the selection.`
    - **中文翻译**：使用这些函数来享受 CallbackResize 的行为。调用这些函数会重置选择状态。
    - **位置**：`jni/include/ImGui/imgui.h:2704`
- **068 `merge`**　/mɜːdʒ/　*v.*　合并　【175 次 / 14 个文件】
    - **用法**：`merge adjacent regions` = 合并相邻区间（题 2.4）。
    - **频次**：标识符 120 次 · 注释 55 次
    - **例句**：`// Split/Merge functions are used to split the draw list into different layers which can be drawn into out of order.`
    - **中文翻译**：拆分/合并函数用于把绘制列表切分成不同图层，从而可以乱序绘制。
    - **位置**：`jni/include/ImGui/imgui.h:3265`
- **069 `release`**　/rɪˈliːs/　*v.*　释放、发布　【161 次 / 26 个文件】
    - **用法**：`Release()` = 释放资源；`release build` = 发布版（无调试信息）。
    - **频次**：标识符 107 次 · 注释 54 次
    - **例句**：`The operation will fail, if there are still modules referencing the foundation object. Release all dependent modules`
    - **中文翻译**：如果仍有模块引用着 foundation 对象，该操作会失败；需要先释放所有依赖模块。
    - **位置**：`jni/include/Embree/foundation/PxFoundation.h:56`
- **070 `show`**　/ʃəʊ/　*v.*　显示　【161 次 / 7 个文件】
    - **用法**：`ShowWindow()` = 显示窗口。与 hide 成对。
    - **频次**：标识符 130 次 · 注释 31 次
    - **例句**：`// - for frequently actioned or hovered items providing a tooltip, you want may to use ImGuiHoveredFlags_ForTooltip (stationary + delay) so the tooltip doesn't show too often.`
    - **中文翻译**：对于频繁被操作或悬停、又需要显示提示的条目，你可以用 ImGuiHoveredFlags_ForTooltip（静止 + 延迟）来避免提示出现得太频繁。
    - **位置**：`jni/include/ImGui/imgui.h:1487`
- **071 `convert`**　/kənˈvɜːt/　*v.*　转换　【155 次 / 10 个文件】
    - **用法**：`ConvertToScreen()` = 转到屏幕坐标（第 51 章）。
    - **频次**：标识符 90 次 · 注释 65 次
    - **例句**：`//---- Define constructor and implicit cast operators to convert back<>forth between your math types and ImVec2/ImVec4.`
    - **中文翻译**：自定义：定义构造函数和隐式转换运算符，在你的数学类型与 ImVec2/ImVec4 之间互相转换。
    - **位置**：`jni/include/ImGui/imconfig.h:106`
- **072 `fill`**　/fɪl/　*v.*　填充　【150 次 / 16 个文件】
    - **用法**：`std::fill` = 填满；`fill rate` = 填充率（图形性能指标）。
    - **频次**：标识符 110 次 · 注释 40 次
    - **例句**：`// Legacy: before 1.87, we required backend to fill io.KeyMap[] (imgui->native map) during initialization and io.KeysDown[] (native indices) every frame.`
    - **中文翻译**：旧式做法：在 1.87 之前，我们要求后端在初始化时填充 io.KeyMap[]（imgui→原生映射），并在每帧填充 io.KeysDown[]（原生索引）。
    - **位置**：`jni/include/ImGui/imgui.h:2641`
- **073 `alloc`**　/əˈlɒk/　*v.*　分配　【136 次 / 9 个文件】
    - **用法**：`allocate` 的缩写。`Allocator` 是分配器（第 14 章）。
    - **频次**：标识符 131 次 · 注释 5 次
    - **例句**：`// 1.09 (2016-01-16) warning fix; avoid crash on outofmem; use alloc userdata for PackFontRanges`
    - **中文翻译**：1.09（2016-01-16）：修复警告；避免内存不足时崩溃；为 PackFontRanges 使用自定义分配的用户数据。
    - **位置**：`jni/include/ImGui/imstb_truetype.h:5002`
- **074 `process`**　/ˈprəʊses/　*v./n.*　处理；进程　【135 次 / 20 个文件】
    - **用法**：动词“处理数据”，名词“进程”（第 18 章）。同一个词两个身份。
    - **频次**：标识符 63 次 · 注释 72 次
    - **例句**：`// Process mirror display`
    - **中文翻译**：处理镜像显示。
    - **位置**：`jni/include/ImGui/ANativeWindowCreator.h:1312`
- **075 `pad`**　/pæd/　*v./n.*　填充字节；内边距　【126 次 / 9 个文件】
    - **用法**：`padding` = 结构体对齐填充（第 07 章）。ImGui 里是界面内边距。
    - **频次**：标识符 109 次 · 注释 17 次
    - **例句**：`// pad on left and top`
    - **中文翻译**：在左侧和顶部留白（padding）。
    - **位置**：`jni/include/ImGui/imstb_truetype.h:4244`
- **076 `insert`**　/ɪnˈsɜːt/　*v.*　插入　【117 次 / 10 个文件】
    - **用法**：`insert` 会移动后续元素（O(n)），`push_back` 不会。
    - **频次**：标识符 86 次 · 注释 31 次
    - **例句**：`// NB: It is illegal to call push_back/push_front/insert with a reference pointing inside the ImVector data itself! e.g. v.push_back(v[10]) is forbidden.`
    - **中文翻译**：注意：用指向 ImVector 自身数据内部的引用来调用 push_back/push_front/insert 是非法的！例如 v.push_back(v[10]) 是被禁止的。
    - **位置**：`jni/include/ImGui/imgui.h:2282`
- **077 `match`**　/mætʃ/　*v./n.*　匹配　【117 次 / 12 个文件】
    - **用法**：`match the format` = 与格式对应。写错格式就解析失败。
    - **频次**：标识符 56 次 · 注释 61 次
    - **例句**：`\param[in] eventName The name of the zone ending, must match the corresponding name passed with 'zoneStart'. Must be a persistent const char *.`
    - **中文翻译**：（Doxygen 参数）eventName：要结束的区域的名称，必须与 'zoneStart' 传入的对应名称一致；必须是一个持久化的 const char*。
    - **位置**：`jni/include/Embree/foundation/PxProfiler.h:62`
- **078 `forward`**　/ˈfɔːwəd/　*v./adv.*　转发；向前　【116 次 / 16 个文件】
    - **用法**：`forward the touch event` = 把触摸事件转发出去（第 71 章）。
    - **频次**：标识符 75 次 · 注释 41 次
    - **例句**：`// camera = matrix[3]*x + matrix[7]*y + matrix[11]*z + matrix[15] (forward 深度)`
    - **中文翻译**：相机深度 = matrix[3]*x + matrix[7]*y + matrix[11]*z + matrix[15]（前向深度）。
    - **位置**：`jni/include/Android_draw/WorldToScreen.h:48`
- **079 `hook`**　/hʊk/　*v./n.*　挂钩、拦截　【116 次 / 5 个文件】
    - **用法**：`hook` = 在函数前后插自己的逻辑（第 20 章 GOT/PLT 可用来 hook）。
    - **频次**：标识符 107 次 · 注释 9 次
    - **例句**：`// Brian Hook Omar Cornut github:vassvik`
    - **中文翻译**：Brian、Hook、Omar Cornut、github:vassvik（贡献者署名）。
    - **位置**：`jni/include/ImGui/imstb_truetype.h:52`
- **080 `save`**　/seɪv/　*v.*　保存　【114 次 / 12 个文件】
    - **用法**：`SaveConfig()` = 存配置。与 load 成对。
    - **频次**：标识符 73 次 · 注释 41 次
    - **例句**：`// - Set io.IniFilename to NULL to load/save manually. Read io.WantSaveIniSettings description about handling .ini saving manually.`
    - **中文翻译**：把 io.IniFilename 设为 NULL 即可手动加载/保存配置。关于手动处理 .ini 保存，可阅读 io.WantSaveIniSettings 的说明。
    - **位置**：`jni/include/ImGui/imgui.h:1134`
- **081 `malloc`**　/ˈmælɒk/　*v.*　分配堆内存（C 函数）　【102 次 / 11 个文件】
    - **用法**：`malloc/free` 是 C 的堆分配对；C++ 用 `new/delete`。
    - **频次**：标识符 85 次 · 注释 17 次
    - **例句**：`//#define IMGUI_DISABLE_DEFAULT_ALLOCATORS // Don't implement default allocators calling malloc()/free() to avoid linking with them. You will need to call ImGui::SetAllocatorFunctions().`
    - **中文翻译**：宏：禁用默认分配器（不实现调用 malloc()/free() 的默认分配器，以避免链接它们）。你需要改用 ImGui::SetAllocatorFunctions()。
    - **位置**：`jni/include/ImGui/imconfig.h:52`
- **082 `cache`**　/kæʃ/　*v./n.*　缓存　【97 次 / 19 个文件】
    - **用法**：`cache the result` = 把结果存起来下次直接用（第 99 章）。
    - **频次**：标识符 77 次 · 注释 20 次
    - **例句**：`// Possible optimization: align the whole struct to cache line`
    - **中文翻译**：可能的优化：把整个结构体按缓存行对齐。
    - **位置**：`jni/include/Embree/PhysX.h:502`
- **083 `submit`**　/səbˈmɪt/　*v.*　提交　【96 次 / 11 个文件】
    - **用法**：`vkQueueSubmit` = 把命令提交给 GPU（第 58 章）。
    - **频次**：标识符 38 次 · 注释 58 次
    - **例句**：`// - User may submit their own textures to e.g. ImGui::Image() function by passing this value.`
    - **中文翻译**：用户可以把自己的纹理通过这个数值传给例如 ImGui::Image() 函数。
    - **位置**：`jni/include/ImGui/imgui.h:328`
- **084 `upload`**　/ˌʌpˈləʊd/　*v.*　上传　【94 次 / 10 个文件】
    - **用法**：`upload the vertex buffer` = 把顶点数据传到显存。
    - **频次**：标识符 75 次 · 注释 19 次
    - **例句**：`// to extract the ImTextureID value during rendering, after texture upload has happened.`
    - **中文翻译**：用于在纹理上传完成后、在渲染期间提取 ImTextureID 的值。
    - **位置**：`jni/include/ImGui/imgui.h:357`
- **085 `bind`**　/baɪnd/　*v.*　绑定　【88 次 / 9 个文件】
    - **用法**：`vkBindBuffer` = 把缓冲绑定到管线。`std::bind` 是函数绑定。
    - **频次**：标识符 79 次 · 注释 9 次
    - **例句**：`// - If you want to bind the current atlas when using custom rectangle, you can use io.Fonts->TexRef.`
    - **中文翻译**：如果你在用自定义矩形时想绑定当前字体图集，可以用 io.Fonts->TexRef。
    - **位置**：`jni/include/ImGui/imgui.h:361`
- **086 `parse`**　/pɑːz/　*v.*　解析　【88 次 / 11 个文件】
    - **用法**：`parse the maps line` = 解析 maps 的一行（第 76 章）。
    - **频次**：标识符 69 次 · 注释 19 次
    - **例句**：`// Helper: Parse and apply text filters. In format "aaaaa[,bbbb][,ccccc]"`
    - **中文翻译**：辅助函数：解析并应用文本过滤器，格式为 "aaaaa[,bbbb][,ccccc]"。
    - **位置**：`jni/include/ImGui/imgui.h:2767`
- **087 `search`**　/sɜːtʃ/　*v./n.*　搜索　【87 次 / 11 个文件】
    - **用法**：`search for a pattern` = 搜字节模式（第 87 章找偏移）。
    - **频次**：标识符 45 次 · 注释 42 次
    - **例句**：`// search domain. Increasing the radius or modifying the time or position of`
    - **中文翻译**：搜索域。增大半径，或修改……的时间或位置，
    - **位置**：`jni/include/Embree/include/rtcore_common.h:446`
- **088 `shrink`**　/ʃrɪŋk/　*v.*　收缩　【84 次 / 8 个文件】
    - **用法**：`shrink_to_fit()` = 收缩容量。与 expand 成对。
    - **频次**：标识符 61 次 · 注释 23 次
    - **例句**：`// radius of the query can be decreased inside the callback to shrink the`
    - **中文翻译**：查询半径可以在回调里减小，以缩小……
    - **位置**：`jni/include/Embree/include/rtcore_common.h:445`
- **089 `compare`**　/kəmˈpeə(r)/　*v.*　比较　【81 次 / 12 个文件】
    - **用法**：`memcmp` = 内存比较；`compares equal` = 比较相等。
    - **频次**：标识符 52 次 · 注释 29 次
    - **例句**：`// pixels, you would compare against (180-36.0*3)/255 = 72/255. The above`
    - **中文翻译**：对于像素，你会拿它和 (180-36.0*3)/255 = 72/255 比较。上面的……
    - **位置**：`jni/include/ImGui/imstb_truetype.h:987`
- **090 `accept`**　/əkˈsept/　*v.*　接受　【80 次 / 8 个文件】
    - **用法**：`accept()` 是 TCP 接受连接；也指“函数接受某类型”。
    - **频次**：标识符 56 次 · 注释 24 次
    - **例句**：`// - The default route is ImGuiInputFlags_RouteFocused (accept inputs if window is in focus stack. Deep-most focused window takes inputs. ActiveId takes inputs over deep-most focused window.)`
    - **中文翻译**：默认路由是 ImGuiInputFlags_RouteFocused（当窗口在焦点栈中时接收输入；最深层的聚焦窗口接收输入；ActiveId 的优先级高于最深层聚焦窗口）。
    - **位置**：`jni/include/ImGui/imgui_internal.h:3637`
- **091 `compute`**　/kəmˈpjuːt/　*v.*　计算　【80 次 / 12 个文件】
    - **用法**：`ComputeOcclusion()` = 算遮挡（第 95 章）。比 calculate 更常用于算法名。
    - **频次**：标识符 29 次 · 注释 51 次
    - **例句**：`// similarity transform and vice versa. The similarity scale allows to compute`
    - **中文翻译**：相似变换以及其逆变换。相似度缩放因子可以用来计算……
    - **位置**：`jni/include/Embree/include/rtcore_common.h:464`
- **092 `wait`**　/weɪt/　*v.*　等待　【78 次 / 14 个文件】
    - **用法**：`Wait()` = 自旋或阻塞等待。第 82 章核心词。
    - **频次**：标识符 67 次 · 注释 11 次
    - **例句**：`/* rtcCommitSceneWithQueue is asynchronous, user has to call queue.wait()`
    - **中文翻译**：rtcCommitSceneWithQueue 是异步的，用户必须调用 queue.wait() 等待其完成。
    - **位置**：`jni/include/Embree/include/rtcore_device.h:35`
- **093 `toggle`**　/ˈtɒɡl/　*v./n.*　切换（开/关）　【77 次 / 7 个文件】
    - **用法**：`ToggleButton` = 开关按钮。布尔状态反转。
    - **频次**：标识符 45 次 · 注释 32 次
    - **例句**：`IMGUI_API bool IsItemToggledSelection(); // Was the last item selection state toggled? Useful if you need the per-item information _before_ reaching EndMultiSelect(). We only returns toggle _event_ in order to handle clipping correctly.`
    - **中文翻译**：IMGUI_API 函数：判断上一个条目的选择状态是否被切换（toggle）；在到达 EndMultiSelect() 之前若需要逐条信息时很有用，为正确处理裁剪它只返回 toggle「事件」。
    - **位置**：`jni/include/ImGui/imgui.h:779`
- **094 `resolve`**　/rɪˈzɒlv/　*v.*　解析、解决　【75 次 / 7 个文件】
    - **用法**：`resolve the symbol` = 解析出符号地址（第 20/77 章）。
    - **频次**：标识符 73 次 · 注释 2 次
    - **例句**：`// Resolve callback data pointers`
    - **中文翻译**：解析回调数据指针。
    - **位置**：`jni/src/ImGui/imgui_draw.cpp:2336`
- **095 `swap`**　/swɒp/　*v.*　交换　【74 次 / 13 个文件】
    - **用法**：`std::swap` = 交换两值。双缓冲就是两个缓冲 swap。
    - **频次**：标识符 51 次 · 注释 23 次
    - **例句**：`// - We use the same sets of flags for DragXXX() and SliderXXX() functions as the features are the same and it makes it easier to swap them.`
    - **中文翻译**：我们给 DragXXX() 和 SliderXXX() 用同一套标志，因为它们的功能相同，这样互换起来也更方便。
    - **位置**：`jni/include/ImGui/imgui.h:677`
- **096 `decode`**　/ˌdiːˈkəʊd/　*v.*　解码　【73 次 / 3 个文件】
    - **用法**：`decode the instruction` = 解码指令。与 encode 成对。
    - **频次**：标识符 53 次 · 注释 20 次
    - **例句**：`// decode the full file, you can use the stbi_info family of functions:`
    - **中文翻译**：要解码完整文件，可以用 stbi_info 系列函数：
    - **位置**：`jni/include/ImGui/stb_image.h:184`
- **097 `append`**　/əˈpend/　*v.*　追加　【71 次 / 8 个文件】
    - **用法**：`append to the end` = 加到末尾。`push_back` 就是一种 append。
    - **频次**：标识符 28 次 · 注释 43 次
    - **例句**：`// - You may append multiple times to the same window during the same frame by calling Begin()/End() pairs multiple times.`
    - **中文翻译**：你可以在同一帧里多次对同一窗口调用 Begin()/End() 对，从而多次追加内容。
    - **位置**：`jni/include/ImGui/imgui.h:429`
- **098 `enter`**　/ˈentə(r)/　*v.*　进入　【70 次 / 6 个文件】
    - **用法**：`enter the lock` = 上锁；`enter the function` = 进函数。
    - **频次**：标识符 43 次 · 注释 27 次
    - **例句**：`ImGuiInputTextFlags_EnterReturnsTrue = 1 << 6, // Return 'true' when Enter is pressed (as opposed to every time the value was modified). Consider disabling LiveEdit! or using IsItemDeactivatedAfterEdit() instead!`
    - **中文翻译**：ImGuiInputTextFlags_EnterReturnsTrue 标志（1<<6）：按下回车键时才返回 true（而不是每次数值变化都返回）；建议改用 IsItemDeactivatedAfterEdit()，或关闭 LiveEdit！
    - **位置**：`jni/include/ImGui/imgui.h:1280`
- **099 `stop`**　/stɒp/　*v.*　停止　【67 次 / 10 个文件】
    - **用法**：`StopThread()` = 让线程退出（第 82 章）。与 start 成对。
    - **频次**：标识符 38 次 · 注释 29 次
    - **例句**：`// - If you stop calling BeginDragDropSource() the payload is preserved however it won't have a preview tooltip (we currently display a fallback "..." tooltip, see #1725)`
    - **中文翻译**：如果你不再调用 BeginDragDropSource()，拖拽载荷会保留，但不会有预览提示（目前会显示一个兜底 "..." 提示，见 #1725）。
    - **位置**：`jni/include/ImGui/imgui.h:976`
- **100 `ignore`**　/ɪɡˈnɔː(r)/　*v.*　忽略　【65 次 / 11 个文件】
    - **用法**：`ignore the return value` = 不检查返回值——注释里出现说明可能有坑。
    - **频次**：标识符 24 次 · 注释 41 次
    - **例句**：`// - Code polling every key (e.g. an interface to detect a key press for input mapping) might want to ignore those`
    - **中文翻译**：轮询每个按键的代码（比如用于检测按键以做输入映射的接口）可能想忽略这些事件。
    - **位置**：`jni/include/ImGui/imgui.h:1678`


---

## 第 3 组

- **101 `refresh`**　/rɪˈfreʃ/　*v.*　刷新　【63 次 / 7 个文件】
    - **用法**：`Refresh()` = 重新拉取数据。与 update 的区别：refresh 强制重来。
    - **频次**：标识符 47 次 · 注释 16 次
    - **例句**：`// Refresh policy/frequency, Load Balancing etc.`
    - **中文翻译**：刷新策略/频率、负载均衡等。
    - **位置**：`jni/include/ImGui/imgui_internal.h:1324`
- **102 `hide`**　/haɪd/　*v.*　隐藏　【60 次 / 6 个文件】
    - **用法**：`Hide()` = 隐藏界面或图层。与 show 成对。
    - **频次**：标识符 31 次 · 注释 29 次
    - **例句**：`IMGUI_API void TableSetColumnEnabled(int column_n, bool v);// change user accessible enabled/disabled state of a column. Set to false to hide the column. User can use the context menu to change this themselves (right-click in headers, or right-click in columns body with ImGuiTableFlags_ContextMenuInBody)`
    - **中文翻译**：IMGUI_API 函数：改变某一列对用户可见的启用/禁用状态；设为 false 即隐藏该列。用户也可通过右键菜单自行修改（在表头右键，或开启 ImGuiTableFlags_ContextMenuInBody 后在列主体右键）。
    - **位置**：`jni/include/ImGui/imgui.h:939`
- **103 `break`**　/breɪk/　*v./n.*　中断；破坏　【59 次 / 14 个文件】
    - **用法**：`break` 跳出循环（C++ 关键字）；`breaking change` = 破坏性改动。
    - **频次**：标识符 0 次 · 注释 59 次
    - **例句**：`// break able wall`
    - **中文翻译**：可击破的墙。
    - **位置**：`jni/include/Embree/PhysX.h:1413`
- **104 `rotate`**　/rəʊˈteɪt/　*v.*　旋转　【58 次 / 12 个文件】
    - **用法**：`Rotate()` = 绕轴旋转（第 47 章）。
    - **频次**：标识符 52 次 · 注释 6 次
    - **例句**：`//! Rotate vector by matrix, equal to v' = M*v`
    - **中文翻译**：用矩阵旋转向量，等价于 v' = M*v。
    - **位置**：`jni/include/Embree/foundation/PxMat44.h:290`
- **105 `run`**　/rʌn/　*v.*　运行　【58 次 / 15 个文件】
    - **用法**：`run()` = 主循环；`run on the GPU` = 在 GPU 上执行。
    - **频次**：标识符 25 次 · 注释 33 次
    - **例句**：`// Run "dumpsys display" and get result`
    - **中文翻译**：运行 "dumpsys display" 并获取结果。
    - **位置**：`jni/include/ImGui/ANativeWindowCreator.h:1258`
- **106 `calculate`**　/ˈkælkjuleɪt/　*v.*　计算　【57 次 / 8 个文件】
    - **用法**：`calculate the distance` = 算距离。比 compute 更口语。
    - **频次**：标识符 1 次 · 注释 56 次
    - **例句**：`/* Sets the bounding callback function to calculate bounding boxes for user primitives. */`
    - **中文翻译**：设置用于计算用户图元包围盒的回调边界函数。
    - **位置**：`jni/include/Embree/include/rtcore_geometry.h:215`
- **107 `lerp`**　/lɜːp/　*v.*　线性插值（linear interpolate 缩写）　【55 次 / 5 个文件】
    - **用法**：`Lerp(a, b, t)` = a 到 b 按比例 t 插值（第 55 章）。
    - **频次**：标识符 54 次 · 注释 1 次
    - **例句**：`// Between 1.91.0 and 1.91.4 we made selected Selectable use an arbitrary lerp between _Header and _HeaderHovered. Removed that now. (#8106)`
    - **中文翻译**：在 1.91.0 到 1.91.4 之间，我们让被选中的 Selectable 在 _Header 和 _HeaderHovered 之间用了一个随意的插值。现在已经去掉了。（#8106）
    - **位置**：`jni/src/ImGui/imgui_widgets.cpp:7548`
- **108 `ensure`**　/ɪnˈʃʊə(r)/　*v.*　确保　【50 次 / 11 个文件】
    - **用法**：`ensure the buffer is large enough` = 确保缓冲够大。
    - **频次**：标识符 0 次 · 注释 50 次
    - **例句**：`// Ensure that the application hasn't tweaked the pack value to less than 8, which would break`
    - **中文翻译**：确保应用程序没有把 pack 值改到小于 8，否则会破坏……
    - **位置**：`jni/include/Embree/foundation/PxPreprocessor.h:475`
- **109 `perform`**　/pəˈfɔːm/　*v.*　执行、进行　【49 次 / 13 个文件】
    - **用法**：`perform a read` = 执行一次读操作。
    - **频次**：标识符 0 次 · 注释 49 次
    - **例句**：`/* Perform a closest point query of the scene. */`
    - **中文翻译**：对场景执行一次「最近点」查询。
    - **位置**：`jni/include/Embree/include/rtcore_scene.h:153`
- **110 `provide`**　/prəˈvaɪd/　*v.*　提供　【49 次 / 12 个文件】
    - **用法**：`provides an API` = 提供接口。
    - **频次**：标识符 0 次 · 注释 49 次
    - **例句**：`//---- Debug Tools: Macro to break in Debugger (we provide a default implementation of this in the codebase)`
    - **中文翻译**：调试工具：用于在调试器中打断点的宏（代码库里已提供默认实现）。
    - **位置**：`jni/include/ImGui/imconfig.h:132`
- **111 `try`**　/traɪ/　*v.*　尝试　【49 次 / 14 个文件】
    - **用法**：`try … catch` 是异常处理（第 11 章）。
    - **频次**：标识符 0 次 · 注释 49 次
    - **例句**：`// MirrorSurface methods - try multiple signatures`
    - **中文翻译**：MirrorSurface 的方法——尝试多种函数签名。
    - **位置**：`jni/include/ImGui/ANativeWindowCreator.h:336`
- **112 `erase`**　/ɪˈreɪz/　*v.*　删除（容器元素）　【46 次 / 8 个文件】
    - **用法**：`erase` 会移动后续元素；`clear` 是清空全部。
    - **频次**：标识符 46 次 · 注释 0 次
    - **例句**：`ShapeDatas.erase(`
    - **中文翻译**：（代码）ShapeDatas 容器的 erase() 调用（擦除某个元素）。
    - **位置**：`jni/include/Embree/PhysX.h:723`
- **113 `escape`**　/ɪˈskeɪp/　*v.*　转义　【46 次 / 6 个文件】
    - **用法**：`escape the quotes` = 给引号加反斜杠。`\n` 就是转义序列。
    - **频次**：标识符 25 次 · 注释 21 次
    - **例句**：`// - If not modal: they can be closed by clicking anywhere outside them, or by pressing Escape (call 'Shortcut(ImGuiKey_Escape)' to claim a higher-priority shortcut).`
    - **中文翻译**：如果不是模态弹窗：点击弹窗外任意位置，或按 Escape 即可关闭（调用 Shortcut(ImGuiKey_Escape) 可抢占更高优先级的快捷键）。
    - **位置**：`jni/include/ImGui/imgui.h:840`
- **114 `specify`**　/ˈspesɪfaɪ/　*v.*　指定　【43 次 / 10 个文件】
    - **用法**：`unless otherwise specified` = 除非另有说明。
    - **频次**：标识符 3 次 · 注释 40 次
    - **例句**：`// - TL;DR: if you don't want to use right mouse button for popups, always specify it explicitly using a named ImGuiPopupFlags_MouseButtonXXXX value.`
    - **中文翻译**：太长不读：如果你不想用鼠标右键触发弹窗，请始终用一个具名的 ImGuiPopupFlags_MouseButtonXXXX 值显式指定。
    - **位置**：`jni/include/ImGui/imgui.h:873`
- **115 `assume`**　/əˈsjuːm/　*v.*　假设　【42 次 / 12 个文件】
    - **用法**：`assumes the pointer is valid` = 假设指针有效——**这种注释等于免责声明，你要自己验证**。
    - **频次**：标识符 5 次 · 注释 37 次
    - **例句**：`// Our system never assume that you identify items by indices, it never attempts to interpolate between two values.`
    - **中文翻译**：我们的系统从不假设你是靠索引来识别条目的，也从不尝试在两个值之间做插值。
    - **位置**：`jni/include/ImGui/imgui.h:3059`
- **116 `expand`**　/ɪkˈspænd/　*v.*　扩展　【42 次 / 8 个文件】
    - **用法**：`Expand(bounds)` = 把包围盒撑大到包含新点（第 96 章）。
    - **频次**：标识符 29 次 · 注释 13 次
    - **例句**：`// NB- Expand the rectangle to be generous on imprecise inputs systems (g.Style.TouchExtraPadding)`
    - **中文翻译**：注意：把矩形放大一些，以便宽容处理不精确的输入系统（如 g.Style.TouchExtraPadding）。
    - **位置**：`jni/src/ImGui/imgui.cpp:10193`
- **117 `require`**　/rɪˈkwaɪə(r)/　*v.*　需要、要求　【41 次 / 11 个文件】
    - **用法**：`requires root` = 需要 root 权限。
    - **频次**：标识符 0 次 · 注释 41 次
    - **例句**：`//#define IMGUI_ENABLE_WIN32_DEFAULT_IME_FUNCTIONS // [Win32] [Default with Visual Studio] Implement default IME handler (require imm32.lib/.a, auto-link for Visual Studio, -limm32 on command-line for MinGW)`
    - **中文翻译**：宏：[Win32][Visual Studio 默认] 启用默认 IME（输入法）处理器（需要 imm32.lib/.a；VS 自动链接，MinGW 命令行需 -limm32）。
    - **位置**：`jni/include/ImGui/imconfig.h:42`
- **118 `split`**　/splɪt/　*v.*　分割　【41 次 / 8 个文件】
    - **用法**：`split the string` = 拆字符串；BVH 里 `split` = 划分节点。
    - **频次**：标识符 28 次 · 注释 13 次
    - **例句**：`/* Callback to split a build primitive */`
    - **中文翻译**：用于拆分构建图元的回调函数。
    - **位置**：`jni/include/Embree/include/rtcore_builder.h:37`
- **119 `follow`**　/ˈfɒləʊ/　*v.*　跟随　【39 次 / 7 个文件】
    - **用法**：`follow the pointer chain` = 跟指针链（第 87 章）。
    - **频次**：标识符 12 次 · 注释 27 次
    - **例句**：`// - In Visual Studio: Ctrl+Comma ("Edit.GoToAll") can follow symbols inside comments, whereas Ctrl+F12 ("Edit.GoToImplementation") cannot.`
    - **中文翻译**：在 Visual Studio 里：Ctrl+逗号（"Edit.GoToAll"）可以跳转到注释里的符号，而 Ctrl+F12（"Edit.GoToImplementation"）不行。
    - **位置**：`jni/include/ImGui/imgui.h:218`
- **120 `commit`**　/kəˈmɪt/　*v.*　提交、生效　【35 次 / 10 个文件】
    - **用法**：`rtcCommitScene` = 让场景构建生效（第 97 章）。不改数据后必须 commit。
    - **频次**：标识符 29 次 · 注释 6 次
    - **例句**：`// commit scene`
    - **中文翻译**：提交场景。
    - **位置**：`jni/include/Embree/PhysX.h:1612`
- **121 `register`**　/ˈredʒɪstə(r)/　*v./n.*　注册；寄存器　【33 次 / 9 个文件】
    - **用法**：动词“注册回调”；名词“寄存器”（第 25 章）。同名两义。
    - **频次**：标识符 10 次 · 注释 23 次
    - **例句**：`// - The general idea is that several callers may register interest in a shortcut, and only one owner gets it.`
    - **中文翻译**：大致思路是：多个调用方都可以对某个快捷键「登记兴趣」，但只有一个所有者能拿到它。
    - **位置**：`jni/include/ImGui/imgui.h:1081`
- **122 `print`**　/prɪnt/　*v.*　打印、输出　【32 次 / 12 个文件】
    - **用法**：`printf` = 格式化输出。调试第一手段。
    - **频次**：标识符 24 次 · 注释 8 次
    - **例句**：`// Complete program (this compiles): get a single bitmap, print as ASCII art`
    - **中文翻译**：一个完整可编译的程序：获取一张位图，并以 ASCII 字符画形式打印出来。
    - **位置**：`jni/include/ImGui/imstb_truetype.h:330`
- **123 `export`**　/ɪkˈspɔːt/　*v.*　导出　【31 次 / 6 个文件】
    - **用法**：`export the symbols` = 导出符号供外部链接（第 28 章）。
    - **频次**：标识符 27 次 · 注释 4 次
    - **例句**：`//#define IMGUI_API __declspec(dllexport) // MSVC Windows: DLL export`
    - **中文翻译**：宏定义 IMGUI_API：__declspec(dllexport)（MSVC Windows：DLL 导出）。
    - **位置**：`jni/include/ImGui/imconfig.h:27`
- **124 `happen`**　/ˈhæpən/　*v.*　发生　【31 次 / 9 个文件】
    - **用法**：`what happens if…` = 如果……会怎样。文档里的引导句。
    - **频次**：标识符 0 次 · 注释 31 次
    - **例句**：`// - Use 'Demo->Tools->Debug Log->Selection' to see requests as they happen.`
    - **中文翻译**：用「Demo→Tools→Debug Log→Selection」可以实时查看这些请求。
    - **位置**：`jni/include/ImGui/imgui.h:3099`
- **125 `dispatch`**　/dɪˈspætʃ/　*v.*　分发、调度　【30 次 / 5 个文件】
    - **用法**：`dispatch the command` = 把命令分发到对应处理函数。
    - **频次**：标识符 14 次 · 注释 16 次
    - **例句**：`// (when reading from the io.WantCaptureMouse, io.WantCaptureKeyboard flags to dispatch your inputs, it is`
    - **中文翻译**：当你读取 io.WantCaptureMouse、io.WantCaptureKeyboard 标志来决定是否由 ImGui 接管输入时……
    - **位置**：`jni/include/ImGui/imgui.h:2578`
- **126 `saturate`**　/ˈsætʃəreɪt/　*v.*　饱和（超范围即取边界）　【30 次 / 3 个文件】
    - **用法**：`saturate()` = 把超范围值压到边界，即 clamp 到 [0,1]。
    - **频次**：标识符 30 次 · 注释 0 次
- **127 `emit`**　/iˈmɪt/　*v.*　发出、生成　【29 次 / 7 个文件】
    - **用法**：`emit an event` = 抛一个事件；编译后端里 emit = 生成指令。
    - **频次**：标识符 10 次 · 注释 19 次
    - **例句**：`// - May be used to alter render state (change sampler, blending, current shader). May be used to emit custom rendering commands (difficult to do correctly, but possible).`
    - **中文翻译**：可用于改变渲染状态（切换采样器、混合模式、当前着色器）。也可用于发出自定义渲染指令（做对很难，但可行）。
    - **位置**：`jni/include/ImGui/imgui.h:3426`
- **128 `reload`**　/ˌriːˈləʊd/　*v.*　重新加载　【29 次 / 4 个文件】
    - **用法**：`reload the config` = 重读配置。与 refresh 近义。
    - **频次**：标识符 24 次 · 注释 5 次
    - **例句**：`// Reload user buf (WIP #2890)`
    - **中文翻译**：重新加载用户缓冲区（进行中 #2890）。
    - **位置**：`jni/include/ImGui/imgui_internal.h:1308`
- **129 `retrieve`**　/rɪˈtriːv/　*v.*　取回、检索　【29 次 / 5 个文件】
    - **用法**：`retrieve the value` = 把值取回来。
    - **频次**：标识符 0 次 · 注释 29 次
    - **例句**：`// - During the rendering loop, the Renderer Backend retrieve the ImTextureID, which stored inside a`
    - **中文翻译**：在渲染循环期间，渲染后端会取出 ImTextureID，它保存在……
    - **位置**：`jni/include/ImGui/imgui.h:329`
- **130 `delete`**　/dɪˈliːt/　*v.*　删除　【28 次 / 7 个文件】
    - **用法**：C++ 关键字 `delete` 释放 new 出来的对象。与 remove 的区别：delete 是销毁。
    - **频次**：标识符 0 次 · 注释 28 次
    - **例句**：`// IM_MALLOC(), IM_FREE(), IM_NEW(), IM_PLACEMENT_NEW(), IM_DELETE()`
    - **中文翻译**：IM_MALLOC()、IM_FREE()、IM_NEW()、IM_PLACEMENT_NEW()、IM_DELETE()（内存分配/释放相关宏与函数）。
    - **位置**：`jni/include/ImGui/imgui.h:2209`
- **131 `revert`**　/rɪˈvɜːt/　*v.*　还原、回退　【28 次 / 6 个文件】
    - **用法**：`revert the change` = 撤销改动。
    - **频次**：标识符 14 次 · 注释 14 次
    - **例句**：`// [2021/03/02] Reworked / reverted the revert, finally. Note we want e.g. BeginGroup/ItemAdd/EndGroup to work as well. (#3851)`
    - **中文翻译**：[2021/03/02] 最终重做/又撤销了之前的撤销。注意我们希望 BeginGroup/ItemAdd/EndGroup 等也能正常工作。（#3851）
    - **位置**：`jni/src/ImGui/imgui.cpp:4950`
- **132 `leave`**　/liːv/　*v.*　留下、离开　【27 次 / 9 个文件】
    - **用法**：`leave it unchanged` = 保持原样不动。
    - **频次**：标识符 0 次 · 注释 27 次
    - **例句**：`// - Neighbors selectable extend their highlight bounds in order to leave no gap between them. This is so a series of selected Selectable appear contiguous.`
    - **中文翻译**：相邻的 Selectable 会扩展各自的高亮边界，从而彼此之间不留空隙，让一连串被选中的 Selectable 看起来是连续的。
    - **位置**：`jni/include/ImGui/imgui.h:765`
- **133 `floor`**　/flɔː(r)/　*v./n.*　向下取整；地板　【25 次 / 7 个文件】
    - **用法**：`std::floor` = 向下取整。与 ceil 成对。
    - **频次**：标识符 24 次 · 注释 1 次
    - **例句**：`//#define IM_FLOOR IM_TRUNC // [OBSOLETE] Renamed in 1.90.0 (Sept 2023)`
    - **中文翻译**：宏：IM_FLOOR 改名为 IM_TRUNC（已废弃，1.90.0（2023 年 9 月）改名）。
    - **位置**：`jni/include/ImGui/imgui_internal.h:288`
- **134 `trigger`**　/ˈtrɪɡə(r)/　*v./n.*　触发　【23 次 / 6 个文件】
    - **用法**：`trigger a bug` = 触发 bug。测边界条件时的关键词。
    - **频次**：标识符 0 次 · 注释 23 次
    - **例句**：`// - Recovery after error/exception: record stack sizes with ErrorRecoveryStoreState(), disable assert, set log callback (to e.g. trigger high-level breakpoint), recover with ErrorRecoveryTryToRecoverState(), restore settings.`
    - **中文翻译**：错误/异常后的恢复：用 ErrorRecoveryStoreState() 记录栈大小，关闭断言，设置日志回调（例如触发高层断点），再用 ErrorRecoveryTryToRecoverState() 恢复，最后还原设置。
    - **位置**：`jni/include/ImGui/imgui.h:2510`
- **135 `prevent`**　/prɪˈvent/　*v.*　防止　【22 次 / 7 个文件】
    - **用法**：`prevent overflow` = 防止溢出。
    - **频次**：标识符 0 次 · 注释 22 次
    - **例句**：`// - OpenPopup(): set popup state to open (unless one of the specified ImGuiPopupFlags prevent opening).`
    - **中文翻译**：OpenPopup()：把弹窗状态设为「打开」（除非指定的某个 ImGuiPopupFlags 阻止打开）。
    - **位置**：`jni/include/ImGui/imgui.h:853`
- **136 `detect`**　/dɪˈtekt/　*v.*　检测　【21 次 / 6 个文件】
    - **用法**：`detect the driver` = 探测驱动是否存在（第 80 章）。
    - **频次**：标识符 0 次 · 注释 21 次
    - **例句**：`// - Code polling every key (e.g. an interface to detect a key press for input mapping) might want to ignore those`
    - **中文翻译**：轮询每个按键的代码（比如用于检测按键以做输入映射的接口）可能想忽略这些事件。
    - **位置**：`jni/include/ImGui/imgui.h:1678`
- **137 `acquire`**　/əˈkwaɪə(r)/　*v.*　获取（锁/语义）　【20 次 / 10 个文件】
    - **用法**：`acquire the lock` = 拿锁；`acquire semantics` = 获取语义（第 68 章内存序）。
    - **频次**：标识符 14 次 · 注释 6 次
    - **例句**：`// by making the caller acquire a temp buffer token, with either explicit or destructor release, e.g.`
    - **中文翻译**：做法是让调用方获取一个临时缓冲区令牌，再用显式或析构的方式释放，例如：
    - **位置**：`jni/src/ImGui/imgui.cpp:2400`
- **138 `compress`**　/kəmˈpres/　*v.*　压缩　【20 次 / 4 个文件】
    - **用法**：`compress the data` = 压缩数据。与 decompress 成对。
    - **频次**：标识符 16 次 · 注释 4 次
    - **例句**：`// Default font TTF is compressed with stb_compress then base85 encoded (see misc/fonts/binary_to_compressed_c.cpp for encoder)`
    - **中文翻译**：默认字体 TTF 先用 stb_compress 压缩，再做 base85 编码（编码器见 misc/fonts/binary_to_compressed_c.cpp）。
    - **位置**：`jni/src/ImGui/imgui_draw.cpp:3156`
- **139 `contain`**　/kənˈteɪn/　*v.*　包含　【20 次 / 8 个文件】
    - **用法**：`contains the address` = 包含该地址。范围判断用。
    - **频次**：标识符 3 次 · 注释 17 次
    - **例句**：`// - A tooltip window can contain items of any types.`
    - **中文翻译**：工具提示（tooltip）窗口可以包含任意类型的控件。
    - **位置**：`jni/include/ImGui/imgui.h:823`
- **140 `validate`**　/ˈvælɪdeɪt/　*v.*　校验　【20 次 / 6 个文件】
    - **用法**：`validate the pointer` = 校验指针合法性（第 89 章）。
    - **频次**：标识符 10 次 · 注释 10 次
    - **例句**：`// After the last step: Let the clipper validate that we have reached the expected Y position (corresponding to element DisplayEnd),`
    - **中文翻译**：在最后一步之后：让裁剪器（clipper）校验我们已经到达预期的 Y 位置（对应元素的 DisplayEnd），
    - **位置**：`jni/src/ImGui/imgui.cpp:3573`
- **141 `execute`**　/ˈeksɪkjuːt/　*v.*　执行　【19 次 / 7 个文件】
    - **用法**：`execute the command buffer` = 执行命令缓冲（第 58 章）。
    - **频次**：标识符 13 次 · 注释 6 次
    - **例句**：`// Helper: Execute a block of code at maximum once a frame. Convenient if you want to quickly create a UI within deep-nested code that runs multiple times every frame.`
    - **中文翻译**：辅助函数：每帧最多执行一次某段代码。当你想在每帧跑很多次的深层嵌套代码里快速建一个 UI 时很方便。
    - **位置**：`jni/include/ImGui/imgui.h:2758`
- **142 `generate`**　/ˈdʒenəreɪt/　*v.*　生成　【19 次 / 7 个文件】
    - **用法**：`generate the mesh` = 生成网格（题 8.6）。
    - **频次**：标识符 1 次 · 注释 18 次
    - **例句**：`// Helpers macros to generate 32-bit encoded colors`
    - **中文翻译**：用于生成 32 位编码颜色的辅助宏。
    - **位置**：`jni/include/ImGui/imgui.h:2981`
- **143 `interpolate`**　/ɪnˈtɜːpəleɪt/　*v.*　插值　【19 次 / 4 个文件】
    - **用法**：`interpolate between two frames` = 两帧之间插值。Triangle 的 u/v 就是插值权重。
    - **频次**：标识符 12 次 · 注释 7 次
    - **例句**：`// SetRange requests will give you two end-points and you will need to iterate/interpolate between them to update your selection.`
    - **中文翻译**：SetRange 请求会给你两个端点，你需要在它们之间迭代/插值来更新你的选择范围。
    - **位置**：`jni/include/ImGui/imgui.h:3057`
- **144 `invalidate`**　/ɪnˈvælɪdeɪt/　*v.*　使失效　【19 次 / 8 个文件】
    - **用法**：`invalidate the cache` = 让缓存失效（第 99 章）。与 refresh 配合。
    - **频次**：标识符 7 次 · 注释 12 次
    - **例句**：`//---- Use legacy CRC32-adler tables (used before 1.91.6), in order to preserve old .ini data that you cannot afford to invalidate.`
    - **中文翻译**：使用旧的 CRC32-adler 表（1.91.6 之前使用），以保留你无法承受其失效的旧 .ini 数据。
    - **位置**：`jni/include/ImGui/imconfig.h:69`
- **145 `represent`**　/ˌreprɪˈzent/　*v.*　表示、代表　【19 次 / 5 个文件】
    - **用法**：`represents the depth` = 表示深度值。讲解数据含义时用。
    - **频次**：标识符 0 次 · 注释 19 次
    - **例句**：`// - In the future we will extend this concept further to also represent Platform Monitor and support a "no main platform window" operation mode.`
    - **中文翻译**：将来我们会进一步扩展这个概念，使其也能表示平台显示器，并支持「无主平台窗口」的运行模式。
    - **位置**：`jni/include/ImGui/imgui.h:1035`
- **146 `ceil`**　/siːl/　*v.*　向上取整　【18 次 / 7 个文件】
    - **用法**：`std::ceil` = 向上取整。与 floor 成对。
    - **频次**：标识符 15 次 · 注释 3 次
    - **例句**：`// N = ceil ( pi / acos(1 - error / r) ) where r > 0, error <= r`
    - **中文翻译**：N = ceil( π / acos(1 - error / r) )，其中 r > 0 且 error <= r。
    - **位置**：`jni/include/ImGui/imgui_internal.h:864`
- **147 `expect`**　/ɪkˈspekt/　*v.*　预期　【18 次 / 5 个文件】
    - **用法**：`expected value` = 期望值（测试断言）；`as expected` = 如预期。
    - **频次**：标识符 0 次 · 注释 18 次
    - **例句**：`// We intentionally do not provide an ImTextureRef constructor for this: we don't expect this`
    - **中文翻译**：我们故意不为这个提供 ImTextureRef 构造函数：我们不期望……
    - **位置**：`jni/include/ImGui/imgui.h:359`
- **148 `notify`**　/ˈnəʊtɪfaɪ/　*v.*　通知　【17 次 / 6 个文件】
    - **用法**：`notify the UI thread` = 通知界面线程刷新。
    - **频次**：标识符 5 次 · 注释 12 次
    - **例句**：`// - Compiling with NDEBUG will usually strip out assert() to nothing, which is NOT recommended because we use asserts to notify of programmer mistakes.`
    - **中文翻译**：用 NDEBUG 编译通常会把 assert() 完全去掉，这并不推荐，因为我们靠断言来提示程序员的错误。
    - **位置**：`jni/include/ImGui/imconfig.h:19`
- **149 `project`**　/prəˈdʒekt/　*v./n.*　投影；项目　【17 次 / 6 个文件】
    - **用法**：`project the point` = 把点投影到屏幕（第 51 章）。名词义是“项目”。
    - **频次**：标识符 4 次 · 注释 13 次
    - **例句**：`// B) or '#define IMGUI_USER_CONFIG "my_imgui_config.h"' in your project and then add directives in your own file without touching this template.`
    - **中文翻译**：B）或者在你的项目里 '#define IMGUI_USER_CONFIG "my_imgui_config.h"'，然后在你自己的文件里加指令，而不动这个模板。
    - **位置**：`jni/include/ImGui/imconfig.h:7`
- **150 `trim`**　/trɪm/　*v.*　裁剪、去空白　【17 次 / 5 个文件】
    - **用法**：`trim the string` = 去掉首尾空格。
    - **频次**：标识符 9 次 · 注释 8 次
    - **例句**：`// Trim str by offsetting contents when there's leading data + writing a \0 at the trailing position. We use this in situation where the cost is negligible.`
    - **中文翻译**：当有前置数据时，通过偏移内容并在末尾写一个 \0 来裁剪字符串。我们在开销可忽略的场景下用这个办法。
    - **位置**：`jni/src/ImGui/imgui.cpp:2310`


---

## 第 4 组

- **151 `unlock`**　/ˌʌnˈlɒk/　*v.*　解锁　【17 次 / 6 个文件】
    - **用法**：`unlock the mutex` = 释放锁。与 lock 成对。
    - **频次**：标识符 8 次 · 注释 9 次
    - **例句**：`// Unlock font atlas`
    - **中文翻译**：解锁字体图集。
    - **位置**：`jni/src/ImGui/imgui.cpp:6181`
- **152 `consume`**　/kənˈsjuːm/　*v.*　消耗、消费　【16 次 / 5 个文件】
    - **用法**：`consume the input` = 读掉输入。队列里 consumer 是消费者。
    - **频次**：标识符 0 次 · 注释 16 次
    - **例句**：`// Invalid or incomplete input may consume less bytes than wanted, therefore every byte has to be inspected in s.`
    - **中文翻译**：无效或不完整的输入可能消耗的字节少于预期，因此必须逐个检查 s 中的每个字节。
    - **位置**：`jni/src/ImGui/imgui.cpp:2700`
- **153 `import`**　/ɪmˈpɔːt/　*v.*　导入　【16 次 / 5 个文件】
    - **用法**：`import symbols` = 导入符号（第 20 章 GOT 表）。与 export 成对。
    - **频次**：标识符 14 次 · 注释 2 次
    - **例句**：`//#define IMGUI_API __declspec(dllimport) // MSVC Windows: DLL import`
    - **中文翻译**：宏定义 IMGUI_API：__declspec(dllimport)（MSVC Windows：DLL 导入）。
    - **位置**：`jni/include/ImGui/imconfig.h:28`
- **154 `iterate`**　/ˈɪtəreɪt/　*v.*　遍历、迭代　【16 次 / 5 个文件】
    - **用法**：`iterate over the array` = 遍历数组（第 84 章）。
    - **频次**：标识符 0 次 · 注释 16 次
    - **例句**：`// [Internal] If you need to iterate all keys (for e.g. an input mapper) you may use ImGuiKey_NamedKey_BEGIN..ImGuiKey_NamedKey_END.`
    - **中文翻译**：[内部] 如果你需要遍历所有按键（例如做输入映射器），可以用 ImGuiKey_NamedKey_BEGIN..ImGuiKey_NamedKey_END。
    - **位置**：`jni/include/ImGui/imgui.h:1670`
- **155 `become`**　/bɪˈkʌm/　*v.*　变成　【15 次 / 7 个文件】
    - **用法**：`becomes invalid` = 变得无效。生命周期注释常用。
    - **频次**：标识符 0 次 · 注释 15 次
    - **例句**：`// If you specify a value for 'inner_width' then effectively the scrolling space is known and Stretch or mixed Fixed/Stretch columns become meaningful again.`
    - **中文翻译**：如果你给 'inner_width' 指定一个值，滚动空间就基本确定了，此时「拉伸」列或「固定/拉伸混合」列才又有意义。
    - **位置**：`jni/include/ImGui/imgui.h:2054`
- **156 `flush`**　/flʌʃ/　*v.*　刷新、清空缓冲　【14 次 / 6 个文件】
    - **用法**：`fflush` = 把缓冲写出去。`vkQueueWaitIdle` 类似语义。
    - **频次**：标识符 13 次 · 注释 1 次
    - **例句**：`// flush to end of line`
    - **中文翻译**：冲刷到行末。
    - **位置**：`jni/src/ImGui/stb_image.cpp:6576`
- **157 `poll`**　/pəʊl/　*v.*　轮询　【14 次 / 7 个文件】
    - **用法**：`poll()` 系统调用；`polling loop` = 反复查询。比阻塞等待费 CPU。
    - **频次**：标识符 8 次 · 注释 6 次
    - **例句**：`// but you will not get a return value from Shortcut() due to ActiveIdUsingAllKeyboardKeys logic. You can however poll IsKeyPressed(ImGuiKey_Escape) afterwards.`
    - **中文翻译**：但你不会从 Shortcut() 拿到返回值（因为 ActiveIdUsingAllKeyboardKeys 的逻辑）。不过之后你可以用 IsKeyPressed(ImGuiKey_Escape) 轮询。
    - **位置**：`jni/src/ImGui/imgui.cpp:5768`
- **158 `sanitize`**　/ˈsænɪtaɪz/　*v.*　净化、洗净输入　【14 次 / 4 个文件】
    - **用法**：`sanitize the input` = 把不可信数据处理干净再用。
    - **频次**：标识符 9 次 · 注释 5 次
    - **例句**：`// We record the number of allocation in recent frames, as a way to audit/sanitize our guiding principles of "no allocations on idle/repeating frames"`
    - **中文翻译**：我们记录最近几帧的分配次数，以此审视/校验我们「空闲帧和重复帧里不做分配」的指导原则。
    - **位置**：`jni/src/ImGui/imgui.cpp:5205`
- **159 `sleep`**　/sliːp/　*v.*　睡眠（让出 CPU）　【14 次 / 2 个文件】
    - **用法**：`usleep(1000)` = 睡 1 毫秒。轮询循环里降低 CPU 占用。
    - **频次**：标识符 13 次 · 注释 1 次
    - **例句**：`// 记录更新时间点，防止 sleep 精度误差积累`
    - **中文翻译**：记录更新时间点，防止 sleep 的精度误差累积。
    - **位置**：`jni/include/Embree/PhysX.h:1700`
- **160 `continue`**　/kənˈtɪnjuː/　*v.*　继续　【13 次 / 6 个文件】
    - **用法**：C++ 关键字 `continue` = 跳过本次循环剩余部分。
    - **频次**：标识符 0 次 · 注释 13 次
    - **例句**：`// - A typical use case where this is convenient for quick hacking (e.g. add storage during a live Edit&Continue session if you can't modify existing struct)`
    - **中文翻译**：一个典型场景是临时快速改代码（比如在不方便修改已有结构体的情况下，借助「编辑并继续」实时加一块存储空间）。
    - **位置**：`jni/include/ImGui/imgui.h:2853`
- **161 `obtain`**　/əbˈteɪn/　*v.*　获得　【13 次 / 6 个文件】
    - **用法**：`obtain the base address` = 取得基址。
    - **频次**：标识符 0 次 · 注释 13 次
    - **例句**：`// - Fixed Columns (if any) will generally obtain their requested width (unless the table cannot fit them all).`
    - **中文翻译**：固定列（如果有）一般能获得它们请求的宽度（除非表格无法容纳全部列）。
    - **位置**：`jni/include/ImGui/imgui.h:2043`
- **162 `produce`**　/prəˈdjuːs/　*v.*　产生、生产　【13 次 / 5 个文件】
    - **用法**：`produce the output` = 产出结果。队列里 producer 是生产者。
    - **频次**：标识符 0 次 · 注释 13 次
    - **例句**：`// Rendering circles with an odd number of segments, while mathematically correct will produce`
    - **中文翻译**：用奇数个分段来渲染圆，虽然在数学上正确，但会产生……
    - **位置**：`jni/include/ImGui/imgui_internal.h:869`
- **163 `receive`**　/rɪˈsiːv/　*v.*　接收　【13 次 / 6 个文件】
    - **用法**：`recv` 是缩写。与 send 成对（第 79 章）。
    - **频次**：标识符 5 次 · 注释 8 次
    - **例句**：`// (The implicit fallback window is NOT automatically ended allowing it to always be able to receive commands without crashing)`
    - **中文翻译**：隐式的兜底窗口不会被自动结束，从而让它始终能接收指令而不崩溃。
    - **位置**：`jni/src/ImGui/imgui.cpp:8328`
- **164 `translate`**　/trænsˈleɪt/　*v.*　平移；翻译　【13 次 / 5 个文件】
    - **用法**：`translate the point` = 平移坐标（第 46 章）。注意不是 rotate。
    - **频次**：标识符 11 次 · 注释 2 次
    - **例句**：`// again, these can translate into one instruction, but often don't.`
    - **中文翻译**：再说一遍，这些可以被编译成一条指令，但通常不会。
    - **位置**：`jni/src/ImGui/stb_image.cpp:2319`
- **165 `determine`**　/dɪˈtɜːmɪn/　*v.*　确定、判定　【12 次 / 7 个文件】
    - **用法**：`determine the size` = 确定大小。
    - **频次**：标识符 0 次 · 注释 12 次
    - **例句**：`// Result of layout query, used by stb_textedit to determine where`
    - **中文翻译**：布局查询的结果，被 stb_textedit 用来确定……
    - **位置**：`jni/include/ImGui/imstb_textedit.h:385`
- **166 `encode`**　/ɪnˈkəʊd/　*v.*　编码　【12 次 / 5 个文件】
    - **用法**：`UTF-8 encoded` = 以 UTF-8 编码（中文字体必踩坑，第 63 章）。
    - **频次**：标识符 7 次 · 注释 5 次
    - **例句**：`// You can encode other things, such as CONTROL or ALT, in additional bits, and`
    - **中文翻译**：你还可以把其他内容（比如 CONTROL 或 ALT）编码到额外的位里，并且……
    - **位置**：`jni/include/ImGui/imstb_textedit.h:197`
- **167 `rebuild`**　/ˌriːˈbɪld/　*v.*　重建　【12 次 / 7 个文件】
    - **用法**：`rebuild the BVH` = 重新构建加速结构（第 99 章）。全量重建很慢。
    - **频次**：标识符 8 次 · 注释 4 次
    - **例句**：`// Advanced: for quicker full rebuild of a storage (instead of an incremental one), you may add all your contents and then sort once.`
    - **中文翻译**：进阶用法：为了让存储做一次更快的「全量重建」（而不是增量重建），你可以先加入全部内容，然后一次性排序。
    - **位置**：`jni/include/ImGui/imgui.h:2860`
- **168 `reverse`**　/rɪˈvɜːs/　*v./adj.*　反转；反向的　【12 次 / 4 个文件】
    - **用法**：`reverse the bytes` = 反转字节序（第 24 章）。`reverse engineering` = 逆向工程。
    - **频次**：标识符 8 次 · 注释 4 次
    - **例句**：`// Store current path (in reverse order)`
    - **中文翻译**：存储当前路径（逆序）。
    - **位置**：`jni/src/ImGui/imgui.cpp:8935`
- **169 `verify`**　/ˈverɪfaɪ/　*v.*　验证（是否正确）　【12 次 / 4 个文件】
    - **用法**：`verify the result` = 核对结果对不对。与 validate（是否合法）区分。
    - **频次**：标识符 0 次 · 注释 12 次
    - **例句**：`// Call IMGUI_CHECKVERSION() from your .cpp file to verify that the data structures your files are using are matching the ones imgui.cpp is using.`
    - **中文翻译**：在你的 .cpp 文件里调用 IMGUI_CHECKVERSION()，以确认你的文件所用的数据结构与 imgui.cpp 使用的版本一致。
    - **位置**：`jni/include/ImGui/imconfig.h:12`
- **170 `send`**　/send/　*v.*　发送　【11 次 / 4 个文件】
    - **用法**：`send the request` = 发请求。与 receive 成对。
    - **频次**：标识符 0 次 · 注释 11 次
    - **例句**：`// call this to directly send text input the textfield, which is required`
    - **中文翻译**：调用这个函数，直接向文本框发送文本输入，这是必须的
    - **位置**：`jni/include/ImGui/imstb_textedit.h:265`
- **171 `catch`**　/kætʃ/　*v.*　捕获　【10 次 / 7 个文件】
    - **用法**：`catch` 捕获异常。也能捕获（发现）错误：“catch the bug”。
    - **频次**：标识符 0 次 · 注释 10 次
    - **例句**：`// Otherwise it would severely hinder your ability to catch and correct mistakes!`
    - **中文翻译**：否则会严重妨碍你发现和纠正错误！
    - **位置**：`jni/include/ImGui/imgui.h:2506`
- **172 `connect`**　/kəˈnekt/　*v.*　连接　【10 次 / 2 个文件】
    - **用法**：`connect to the server` = 连服务端。
    - **频次**：标识符 9 次 · 注释 1 次
    - **例句**：`IMGUI_API ImGuiPlatformIO& GetPlatformIO(); // access the ImGuiPlatformIO structure (mostly hooks/functions to connect to platform/renderer and OS Clipboard, IME etc.)`
    - **中文翻译**：IMGUI_API 函数：访问 ImGuiPlatformIO 结构体（主要是连接平台/渲染器以及操作系统剪贴板、输入法等所需的钩子与函数）。
    - **位置**：`jni/include/ImGui/imgui.h:401`
- **173 `exit`**　/ˈeksɪt/　*v./n.*　退出　【10 次 / 5 个文件】
    - **用法**：`exit(0)` = 正常退出。与 return 的区别：exit 直接终止整个进程。
    - **频次**：标识符 5 次 · 注释 5 次
    - **例句**：`// - either to move most/all of those tests to the epilogue/end functions of the scope they are dealing with (e.g. exit child window in EndChild()) or in EndFrame(), to allow an earlier intercept`
    - **中文翻译**：要么把这些测试中的大部分/全部挪到它们所处理作用域的收尾函数里（例如在 EndChild() 中退出子窗口），要么挪到 EndFrame() 里，以便更早进行拦截。
    - **位置**：`jni/src/ImGui/imgui.cpp:14487`
- **174 `exist`**　/ɪɡˈzɪst/　*v.*　存在　【9 次 / 8 个文件】
    - **用法**：`if it exists` = 如果存在。文件/驱动探测常用。
    - **频次**：标识符 1 次 · 注释 8 次
    - **例句**：`// Those functions only exist because:`
    - **中文翻译**：这些函数之所以存在，是因为：
    - **位置**：`jni/include/ImGui/imgui_impl_vulkan.h:186`
- **175 `slice`**　/slaɪs/　*v./n.*　切片　【9 次 / 3 个文件】
    - **用法**：`slice the array` = 取数组一段。GPU 里也指“切片”概念。
    - **频次**：标识符 8 次 · 注释 1 次
    - **例句**：`// Write each slice`
    - **中文翻译**：写出每一片（slice）。
    - **位置**：`jni/src/ImGui/imgui_draw.cpp:3671`
- **176 `yield`**　/jiːld/　*v.*　让出（CPU）；产出　【8 次 / 4 个文件】
    - **用法**：`sched_yield()` = 让出时间片（第 82 章）。自旋锁里用它降 CPU 占用。
    - **频次**：标识符 4 次 · 注释 4 次
    - **例句**：`// Issue X86 PAUSE or ARM YIELD instruction to reduce contention between`
    - **中文翻译**：发出 X86 的 PAUSE 或 ARM 的 YIELD 指令，以降低……之间的争用。
    - **位置**：`jni/include/ImGui/spinlock.h:16`

