---
tags: [教程, 卷八, 实战, 毕业]
day: 60
aliases: [ch100]
---

# 第 100 章 · 综合实战：拼出你的覆盖层

> [!abstract] 本章目标
> 把八卷所学组装成一个完整的程序，并回顾整条学习路径。
> **这是总纲承诺的第 6 个成果，也是本教程的终点。**

> [!note] 承上
> 八卷 99 章，你学到了：C/C++ → 系统 → Android → 3D 数学 → 图形 → 跨进程内存 → 引擎数据 → 物理可见性。
> 本章把它们**组装成一个完整程序** `coverlay`——总纲承诺的第 6 个成果，也是本教程的终点。

## 先看你要造的东西

```
┌─────────────────────────────────────────────┐
│  你的程序（coverlay，一个可执行文件）           │
│                                              │
│  ① 创建透明图层      SurfaceFlinger           │  卷五
│  ② 画菜单和图形      ImGui + Vulkan           │  卷五
│  ③ 读目标进程数据    process_vm_readv / 驱动   │  卷六
│  ④ 解析成有意义的对象  Actor / 骨骼 / 名字      │  卷七
│  ⑤ 算屏幕坐标        W2S 投影                 │  卷四
│  ⑥ 判断遮挡          Embree 光线投射           │  卷八
│  ⑦ 画到屏幕上        方框 / 骨骼 / 射线         │  卷五
│  ⑧ 接收触摸          触摸转发                  │  卷五
└─────────────────────────────────────────────┘
```

**每一块你都已经学过，并且都动手做过。**

## 最终项目结构

```
coverlay/
├── .vscode/
│   ├── settings.json
│   ├── c_cpp_properties.json
│   └── launch.json
├── jni/
│   ├── Android.mk
│   ├── Application.mk
│   ├── include/
│   │   ├── Math/mathlib.h              ← 卷四（第53章）
│   │   ├── Math/test_framework.h       ← 卷四（第54章）
│   │   ├── Memory/MemReader.h          ← 卷六（第75章）
│   │   ├── Memory/MemDriver.h          ← 卷六（第80章）
│   │   ├── Memory/SysHal.h             ← 卷六（第79章）
│   │   ├── Memory/RegionCache.h        ← 卷七（第89章）
│   │   ├── Engine/Offsets.h            ← 卷七（第92章）
│   │   ├── Engine/Actor.h              ← 卷七（第83章）
│   │   ├── Engine/FName.h              ← 卷七（第85章）
│   │   ├── Engine/Skeleton.h           ← 卷七（第86章）
│   │   ├── Render/VulkanGraphics.h     ← 卷五（第58章）
│   │   ├── Render/WindowCreator.h      ← 卷五（第65章）
│   │   ├── Render/TouchHelper.h        ← 卷五（第70章）
│   │   ├── Visibility/PhysicsScene.h   ← 卷八（第98章）
│   │   ├── Config/ConfigManager.h      ← 卷三（第38章配套）
│   │   └── ThirdParty/
│   │       ├── ImGui/                  ← 卷五（第42章）
│   │       ├── Embree/                 ← 卷五（第43章）
│   │       └── Vulkan/                 ← 卷五（第60章）
│   └── src/
│       ├── main.cpp                    ← 主循环
│       ├── Math/                       ← 卷四
│       ├── Memory/                     ← 卷六
│       ├── Engine/                     ← 卷七
│       ├── Render/                     ← 卷五
│       ├── Visibility/                 ← 卷八
│       ├── Config/                     ← 卷三
│       └── Assets/heiti_ttf.cpp        ← 卷五（第63章）
├── tests/
│   └── mathlib_test.cpp                ← 卷四（第54章）
├── scripts/
│   ├── build.sh                        ← 卷三（第35章）
│   └── run.sh
├── docs/
│   ├── OFFSETS.md                      ← 卷七（第92章）
│   └── CHANGELOG.md
├── libs/                               ← 产物（gitignore）
└── obj/                                ← 中间产物（gitignore）
```

## 主循环的最终形态

```cpp
int main(int argc, char *argv[]) {
    // ===== 初始化 =====
    ConfigManager::LoadConfig();                    // 卷三：读配置

    graphics = GraphicsManager::getGraphicsInterface();   // 卷五：Vulkan 后端

    screen_config();                                     // 卷五：屏幕信息
    native_window_screen_x = displayInfo.width;
    native_window_screen_y = displayInfo.height;
    abs_ScreenX = displayInfo.width;
    abs_ScreenY = displayInfo.height;

    window = ANativeWindowCreator::Create(                // 卷五：建图层
        "overlay", native_window_screen_x, native_window_screen_y);
    graphics->Init_Render(window, native_window_screen_x, native_window_screen_y);

    Touch::Init({(float)abs_ScreenX, (float)abs_ScreenY}, true);   // 卷五
    Touch::setOrientation(displayInfo.orientation);

    init_My_drawdata();                                  // 卷五：字体与样式

    // ===== 主循环 =====
    bool running = true;
    while (running) {
        drawBegin();                                     // 卷五：帧率+尺寸
        Touch::UpdateImGuiInput();                       // 卷五：触摸
        graphics->NewFrame(true);

        Layout_tick_UI(&running);                        // 卷五：菜单
        UpdateGameData();                                // 卷七：读数据
        UpdateCamera();                                  // 卷七：相机
        BuildProjectionMatrix();                         // 卷四：投影

        auto *draw = ImGui::GetForegroundDrawList();
        DrawPlayers(draw);                               // 卷四+卷七：ESP
        DrawPhysicsWireframe(draw);                      // 卷八：可见几何

        graphics->EndFrame();                            // 卷五：提交
    }

    // ===== 清理 =====
    Touch::Close();
    graphics->Shutdown();
    ANativeWindowCreator::Destroy(window);
    ConfigManager::SaveConfig();
    return 0;
}
```

## 核心绘制流程（串起所有卷）

```cpp
void DrawPlayers(ImDrawList *draw) {
    // ① 一次性读完整个 Actor 数组          卷六（批量读）
    std::vector<uint64_t> actors(count);
    dr->Read(arrayAddr, actors.data(), count * 8);

    // ② 批量读每个对象的字段              卷六
    ReadBatch(dr, requests);

    for (auto &snapshot : snapshots) {
        // ③ 合法性过滤                    卷七（第89章）
        if (!IsValidObject(snapshot.address)) continue;

        // ④ 名字解析 + 类型过滤            卷七（第85章）
        std::string name = GetNameById(snapshot.nameId);
        if (name.find("BP_Character") == npos) continue;

        // ⑤ 世界坐标 → 屏幕坐标            卷四（第51章）
        Vec2 screen;
        if (!WorldToScreen(snapshot.position, screen)) continue;

        // ⑥ 遮挡判定                      卷八（第95章）
        bool occluded = physicsReady && IsOccluded(camPos, aimPoint);

        // ⑦ 画方框                        卷五（第61章）
        draw->AddRect(min, max, occluded ? GREEN : RED);

        // ⑧ 画骨骼                        卷七（第86章）
        if (showBones) {
            for (int seg = 0; seg < 14; seg++)
                draw->AddLine(bones[segA[seg]], bones[segB[seg]], color);
        }
    }
}
```

**八行代码，串起八卷。**

> [!important] 本章怎么用：别只读，动手拼
> 这是毕业章，**不是让你读完，是让你拼出来**。给你一个 6 天的节奏：
>
> | 天 | 做什么 | 对照节 |
> |---|---|---|
> | 第 1 天 | 按「最终项目结构」把目录建好，文件先留空 | 「最终项目结构」 |
> | 第 2-3 天 | 按「主循环的最终形态」写 main，每加一块**编译一次** | 「主循环的最终形态」 |
> | 第 4-5 天 | 按「核心绘制流程」串起：读数据 → 算坐标 → 画线 | 「核心绘制流程」 |
> | 第 6 天 | 跑「毕业项目验收清单」的 6 项，缺哪项补哪项 | 「毕业项目验收清单」 |
>
> **卡住时**：回对应卷的"动手"任务（附录 Q 有全部 100 项清单）。
> **不要一次写完再调试**——每加一块就编译运行，出错时才知道是哪一块的问题。

## 分阶段综合验收表

> [!important] 和下面的"毕业项目验收清单"有什么不同
> 下面那份按**知识维度**（卷一~卷八各验什么）；这份按**开发阶段**——
> 每阶段有产出物、通过标准、前置依赖。**必须前一阶段全绿才能进下一阶段**，
> 否则会陷入"一次写完几百行，调试不知从哪查"的泥潭（本章反复强调的"每加一块编译一次"）。

### 阶段依赖关系

```
阶段0 环境就绪
   │
阶段1 渲染骨架（透明图层+ImGui）
   │
阶段2 数据读取（MemReader+IDriver）
   │
阶段3 对象解析（Actor+名字+骨骼）
   │
阶段4 投影绘制（W2S+ESP方框）
   │
阶段5 遮挡优化（Embree+增量）
   │
阶段6 打磨验收（性能+健壮性）
```

### 阶段 0 · 环境就绪（约 0.5 天）

| 验收项 | 验证命令 | 通过标准 | 回查 |
|---|---|---|---|
| NDK 可用 | `$NDK/ndk-build --version` | 打印版本 | 33 |
| clang 可用 | `aarch64-linux-android21-clang --version` | 打印版本 | 33 |
| 设备连接 | `adb devices -l` | 状态为 `device` | 36 |
| 目录骨架 | `ls coverlay/jni/{include,src}` | 六层目录都在 | 44 |

**阶段产物**：`coverlay/` 目录树 + 能跑通一个 `hello_arm64`

### 阶段 1 · 渲染骨架（约 1.5 天）

| 验收项 | 验证命令 | 通过标准 | 回查 |
|---|---|---|---|
| 编出 overlay | `ndk-build` | 产物在 `libs/arm64-v8a/` | 33 |
| 透明图层可见 | `dumpsys SurfaceFlinger \| grep overlay` | 看到你的图层名 | 64 |
| 纯色清屏 | 运行程序 | 屏幕蒙一层色（alpha 生效） | 62 |
| ImGui 帧 | 加 `ImGui::Text("hi")` | 屏幕上出现文字 | 61 |
| 中文正常 | 加一行中文 | 不是方块 | 63 |
| 触摸可用 | 点一个按钮 | 按钮响应 | 69-70 |

**阶段产物**：能在屏幕上显示透明覆盖层 + 中文菜单 + 可点击

> [!warning] 这阶段最容易卡在图层不可见
> 先不加任何数据逻辑——**只要能看到纯色蒙层就算过**。
> 图层不出现时，99% 是交换链/像素格式问题（第 59、64 章），不是渲染逻辑。

### 阶段 2 · 数据读取（约 1 天）

| 验收项 | 验证命令 | 通过标准 | 回查 |
|---|---|---|---|
| MemReader 可用 | 单元测试读 demo | 读出真值 | 75 |
| 模块基址 | `./memview <pid>` | 列出目标模块 + 基址 | 76 |
| 后端可切换 | 切 MemDriver/Driver | 业务代码零改动 | 80 |
| 批量读生效 | 计时对比 | 耗时明显下降 | 81 |

**阶段产物**：一个能稳定读出目标进程任意字段的读取层

> [!tip] 先拿自己写的 demo 当靶子
> 不要一上来就读真实游戏——用第 83 章的 demo 引擎，
> 你知道每个字段的真值，读错了能立刻发现。

### 阶段 3 · 对象解析（约 1 天）

| 验收项 | 验证命令 | 通过标准 | 回查 |
|---|---|---|---|
| 遍历对象数组 | 打印 count 个对象 | 数量与真值一致 | 84 |
| 名字解析 | 打印对象名 | 不是乱码 | 85 |
| 骨骼世界坐标 | 打印头部骨骼坐标 | 与真值一致 | 86 |
| 偏移自检 | 故意填错偏移 | 明确报错不崩 | 88/92 |

**阶段产物**：能从内存解析出"第 N 个对象叫什么、头在哪"

### 阶段 4 · 投影与绘制（约 1 天）

| 验收项 | 验证命令 | 通过标准 | 回查 |
|---|---|---|---|
| W2S 正确 | `w2s_demo` 七项 | 全绿 | 51 |
| ESP 方框 | 屏幕上看到框 | 框住目标 | 51+84 |
| 骨骼连线 | 开 showBones | 14 段连线正确 | 86 |
| 后方不画 | 目标转到身后 | 框消失 | 52 |

**阶段产物**：**第一次看到"活的"ESP**——这阶段过了一般就成功一半了

### 阶段 5 · 遮挡与优化（约 1 天）

| 验收项 | 验证命令 | 通过标准 | 回查 |
|---|---|---|---|
| 重建三角形 | 打印顶点/索引数 | 8 顶点 36 索引 | 98 |
| Embree 建场景 | 打印 commit 结果 | 无错误 | 97 |
| 遮挡判定 | 三个测试用例 | 被挡/没挡/擦边都对 | 95 |
| 增量更新 | 计时对比 | 加速比 > 2 | 99 |

**阶段产物**：透过墙的目标变绿、被挡的变红；每帧开销下降

> [!note] 这阶段可以先跳过
> 遮挡是"锦上添花"。如果时间紧，先完成阶段 6 拿一个能跑的成品，再回头补。

### 阶段 6 · 打磨验收（约 0.5 天）

| 验收项 | 验证命令 | 通过标准 | 回查 |
|---|---|---|---|
| 稳定 60fps | 性能面板 | 帧时间 < 16ms | 71 |
| 无内存泄漏 | 反复开关菜单 | 内存不涨 | 72 |
| 异常不崩 | 杀目标进程 | 程序优雅降级 | 89 |
| 配置持久化 | 改设置重启 | 设置保留 | 38 |
| 一键运行 | `./run.sh` | 编译→推送→运行一条龙 | 35 |

**阶段产物**：`coverlay` 完整成品

### 阶段回退规则

> [!danger] 卡住时先回退，别硬扛
> - **图层不可见** → 回阶段 1，只留纯色清屏，确认图层本身没问题
> - **数据读出来是垃圾** → 回阶段 2，用 demo 进程验证每一步读取
> - **框位置飘** → 回阶段 4，先只画 1 个固定世界点的框，验证 W2S
> - **崩溃但不知在哪** → 用 `addr2line` 还原（第 30 章），再回对应阶段
> - **每加一块都编译运行**——这是本章最重要的一条纪律。

## 毕业项目验收清单

逐项打勾，全绿才算完成。这份清单**汇总了全书 100 章的关键能力**（相当于附录 Q 的 M6 里程碑）：

### 基础（卷一~卷三）
- [ ] 环境搭好，`ndk-build` 能编译
- [ ] 产物能 push 到手机并运行
- [ ] 日志能分级输出，可控开关
- [ ] 配置能持久化到文件

### 数学（卷四）
- [ ] `mathlib` 完成，测试全绿
- [ ] `w2s_demo` 七项用例结果符合预期
- [ ] 理解每一处除零/NaN 防护（指出代码里所有 `w < 0.001` 和 `isfinite` 检查的位置）

### 图形（卷五）
- [ ] `overlay` 能在屏幕上显示透明图层
- [ ] `dumpsys SurfaceFlinger` 能看到你的图层
- [ ] 中文正常显示
- [ ] 菜单能点击（触摸转发工作）
- [ ] 稳定 60fps，性能面板显示正常

### 内存（卷六）
- [ ] `memview` 能列出模块并找到基址
- [ ] 能读出 demo 进程的数据并与真值一致
- [ ] `IDriver` 双后端可切换
- [ ] 批量读优化生效（实测耗时下降）

### 数据（卷七）
- [ ] demo 引擎跑起来
- [ ] 能遍历对象数组
- [ ] 能解析名字
- [ ] 能读骨骼并验证坐标正确
- [ ] 偏移集中在 `offsets.h`，有启动自检

### 可见性（卷八）
- [ ] 能从碰撞体重建三角形
- [ ] Embree 场景构建成功
- [ ] 遮挡判定正确（三个测试用例）
- [ ] 增量更新生效（实测加速比）

## 八卷回顾

| 卷 | 章 | 你获得了什么 |
|---|---|---|
| 一 编程地基 | 1-16 | C/C++ 全流程能力：指针、内存、结构体、编译链接、调试 |
| 二 系统与底层 | 17-30 | 操作系统视角：虚拟内存、进程、ELF、系统调用、ARM64、二进制工具 |
| 三 Android 原生 | 31-44 | 交叉编译与部署：NDK、构建脚本、adb、SELinux、第三方库引入 |
| 四 3D 数学 | 45-56 | 空间计算能力：向量矩阵、旋转、投影、W2S、数值稳定 |
| 五 图形与界面 | 57-72 | 渲染与交互：Vulkan、ImGui、图层、触摸、性能 |
| 六 跨进程内存 | 73-82 | 读取能力：系统调用、抽象层、批量优化、并发 |
| 七 引擎数据模型 | 83-92 | 解析能力：对象模型、名字系统、骨骼、偏移方法论、防御体系 |
| 八 物理与可见性 | 93-100 | 判定能力：碰撞体、网格、光线投射、BVH、Embree、增量更新 |

## 六个成品回顾

| # | 产物 | 章 | 证明 |
|---|---|---|---|
| 1 | `hello_arm64` | 33/35 | 交叉编译与部署 |
| 2 | `mathlib` | 53/54 | 3D 数学 + 工程化测试 |
| 3 | `overlay` | 66 | 原生覆盖层框架 |
| 4 | `memview` | 76 | 内存布局分析 |
| 5 | `w2s_demo` | 51 | 投影链路 |
| 6 | `coverlay` | 100 | **全部** |

## 还能往哪走

教程到此结束，但技术没有边界。几个可以深入的方向：

| 方向 | 内容 |
|---|---|
| **性能极致** | SIMD 优化、无锁数据结构、GPU 计算 |
| **更稳的解析** | 特征码自动定位、多版本偏移自动推断 |
| **更好的可视化** | 完整的 3D 线框渲染（带深度）、热力图、雷达 |
| **跨平台** | 把同样的架构移植到其它平台 |
| **正向能力** | 用这套知识做性能剖析工具、自动化测试框架、无障碍辅助 |

> [!tip] 最有价值的下一步
> 把你学到的能力用在**正向**的方向上：
> - 给开源项目贡献性能剖析工具
> - 做游戏引擎的学习型实现（自己写一个 mini 引擎）
> - 做无障碍辅助工具（帮助行动不便的玩家）
> - 做安全研究（在授权范围内）
>
> 这些方向同样需要全部八卷的知识，而且没有法律风险。

## 最后的合规提醒

> [!warning] 请记住这三件事
> 1. **本教程的练习对象始终是你自己写的 demo 程序。**
>    掌握原理后，如何运用取决于你——但请在法律和道德边界内。
> 2. **把技术手段用于未授权的第三方程序**，可能违反用户协议、
>    触犯《刑法》第 285/286 条（破坏计算机信息系统罪）等法律。
> 3. **技术本身是中立的**，操作系统、图形学、编译原理这些知识，
>    在游戏开发、引擎研发、系统编程、安全研究等正当领域都有巨大价值。
>
> 愿你用这些知识造出让自己骄傲的东西。

## 结语

100 章，从"变量是内存里的一块字节"到"从物理数据重建可见几何"。

如果你一路做下来，现在应该能：

- 看着一个程序，知道它在内存里是什么样子
- 看着一个地址，知道怎么一步步解析出有意义的数据
- 看着一个图形问题，知道该用哪些数学工具
- 遇到 bug，有系统的排查方法而不是乱试

**这些能力比任何一个具体的项目都值钱。**

恭喜完成全部课程。

---

> [!success] 下一步
> 1. 回到 [[00-开始之前]]，对照 100 章目录检查哪些还没做
> 2. 补齐 [[附录A-命令速查表]]、[[附录B-术语表]]、[[附录C-排错手册]]、[[附录D-项目源码地图]]
> 3. 把你自己的 `coverlay` 跑起来，然后去造点更好的东西
