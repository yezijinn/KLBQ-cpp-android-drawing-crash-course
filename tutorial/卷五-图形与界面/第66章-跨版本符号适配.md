---
tags: [教程, 卷五, Android, 兼容]
day: 41
aliases: [ch66]
---

# 第 66 章 · 跨版本符号适配

> [!abstract] 本章目标
> 学会"一套代码适配 Android 5~17"的方法。
> **本章产出 `overlay` 成品**（总纲承诺的第 3 个成果）。

> [!important] 这是全书最难的一章，别想一次吃下
> 本章是全教程**环境复杂度最高**的一章（要真机 + root + SurfaceFlinger 私有符号）。建议分两步走：
>
> **第一步：先在你手头这一台设备上跑通**
> - 用本章的"按版本探测"代码，只针对**你这台设备的 Android 版本**做适配
> - 目标：看到屏幕上出现一个透明图层（哪怕只有一个版本能用）
> - **跑通一台，就已经成功了 80%**
>
> **第二步：再扩展适配其它版本**
> - 手头没有的设备不用管，等有设备/用户反馈时再补
> - 维护一张"设备型号 → Android 版本 → 用哪个符号"的兼容表
>
> **不要追求"一次写对 5 个版本"**——那会让你卡很久。**先跑通一个，再逐步扩展。**
>
> 本章难度主要在"环境"（设备、root、符号），不在"代码逻辑"。卡住时先确认：
> 设备 root 了吗？`dumpsys SurfaceFlinger` 能看到图层吗？符号侦察器找到目标符号了吗？

## 先看问题有多严重

同一个函数 `createSurface`，在不同 Android 版本：

| Android | API | 签名 |
|---|---|---|
| 5.0 | 21 | `(name, w, h, format, flags)` |
| 8.0 | 26 | `(name, w, h, format, flags, parentHandle)` |
| 10 | 29 | `(name, w, h, format, flags, parentHandle, windowType, ownerUid)` |
| 11 | 30 | `(name, w, h, format, flags, parentHandle, LayerMetadata)` |
| 12 | 31 | `(..., LayerMetadata)` 参数位置又变了 |

**符号名完全不同，参数个数也不同。**

## 解法：运行时探测 + 备选列表

```cpp
struct SymbolEntry {
    const char *mangled;      // 符号名
    int minApi, maxApi;       // 适用版本范围
};
```

初始化时按当前 API 选择：

```cpp
int GetApiLevel() {
    char buf[PROP_VALUE_MAX];
    __system_property_get("ro.build.version.sdk", buf);
    return atoi(buf);
}
```

然后：

本项目 `ANativeWindowCreator.h` 里真实的版本 → 符号对照（**照抄自项目源码**）：

```cpp
// 构造函数（各版本通用）
// _ZN7android21SurfaceComposerClientC2Ev

// createSurface 随版本变化
if (5 <= ver && ver <= 7)
    "_ZN7android21SurfaceComposerClient13createSurfaceERKNS_7String8Ejjij";                                    // 5-7
else if (ver == 8)
    "_ZN7android21SurfaceComposerClient13createSurfaceERKNS_7String8EjjijPNS_14SurfaceControlEjj";             // 8
else if (ver == 9)
    "_ZN7android21SurfaceComposerClient13createSurfaceERKNS_7String8EjjijPNS_14SurfaceControlEii";             // 9
else if (ver == 10)
    "_ZN7android21SurfaceComposerClient13createSurfaceERKNS_7String8EjjijPNS_14SurfaceControlENS_13LayerMetadataE";        // 10
else if (ver == 11)
    "_ZN7android21SurfaceComposerClient13createSurfaceERKNS_7String8EjjijPNS_14SurfaceControlENS_13LayerMetadataEPj";       // 11
else if (12 <= ver && ver <= 13)
    "_ZN7android21SurfaceComposerClient13createSurfaceERKNS_7String8EjjijRKNS_2spINS_7IBinderEEENS_13LayerMetadataEPj";     // 12-13
else if (14 <= ver && ver <= 16)
    "_ZN7android21SurfaceComposerClient13createSurfaceERKNS_7String8EjjiiRKNS_2spINS_7IBinderEEENS_3gui13LayerMetadataEPj"; // 14-16
else if (17 <= ver)
    "_ZN7android21SurfaceComposerClient13createSurfaceERKNS_7String8EjjiiRKNS_2spINS_7IBinderEEERKNS_3gui13LayerMetadataEPj"; // 17+
```

> [!note] 符号不是"猜"出来的，是从真机 `libgui.so` 里读出来的
> 这张表每一行都是作者在真机上 `nm -D libgui.so | grep createSurface` 确认过的。
> 你换设备时也要这么做（见本章"验证清单"）。
> 注意：**参数类型从 `PNS_14SurfaceControlE` 变成 `RKNS_2spINS_7IBinderEEE`** 的分界点在 Android 12，
> 这是系统 ABI 的真实变化，不是笔误。

**"按版本找 → 找不到就全试"** 是双保险。

> [!note] 为什么不能只靠版本判断
> 因为：
> - 厂商可能修改系统（MIUI、ColorOS 等）
> - Android 版本的 API 号与实际符号不一定严格对应
> - 某些版本有多个重载，`dlsym` 返回哪个不确定
>
> 所以**以 `dlsym` 是否成功为准**，版本只用来排序优先级。

## 需要适配的东西不止符号

| 项目 | 版本差异 |
|---|---|
| **符号名** | 上面说的 |
| **结构体布局** | `String8`、`LayerMetadata`、`sp<T>` |
| **创建标志** | `eFXSurfaceBufferState` / `eFXSurfaceBufferQueue` |
| **窗口类型值** | `441731` 只在特定版本有效 |
| **跳过截图的设置方式** | flag / windowType / LayerMetadata（第 67 章） |

## 结构体布局适配

`String8` 是个典型：

```cpp
// Android 9 之前：内部是 const char*
struct String8_old {
    const char *mString;
};

// Android 10+：内部是 std::string（有 SSO 优化）
struct String8_new {
    char  data[16];      // 短字符串优化缓冲
    size_t size;
    // ... 联合体
};
```

**大小从 8 字节变成 32 字节。** 栈上分配时必须区分：

```cpp
union String8Storage {
    uint8_t  raw[40];          // 取最大值
    uint64_t align;            // 保证对齐
};

String8Storage nameBuf;
memset(&nameBuf, 0, sizeof(nameBuf));

if (api >= 29) {
    // 用新版本的构造函数
    pfnString8_ctor_new(&nameBuf, name);
} else {
    pfnString8_ctor_old(&nameBuf, name);
}
```

> [!danger] 用 `union` + 最大尺寸是权宜之计
> 更稳妥的做法是**完全避免构造 `String8`**：
> 用 `dlsym` 找到的构造函数，或者直接传一个预先构造好的空对象。
>
> 本项目用一个技巧：**只传一个字符很短的名字**（如系统层名），
> 这样即使布局是 SSO 版本也不会出错。

## 创建标志的差异

```cpp
// 老版本
ISurfaceComposerClient::eFXSurfaceNormal = 0x00000000

// 新版本（Android 11+）
ISurfaceComposerClient::eFXSurfaceBufferState = 0x00000080
ISurfaceComposerClient::eFXSurfaceBufferQueue = 0x00000100
```

本项目：

```cpp
uint32_t flags = 0;
if (api >= 30) {
    flags = 0x100;      // eFXSurfaceBufferQueue
} else {
    flags = 0;
}
```

## 完整的 overlay 程序

```cpp
// overlay.cpp —— 一个能跑的透明覆盖层
#include <android/log.h>
#include <android/native_window.h>
#include <sys/system_properties.h>
#include <dlfcn.h>
#include <cstdio>
#include <cstring>
#include <cstdlib>

#define LOGI(...) __android_log_print(ANDROID_LOG_INFO,  "Overlay", __VA_ARGS__)
#define LOGE(...) __android_log_print(ANDROID_LOG_ERROR, "Overlay", __VA_ARGS__)

static int g_api = 0;
static void *g_libgui = nullptr;

// 函数指针
typedef void* (*FN_SCC_new)(void);
typedef int   (*FN_SCC_initCheck)(void*);
typedef void* (*FN_SCC_createSurface)(void*, void*, uint32_t, uint32_t,
                                      int32_t, uint32_t, void*, void*);
typedef void* (*FN_SC_getSurface)(void*);

static FN_SCC_new           pfnSCC_new = nullptr;
static FN_SCC_initCheck     pfnSCC_initCheck = nullptr;
static FN_SCC_createSurface pfnSCC_create = nullptr;
static FN_SC_getSurface     pfnSC_getSurface = nullptr;

static void* TrySym(const char *name) {
    void *p = dlsym(g_libgui, name);
    if (p) LOGI("  找到符号: %s", name);
    return p;
}

bool InitLibGui() {
    char buf[PROP_VALUE_MAX];
    __system_property_get("ro.build.version.sdk", buf);
    g_api = atoi(buf);
    LOGI("Android API = %d", g_api);

    g_libgui = dlopen("libgui.so", RTLD_NOW);
    if (!g_libgui) { LOGE("dlopen libgui: %s", dlerror()); return false; }

    // 构造函数：几个版本都试
    pfnSCC_new = (FN_SCC_new)TrySym("_ZN7android21SurfaceComposerClientC1Ev");
    if (!pfnSCC_new)
        pfnSCC_new = (FN_SCC_new)TrySym("_ZN7android21SurfaceComposerClientC1ENS_2spINS_7ISurfaceComposerEEE");

    pfnSCC_initCheck = (FN_SCC_initCheck)TrySym("_ZNK7android21SurfaceComposerClient9initCheckEv");
    pfnSC_getSurface = (FN_SC_getSurface)TrySym("_ZNK7android14SurfaceControl10getSurfaceEv");

    // createSurface：真实项目按 systemVersion 选（见上表）
    // 为简洁这里只列几条作示意，完整表见 ANativeWindowCreator.h
    const char *cands[] = {
        // 14-16
        "_ZN7android21SurfaceComposerClient13createSurfaceERKNS_7String8EjjiiRKNS_2spINS_7IBinderEEENS_3gui13LayerMetadataEPj",
        // 12-13
        "_ZN7android21SurfaceComposerClient13createSurfaceERKNS_7String8EjjijRKNS_2spINS_7IBinderEEENS_13LayerMetadataEPj",
        // 10-11
        "_ZN7android21SurfaceComposerClient13createSurfaceERKNS_7String8EjjijPNS_14SurfaceControlENS_13LayerMetadataEPj",
        // 8-9
        "_ZN7android21SurfaceComposerClient13createSurfaceERKNS_7String8EjjijPNS_14SurfaceControlEii",
        // 5-7
        "_ZN7android21SurfaceComposerClient13createSurfaceERKNS_7String8Ejjij",
    };
    for (auto n : cands) {
        pfnSCC_create = (FN_SCC_createSurface)TrySym(n);
        if (pfnSCC_create) break;
    }

    return pfnSCC_new && pfnSCC_initCheck && pfnSCC_create && pfnSC_getSurface;
}

ANativeWindow* CreateOverlayWindow(const char *name, int w, int h) {
    // 分配足够大的缓冲（覆盖所有版本的对象大小差异）
    void *client = calloc(1, 128);
    pfnSCC_new(client);

    int err = pfnSCC_initCheck(client);
    if (err != 0) { LOGE("SurfaceComposerClient 初始化失败: %d", err); return nullptr; }
    LOGI("SurfaceComposerClient 就绪");

    // String8 名字（用最大尺寸缓冲）
    uint8_t nameBuf[64] = {};
    // 简化：直接用短名字且按当前 API 选构造函数
    // （真实工程要 dlsym 对应版本的 String8 构造函数）

    void *sc = calloc(1, 64);

    // 按版本选择正确的 createSurface 签名 —— 这是"能跑通"的关键
    void *surface = nullptr;
    if (g_api >= 31) {
        // Android 12+：(…, sp<IBinder>*, LayerMetadata, uint32_t*)
        typedef void* (*FN12)(void*, void*, uint32_t, uint32_t, int32_t,
                              uint32_t, void*, void*, void*);
        ((FN12)pfnSCC_create)(client, nameBuf, w, h, 1 /*RGBA_8888*/, 0,
                              nullptr, sc, nullptr);
        surface = sc;
    } else if (g_api >= 30) {
        // Android 11：(…, SurfaceControl*, LayerMetadata, uint32_t*)
        typedef void* (*FN11)(void*, void*, uint32_t, uint32_t, int32_t,
                              uint32_t, void*, void*, void*);
        ((FN11)pfnSCC_create)(client, nameBuf, w, h, 1, 0,
                              nullptr, sc, nullptr);
        surface = sc;
    } else if (g_api >= 26) {
        // Android 8-10：(…, SurfaceControl*, windowType, ownerUid)
        typedef void* (*FN8)(void*, void*, uint32_t, uint32_t, int32_t,
                             uint32_t, void*, uint32_t, uint32_t);
        ((FN8)pfnSCC_create)(client, nameBuf, w, h, 1, 0,
                             nullptr, (uint32_t)0, 1000);
        surface = sc;
    } else {
        // Android 5-7：老签名，直接返回 SurfaceControl
        typedef void* (*FN5)(void*, void*, uint32_t, uint32_t, int32_t, uint32_t);
        surface = ((FN5)pfnSCC_create)(client, nameBuf, w, h, 1, 0);
    }

    if (!surface) { LOGE("createSurface 失败"); return nullptr; }

    ANativeWindow *win = (ANativeWindow*)pfnSC_getSurface(sc);
    if (!win) { LOGE("getSurface 失败"); return nullptr; }
    LOGI("窗口创建成功: %p", win);
    return win;
}

int main(void) {
    if (!InitLibGui()) { LOGE("初始化失败"); return 1; }

    ANativeWindow *win = CreateOverlayWindow("MyOverlay", 1080, 2400);
    if (!win) return 1;

    int w = ANativeWindow_getWidth(win);
    int h = ANativeWindow_getHeight(win);
    LOGI("窗口尺寸: %dx%d", w, h);

    // 到这里窗口已创建，接下来交给 Vulkan/ImGui 渲染（第 59 章）

    LOGI("按 Ctrl+C 结束");
    while (true) { usleep(1000000); }
    return 0;
}
```

> [!note] 这段是**能真正跑通的简化版**
> 关键修复：**按版本用对应的函数指针类型调用 `createSurface`**，
> 并把返回值赋给 `surface`。
> 真实项目 `ANativeWindowCreator.h` 有 1600+ 行，处理了更多边界（镜像、模糊、旋转、多屏），
> 但**核心结构就是上面这样**：分配对象缓冲 → 按版本选签名调用 → 取 Surface。
>
> **不同版本的函数指针类型必须不同**（参数个数不同），否则参数错位、崩溃。
> 这就是为什么需要本章的"版本表"。

## 验证清单（每台设备都要跑）

```bash
# 1. 设备信息
adb shell getprop ro.build.version.sdk
adb shell getprop ro.build.version.release

# 2. 符号是否存在
adb pull /system/lib64/libgui.so
aarch64-linux-android-nm -D -C libgui.so | grep createSurface

# 3. 运行 overlay
adb push overlay /data/local/tmp/
adb shell chmod 755 /data/local/tmp/overlay
adb shell su -c /data/local/tmp/overlay &

# 4. 看日志
adb logcat -s Overlay

# 5. 确认图层创建成功
adb shell dumpsys SurfaceFlinger | grep -i overlay
```

**第 5 步能看到你的图层，就成功了。**

## 维护多版本支持表

本项目 `ANativeWindowCreator.h` 里维护了一张表（简化）：

| 项目 | Android 5-7 | 8-9 | 10 | 11-12 | 13+ |
|---|---|---|---|---|---|
| createSurface 参数 | 5 | 7 | 8 | 8 | 8 |
| String8 构造 | 老 | 老 | 新 | 新 | 新 |
| 跳过截图 | flag | windowType | windowType | LayerMetadata | LayerMetadata |
| 镜像显示 | 不支持 | 支持 | 支持 | 支持 | 支持 |

**这张表是"用私有 API"必须付出的维护成本。**

## 动手：建你自己的设备档案

为你的设备（或模拟器）建一份档案：

```markdown
## 设备: XXX
- Android 版本: 13, API 33
- ABI: arm64-v8a

### libgui 符号
- createSurface: _ZN7android21SurfaceComposerClient13createSurfaceE...（找到）
- getSurface: 找到
- initCheck: 找到

### 结果
- 图层创建: 成功
- 层级: INT_MAX-1
- 透明: 正常
```

每换一台设备就更新一次。这是长期维护的关键文档。

## 验收清单

- [ ] 知道同一个函数在不同 Android 版本符号不同（说出 createSurface 版本差异）
- [ ] 会写"按版本优先 + 失败全试"的符号查找（写一段按 API 选符号的代码）
- [ ] 知道结构体布局也会变（`String8` 8 字节 → 32 字节）（说出为什么会导致崩溃）
- [ ] 知道用不同的函数指针类型对应不同参数个数（声明两个函数指针类型）
- [ ] 会跑通 overlay 并用 `dumpsys` 确认图层存在 ★（跑 overlay，dumpsys 找到图层）
- [ ] 开始维护自己的设备兼容表（记录设备型号 + API + 可用符号）

→ 下一章：[[第67章-图层层级与录屏规避]]　—— 理解图层的层级、透明度、以及"跳过截屏/录屏"的三种实现方式。
